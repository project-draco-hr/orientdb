{
  if (unaligned)   return unsafe.getShort(pointer);
  if (alignedOrder.equals(ByteOrder.BIG_ENDIAN))   return (short)(unsafe.getByte(pointer++) << 8 | (unsafe.getByte(pointer) & 0xff));
  return (short)((unsafe.getByte(pointer++) & 0xff) | (unsafe.getByte(pointer) << 8));
}
