{
  if (config instanceof OStorageMemoryClusterConfiguration)   return new OClusterMemoryArrayList();
 else   if (config instanceof OStorageMemoryLinearHashingClusterConfiguration)   return new OClusterMemoryHashing();
 else   throw new OStorageException("Cluster type '" + config + "' is not supported. Supported types are: "+ Arrays.toString(TYPES));
}
