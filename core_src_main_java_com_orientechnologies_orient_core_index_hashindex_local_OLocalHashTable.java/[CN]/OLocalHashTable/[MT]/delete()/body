{
  acquireExclusiveLock();
  try {
    for (int i=0; i < filesMetadata.length; i++) {
      if (filesMetadata[i] != null)       diskCache.deleteFile(fileLevelIds[i]);
    }
    metadataStore.delete();
    treeStateStore.delete();
    if (nullKeyIsSupported)     diskCache.deleteFile(nullBucketFileId);
  }
 catch (  IOException e) {
    throw new OIndexException("Exception during index deletion",e);
  }
 finally {
    releaseExclusiveLock();
  }
}
