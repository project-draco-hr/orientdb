{
  acquireExclusiveLock();
  try {
    truncate();
    for (    OFile file : files) {
      OMMapManager.removeFile(file);
      file.delete();
    }
    files=null;
    holeSegment.delete();
  }
  finally {
    releaseExclusiveLock();
  }
}
