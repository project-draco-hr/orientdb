{
  acquireExclusiveLock();
  try {
    flush();
    for (    final OIndex<?> idx : indexes.values())     if (idx instanceof OCloseable)     ((OCloseable)idx).close();
    clearMetadata();
  }
  finally {
    releaseExclusiveLock();
  }
}
