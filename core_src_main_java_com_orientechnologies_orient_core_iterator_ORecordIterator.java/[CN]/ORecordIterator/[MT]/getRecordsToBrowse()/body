{
  long recordsToBrowse=currentClusterPosition > -2 && lastClusterPosition > -1 ? lastClusterPosition - currentClusterPosition : 0;
  if (txEntries != null)   recordsToBrowse+=txEntries.size() - (currentTxEntryPosition + 1);
  return recordsToBrowse;
}
