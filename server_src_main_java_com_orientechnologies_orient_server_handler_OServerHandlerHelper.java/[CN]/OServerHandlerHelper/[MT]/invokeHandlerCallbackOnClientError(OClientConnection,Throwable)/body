{
  final List<OServerHandler> handlers=OServerMain.server().getHandlers();
  if (handlers != null)   for (  OServerHandler handler : handlers) {
    handler.onClientError(connection,iThrowable);
  }
}
