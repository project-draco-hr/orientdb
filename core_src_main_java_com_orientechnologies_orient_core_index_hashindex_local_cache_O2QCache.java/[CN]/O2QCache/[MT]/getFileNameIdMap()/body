{
synchronized (syncObject) {
    Map<String,Long> fileNameIdMap=new HashMap<String,Long>();
    for (    Map.Entry<Long,OMultiFileSegment> fileSegmentEntry : files.entrySet())     fileNameIdMap.put(fileSegmentEntry.getValue().getName(),fileSegmentEntry.getKey());
    return fileNameIdMap;
  }
}
