{
  if (!connect(nodeId,dbName,mode))   return;
  final ODistributedNode dNode=getOrCreateDistributedNode(nodeId);
  ODistributedDatabaseInfo db=dNode.getOrCreateDatabaseEntry(dbName);
  dNode.startDatabaseReplication(db);
}
