{
  if (!database.getClusterNames().contains("Animal"))   database.getStorage().addCluster("Animal",OClusterLocal.TYPE);
  if (!database.getClusterNames().contains("Vaccinate"))   database.getStorage().addCluster("Vaccinate",OClusterLocal.TYPE);
}
