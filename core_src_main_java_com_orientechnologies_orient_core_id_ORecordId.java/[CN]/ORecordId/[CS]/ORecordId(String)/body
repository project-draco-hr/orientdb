{
  if (!iRecordId.contains(SEPARATOR))   throw new IllegalArgumentException("Argument is not a RecordId in form of string");
  final String parts[]=iRecordId.split(SEPARATOR);
  clusterId=Integer.parseInt(parts[0]);
  checkClusterLimits();
  clusterPosition=Long.parseLong(parts[1]);
}
