{
  if (mainBucketsSize <= 2)   return;
  final double currentCapacity=1.0 * size / (mainBucketsSize * OClusterLocalLHPEBucket.BUCKET_CAPACITY);
  if (currentCapacity < storageMergeOverflow)   mergeBuckets();
}
