{
  if (iStrategy.startsWith("$"))   iStrategy=iStrategy.substring(1);
  final OReplicationStrategy strategy=strategies.get(iStrategy);
  if (strategy == null)   throw new ODistributedException("Configured strategy '" + iStrategy + "' is not configured");
  return strategy;
}
