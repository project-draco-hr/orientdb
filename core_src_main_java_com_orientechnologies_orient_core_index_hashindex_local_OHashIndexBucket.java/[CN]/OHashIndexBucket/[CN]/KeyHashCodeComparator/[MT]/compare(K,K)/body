{
  final long hashCodeOne=keyHashFunction.hashCode(keyOne);
  final long hashCodeTwo=keyHashFunction.hashCode(keyTwo);
  if (hashCodeOne > hashCodeTwo)   return 1;
  if (hashCodeOne < hashCodeTwo)   return -1;
  return comparator.compare(keyOne,keyTwo);
}
