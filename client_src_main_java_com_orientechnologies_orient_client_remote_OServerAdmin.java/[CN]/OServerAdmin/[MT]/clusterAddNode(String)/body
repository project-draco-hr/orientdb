{
  final ODocument response=sendRequest(OChannelBinaryProtocol.REQUEST_CLUSTER,new ODocument().field("operation","addNode").field("node",iRemoteNode),"Cluster add node");
  OLogManager.instance().debug(this,"Added node '%s' to the cluster",iRemoteNode);
  return response;
}
