{
  final String result=workload.getPartialResult();
  if (lastResult == null || !lastResult.equals(result))   System.out.print("\rStress test in progress " + result);
  lastResult=result;
  try {
    Thread.sleep(200);
  }
 catch (  InterruptedException e) {
    interruptCurrentOperation();
  }
}
