{
  if (key != null && lastSearchKey != null)   if (comparator != null)   return comparator.compare((K)key,(K)lastSearchKey) == 0 ? lastSearchNode : null;
 else   try {
    if (key instanceof OCompositeKey)     return key.equals(lastSearchKey) ? lastSearchNode : null;
    return ((Comparable<? super K>)key).compareTo((K)lastSearchKey) == 0 ? lastSearchNode : null;
  }
 catch (  Exception e) {
  }
  return null;
}
