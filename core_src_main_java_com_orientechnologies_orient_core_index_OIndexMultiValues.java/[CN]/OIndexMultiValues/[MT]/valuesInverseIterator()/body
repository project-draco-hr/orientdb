{
  checkForRebuild();
  acquireExclusiveLock();
  try {
    return new OSharedResourceIterator(this,new OMultiCollectionIterator<OIdentifiable>(((OMVRBTree.Values)map.values()).inverseIterator()));
  }
  finally {
    releaseExclusiveLock();
  }
}
