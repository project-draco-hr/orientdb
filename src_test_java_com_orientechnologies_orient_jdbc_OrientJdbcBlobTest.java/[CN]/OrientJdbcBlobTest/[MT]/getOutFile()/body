{
  File binaryFile=new File("./target/working/output_blob.pdf");
  if (binaryFile.exists()) {
    do {
      binaryFile.delete();
    }
 while (binaryFile.exists());
  }
 else   binaryFile.mkdirs();
  return binaryFile;
}
