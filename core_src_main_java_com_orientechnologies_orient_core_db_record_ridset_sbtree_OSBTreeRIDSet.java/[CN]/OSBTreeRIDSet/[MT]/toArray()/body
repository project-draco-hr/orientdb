{
  final ArrayList<OIdentifiable> list=new ArrayList<OIdentifiable>(size());
  for (  OIdentifiable identifiable : this) {
    if (autoConvertToRecord)     identifiable=identifiable.getRecord();
    list.add(identifiable);
  }
  return list.toArray();
}
