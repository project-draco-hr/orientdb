{
  if (!closed) {
    stopFlush(flush);
    rndFile.close();
    closed=true;
    if (!pagesCache.isEmpty()) {
      for (      OWALPage page : pagesCache)       directMemory.free(page.getPagePointer());
    }
    currentPage=null;
  }
}
