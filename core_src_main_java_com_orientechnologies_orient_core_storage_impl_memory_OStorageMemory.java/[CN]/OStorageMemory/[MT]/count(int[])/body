{
  final boolean locked=acquireSharedLock();
  try {
    long tot=0;
    for (int i=0; i < iClusterIds.length; ++i)     tot+=clusters.get(iClusterIds[i]).getEntries();
    return tot;
  }
  finally {
    releaseSharedLock(locked);
  }
}
