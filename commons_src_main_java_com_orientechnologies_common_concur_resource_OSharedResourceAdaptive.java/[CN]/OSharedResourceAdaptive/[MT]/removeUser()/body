{
  if (users < 1)   throw new IllegalStateException("Can't remove user of the shared resource " + toString() + " because no user is using it");
  try {
    lock.writeLock().lock();
    return --users;
  }
  finally {
    lock.writeLock().unlock();
  }
}
