{
  final List<Object> list=new ArrayList<Object>();
  for (  Object o : result) {
    if (o != null) {
      final Object fieldValue=source.field(o.toString());
      if (fieldValue == null && nullAsEmpty)       list.add("");
 else       list.add(fieldValue);
    }
  }
  return list;
}
