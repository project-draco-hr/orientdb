{
  final int size=in.readInt();
  for (int i=0; i < size; ++i)   tasks.add((OAbstractRemoteTask)in.readObject());
  final int lockSize=in.readInt();
  for (int i=0; i < lockSize; ++i)   locks.add((ORID)in.readObject());
}
