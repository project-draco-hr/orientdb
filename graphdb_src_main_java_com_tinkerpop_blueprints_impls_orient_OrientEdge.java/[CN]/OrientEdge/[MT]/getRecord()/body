{
  if (rawElement == null) {
    final ODocument tmp=new ODocument(getClassName(label)).setTrackingChanges(false);
    tmp.field("in",vIn);
    tmp.field("out",vOut);
    if (label != null && settings != null && !settings.isUseClassForEdgeLabel())     tmp.field("label",label);
    return tmp;
  }
  return super.getRecord();
}
