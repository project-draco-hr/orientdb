{
  try {
    for (long i=end; i < iIndex - 1; ++i)     input.read();
    start=iIndex;
    final int byteRead=input.read(buffer);
    end=start + byteRead;
    current=start;
  }
 catch (  IOException e) {
    throw OException.wrapException(new OIOException("Error in read"),e);
  }
}
