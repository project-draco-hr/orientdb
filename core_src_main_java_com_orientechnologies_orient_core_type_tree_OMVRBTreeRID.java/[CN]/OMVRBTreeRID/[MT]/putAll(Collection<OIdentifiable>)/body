{
  final long timer=Orient.instance().getProfiler().startChrono();
  try {
    for (    OIdentifiable rid : coll)     internalPut(rid,null);
    commitChanges();
  }
  finally {
    Orient.instance().getProfiler().stopChrono("OMVRBTreePersistent.putAll",timer);
  }
}
