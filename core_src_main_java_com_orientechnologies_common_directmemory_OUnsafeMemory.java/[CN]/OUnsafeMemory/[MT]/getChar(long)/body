{
  if (unaligned)   return unsafe.getChar(pointer);
  if (alignedOrder.equals(ByteOrder.BIG_ENDIAN))   return (char)((unsafe.getByte(pointer++) << 8) | (unsafe.getByte(pointer) & 0xff));
  return (char)((unsafe.getByte(pointer++) & 0xff) | (unsafe.getByte(pointer) << 8));
}
