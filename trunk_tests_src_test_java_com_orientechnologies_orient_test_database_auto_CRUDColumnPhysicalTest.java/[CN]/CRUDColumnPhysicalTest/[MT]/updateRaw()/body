{
  database.open("admin","admin");
  int i=0;
  for (  ORecordColumn rec : database.browseCluster(CLUSTER_NAME)) {
    if (i % 2 == 0)     rec.field(4,"Spain");
    rec.field(5,String.valueOf(Float.parseFloat(rec.field(5)) + 100f));
    rec.save();
    i++;
  }
  database.close();
}
