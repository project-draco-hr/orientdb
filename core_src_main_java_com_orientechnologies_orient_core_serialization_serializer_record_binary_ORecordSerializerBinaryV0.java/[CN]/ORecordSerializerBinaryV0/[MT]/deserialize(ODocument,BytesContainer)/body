{
  String className=readString(bytes);
  if (className != null)   document.setClassNameIfExists(className);
  int last=0;
  String field;
  while ((field=readString(bytes)) != null) {
    int valuePos=OIntegerSerializer.INSTANCE.deserialize(bytes.bytes,bytes.offset);
    bytes.read(OIntegerSerializer.INT_SIZE);
    OType type=readOType(bytes);
    if (document.containsField(field))     continue;
    if (valuePos != 0) {
      int headerCursor=bytes.offset;
      bytes.offset=valuePos;
      Object value=readSingleValue(bytes,type,document);
      if (bytes.offset > last)       last=bytes.offset;
      bytes.offset=headerCursor;
      if (document.fieldType(field) != null || OType.LINK == type)       document.field(field,value);
 else       document.field(field,value,type);
    }
 else     document.field(field,(Object)null);
  }
  if (last > bytes.offset)   bytes.offset=last;
}
