{
  if (!database.getClusterNames().contains("Animal"))   database.getStorage().addCluster("Animal",OStorage.TYPE_PHYSICAL);
  if (!database.getClusterNames().contains("Vaccinate"))   database.getStorage().addCluster("Vaccinate",OStorage.TYPE_PHYSICAL);
}
