{
  latch.await();
  try {
    for (int i=0; i < limit; i++) {
      dictionary.add((long)i,new CItem(i));
    }
  }
 catch (  Exception e) {
    e.printStackTrace();
    throw e;
  }
  return null;
}
