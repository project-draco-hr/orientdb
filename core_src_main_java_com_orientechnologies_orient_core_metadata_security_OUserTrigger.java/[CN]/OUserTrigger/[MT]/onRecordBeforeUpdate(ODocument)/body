{
  if ("OUser".equals(iRecord.getClassName())) {
    final String password=(String)iRecord.field("password");
    if (iRecord.getOriginalValue("password") != null)     iRecord.field("password",OUser.encryptPassword(password));
  }
}
