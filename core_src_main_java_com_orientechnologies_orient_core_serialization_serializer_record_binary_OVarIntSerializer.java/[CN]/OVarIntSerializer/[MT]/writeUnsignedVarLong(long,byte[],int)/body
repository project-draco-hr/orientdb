{
  int size=0;
  while ((value & 0xFFFFFFFFFFFFFF80L) != 0L) {
    bos[pos + size]=(byte)(value & 0x7F | 0x80);
    value>>>=7;
    size++;
  }
  bos[pos + size]=(byte)(value & 0x7F);
  size++;
  return size;
}
