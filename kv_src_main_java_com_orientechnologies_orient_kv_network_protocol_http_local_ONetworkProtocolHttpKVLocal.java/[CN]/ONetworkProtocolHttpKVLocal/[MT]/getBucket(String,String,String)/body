{
  ODatabaseBinary db=null;
  db=(ODatabaseBinary)OServerMain.server().getMemoryDatabases().get(dbName);
  if (db != null)   return getBucketFromMemory(dbName,iBucketName);
 else   return getBucketFromDatabase(dbName,iAuthorization,iBucketName);
}
