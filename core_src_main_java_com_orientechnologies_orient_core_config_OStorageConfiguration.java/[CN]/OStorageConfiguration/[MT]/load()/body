{
  final byte[] record=storage.readRecord(null,-1,CONFIG_RID,null).buffer;
  if (record == null)   throw new OStorageException("Can't load database's configuration. The database seems to be corrupted.");
  fromStream(record);
  return this;
}
