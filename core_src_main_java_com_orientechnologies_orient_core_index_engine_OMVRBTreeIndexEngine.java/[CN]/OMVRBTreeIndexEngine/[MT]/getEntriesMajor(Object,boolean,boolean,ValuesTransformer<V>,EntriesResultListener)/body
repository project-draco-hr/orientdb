{
  acquireExclusiveLock();
  try {
    if (ascOrder)     getEntriesMajorAscOrder(fromKey,isInclusive,transformer,entriesResultListener);
 else     getEntriesMajorDescOrder(fromKey,isInclusive,transformer,entriesResultListener);
  }
  finally {
    releaseExclusiveLock();
  }
}
