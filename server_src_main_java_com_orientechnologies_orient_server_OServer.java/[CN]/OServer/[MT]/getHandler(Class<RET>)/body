{
  try {
    startupLatch.await();
  }
 catch (  InterruptedException e) {
    Thread.currentThread().interrupt();
  }
  for (  OServerHandler h : plugins.values())   if (h.getClass().equals(iHandlerClass))   return (RET)h;
  return null;
}
