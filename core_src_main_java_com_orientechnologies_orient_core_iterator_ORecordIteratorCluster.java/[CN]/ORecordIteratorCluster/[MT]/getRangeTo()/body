{
  if (!liveUpdated)   return lastClusterPosition;
  final long limit=database.getStorage().getClusterLastEntryPosition(currentClusterId);
  if (rangeTo > -1)   return Math.min(rangeTo,limit);
  return limit;
}
