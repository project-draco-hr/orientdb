{
  checkConnection();
  if (iClusterName == null)   return null;
  boolean locked=acquireSharedLock();
  try {
    return clustersTypes.get(iClusterName.toLowerCase());
  }
  finally {
    releaseSharedLock(locked);
  }
}
