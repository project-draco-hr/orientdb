{
  final OType propertyType=property.getType();
  if (propertyType.equals(OType.EMBEDDEDLIST) || propertyType.equals(OType.EMBEDDEDSET) || propertyType.equals(OType.LINKLIST)|| propertyType.equals(OType.LINKSET))   return property.getLinkedType();
  return propertyType;
}
