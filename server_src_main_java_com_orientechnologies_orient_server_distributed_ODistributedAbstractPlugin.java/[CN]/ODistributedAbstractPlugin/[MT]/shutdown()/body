{
  if (!enabled)   return;
  for (  ODistributedStorage s : storages.values())   try {
    s.close();
  }
 catch (  Exception e) {
  }
  storages.clear();
  Orient.instance().removeDbLifecycleListener(this);
}
