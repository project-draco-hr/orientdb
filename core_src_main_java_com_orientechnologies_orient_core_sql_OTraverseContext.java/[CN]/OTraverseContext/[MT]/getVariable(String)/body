{
  if ("depth".equalsIgnoreCase(iName))   return history.size();
 else   if ("path".equalsIgnoreCase(iName))   return path;
 else   if ("history".equalsIgnoreCase(iName))   return history;
  return null;
}
