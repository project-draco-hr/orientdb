{
  if (commands != null && commands.length > 0) synchronized (getClass()) {
    if (cmdManager == null) {
      cmdManager=new OHttpNetworkCommandManager();
      for (      Object cmd : commands)       cmdManager.registerCommand(cmd);
    }
  }
}
