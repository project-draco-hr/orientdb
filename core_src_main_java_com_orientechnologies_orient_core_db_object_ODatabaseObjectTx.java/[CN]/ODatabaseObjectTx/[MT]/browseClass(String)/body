{
  checkSecurity(ODatabaseSecurityResources.CLASS + "." + iClassName,ORole.CRUD_OPERATIONS.READ);
  return new OObjectIteratorMultiCluster<RET>(this,(ODatabaseRecordAbstract<ODocument>)getUnderlying().getUnderlying(),getMetadata().getSchema().getClass(iClassName).getClusterIds());
}
