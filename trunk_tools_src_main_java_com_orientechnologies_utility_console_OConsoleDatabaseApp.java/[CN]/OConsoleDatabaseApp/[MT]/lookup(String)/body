{
  checkCurrentDatabase();
  currentRecord=(ORecordVObject)currentDatabase.getDictionary().get(iKey);
  if (currentRecord == null)   out.println("Entry not found in dictionary.");
 else   displayObject(null);
}
