{
  if (recordingFrom < 0)   return;
  for (  Map.Entry<OProfilerHookValue,String> entry : hooks.entrySet()) {
    if (entry.getValue().equals(iName)) {
      hooks.remove(entry.getKey());
      break;
    }
  }
}
