{
  if (isDirty) {
    if (evictedPages.size() >= writeQueueLength)     flushEvictedPages();
    evictedPages.put(new FileLockKey(fileId,pageIndex),dataPointer);
  }
 else {
    directMemory.free(dataPointer);
  }
}
