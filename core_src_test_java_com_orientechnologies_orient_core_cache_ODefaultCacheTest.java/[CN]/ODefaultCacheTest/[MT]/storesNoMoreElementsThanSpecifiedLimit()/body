{
  OCache sut=enabledCache();
  for (int i=sut.limit() + 2; i > 0; i--)   sut.put(new ODocument(new ORecordId(i,i)));
  assertEquals(sut.size(),sut.limit(),"Cache doesn't meet limit requirements");
}
