{
  acquireWriteLock();
  try {
    if (headerBuffer == null)     return;
    headerBuffer.put(SOFTLY_CLOSED_OFFSET,(byte)(value ? 1 : 0));
    setHeaderDirty();
    flushHeader();
  }
  finally {
    releaseWriteLock();
  }
}
