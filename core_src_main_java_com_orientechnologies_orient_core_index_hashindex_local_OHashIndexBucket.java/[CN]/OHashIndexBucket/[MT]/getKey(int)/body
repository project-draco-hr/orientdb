{
  final int positionIndex=OIntegerSerializer.INSTANCE.deserializeNative(dataBuffer,positionsArrayOffset + index * OIntegerSerializer.INT_SIZE + dataBufferOffset);
  final int bufferPosition=positionIndex * maxKeySize + keyLineOffset + dataBufferOffset;
  return keySerializer.deserializeNative(dataBuffer,bufferPosition);
}
