{
  final OIdentifiable value=underlying.next();
  if (value == null)   return null;
  if (value instanceof ORecordId && autoConvert2Record)   return ((ORecordId)value).getRecord();
  return value;
}
