{
  final OMVRBTreeEntry<K,V> p=getEntry(key,true,PartialSearchMode.HIGHEST_BOUNDARY);
  if (p == null)   return null;
  if (pageItemFound)   return next(p);
 else   if (pageIndex < p.getSize())   return p;
  return null;
}
