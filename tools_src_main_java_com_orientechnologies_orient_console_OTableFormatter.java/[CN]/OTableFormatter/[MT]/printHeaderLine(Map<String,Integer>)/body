{
  if (iColumns.size() > 0) {
    int i=0;
    for (    Entry<String,Integer> col : iColumns.entrySet()) {
      if (i++ > 0)       out.print("+");
      for (int k=0; k < col.getValue(); ++k)       out.print("-");
    }
  }
  out.print("\n");
}
