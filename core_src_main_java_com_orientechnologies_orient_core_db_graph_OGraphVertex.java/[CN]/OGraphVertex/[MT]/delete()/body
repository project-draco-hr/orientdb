{
  if (inEdges != null && inEdges.get() != null) {
    for (    OGraphEdge e : inEdges.get())     e.delete();
  }
  if (outEdges != null && outEdges.get() != null) {
    for (    OGraphEdge e : outEdges.get())     e.delete();
  }
  return this;
}
