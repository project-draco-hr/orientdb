{
  try {
    final ODatabaseCompare compare;
    if (iUserName == null)     compare=new ODatabaseCompare(iDb1URL,iDb2URL,this);
 else     compare=new ODatabaseCompare(iDb1URL,iDb1URL,iUserName,iUserPassword,this);
    compare.compare();
  }
 catch (  ODatabaseExportException e) {
    printError(e);
  }
}
