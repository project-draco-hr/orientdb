{
  if (addValues.contains(o))   return true;
  if (removedValues.contains(o))   return false;
  final OSBTreeBonsai<OIdentifiable,Boolean> tree=loadTree();
  if (tree == null)   return false;
  try {
    return tree.get(o) != null;
  }
  finally {
    releaseTree();
  }
}
