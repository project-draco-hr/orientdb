{
  if (transformer == null)   return concurrentHashMap.size();
 else {
    long counter=0;
    for (    V value : concurrentHashMap.values()) {
      counter+=transformer.transform(value).size();
    }
    return counter;
  }
}
