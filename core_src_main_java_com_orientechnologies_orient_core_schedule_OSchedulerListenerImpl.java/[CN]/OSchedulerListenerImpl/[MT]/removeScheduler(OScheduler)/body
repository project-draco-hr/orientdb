{
  if (scheduler.isRunning())   throw new OSystemException("Cannot delete scheduler " + scheduler.getSchduleName() + " due to it is still running");
  schedulers.remove(scheduler.getSchduleName());
}
