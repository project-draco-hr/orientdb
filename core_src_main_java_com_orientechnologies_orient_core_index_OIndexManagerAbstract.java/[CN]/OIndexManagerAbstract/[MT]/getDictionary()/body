{
  OIndex<?> idx=getIndex(DICTIONARY_NAME);
  if (idx == null)   idx=createIndex(DICTIONARY_NAME,OProperty.INDEX_TYPE.DICTIONARY.toString(),OType.STRING,null,null,null,false);
  return new ODictionary<ORecordInternal<?>>((OIndex<OIdentifiable>)idx);
}
