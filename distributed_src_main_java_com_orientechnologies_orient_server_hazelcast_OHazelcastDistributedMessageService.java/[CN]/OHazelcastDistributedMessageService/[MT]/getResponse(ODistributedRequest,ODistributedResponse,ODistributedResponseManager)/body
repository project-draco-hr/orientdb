{
switch (iRequest.getPayload().getResultStrategy()) {
case FIRST_RESPONSE:
    return firstResponse;
case MERGE:
  return currentResponseMgr.merge(new OHazelcastDistributedResponse(firstResponse.getRequestId(),null,firstResponse.getSenderNodeName(),firstResponse.getSenderThreadId(),null));
}
return firstResponse;
}
