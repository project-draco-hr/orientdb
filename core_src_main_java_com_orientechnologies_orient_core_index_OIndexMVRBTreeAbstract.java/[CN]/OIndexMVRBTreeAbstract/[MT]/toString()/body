{
  if (tryAcquireExclusiveLock())   try {
    return name + " (" + (type != null ? type : "?")+ ")"+ (map != null ? " " + map : "");
  }
  finally {
    releaseExclusiveLock();
  }
  return "!Locked resource";
}
