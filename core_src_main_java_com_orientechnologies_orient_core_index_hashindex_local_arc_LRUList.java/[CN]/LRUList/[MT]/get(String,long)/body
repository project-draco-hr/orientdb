{
  long hashCode=hashCode(fileName,pageIndex);
  int index=index(hashCode);
  LRUEntry lruEntry=entries[index];
  while (lruEntry != null && (lruEntry.hashCode != hashCode || lruEntry.pageIndex != pageIndex || !lruEntry.fileName.equals(fileName)))   lruEntry=lruEntry.next;
  return lruEntry;
}
