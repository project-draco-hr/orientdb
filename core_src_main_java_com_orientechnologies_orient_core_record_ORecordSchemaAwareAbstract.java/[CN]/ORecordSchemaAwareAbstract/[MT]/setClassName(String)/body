{
  if (_database == null || iClassName == null) {
    _clazz=null;
    return;
  }
  setClass(_database.getMetadata().getSchema().getClass(iClassName));
  if (_clazz == null)   setClass(_database.getMetadata().getSchema().createClass(iClassName));
}
