{
  final byte serializerId=stream[startPosition];
  return OBinarySerializerFactory.TYPE_IDENTIFIER_SIZE + OBinarySerializerFactory.INSTANCE.getObjectSerializer(serializerId).getObjectSize(stream,startPosition + OBinarySerializerFactory.TYPE_IDENTIFIER_SIZE);
}
