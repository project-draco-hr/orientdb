{
  if (fileReader == null)   return false;
  try {
    final boolean res=fileReader.ready();
    if (!res)     end();
    return res;
  }
 catch (  IOException e) {
    throw new OExtractorException(e);
  }
}
