{
  if (newItems == null || newItems.size() == 0)   return delegate.rawIterator();
  lazyLoad(false);
  return new OLazyRecordMultiIterator(delegate.sourceRecord,delegate.database,delegate.recordType,new Object[]{delegate.iterator(),newItems.keySet().iterator()},false);
}
