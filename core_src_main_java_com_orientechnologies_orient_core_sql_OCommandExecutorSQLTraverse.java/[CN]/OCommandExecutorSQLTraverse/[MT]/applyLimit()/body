{
  if (tempResult != null && limit > 0) {
    final List<OIdentifiable> newList=new ArrayList<OIdentifiable>();
    final int tot=Math.min(limit,tempResult.size());
    for (int i=0; i < tot; ++i)     newList.add(tempResult.get(i));
    tempResult.clear();
    tempResult=newList;
  }
}
