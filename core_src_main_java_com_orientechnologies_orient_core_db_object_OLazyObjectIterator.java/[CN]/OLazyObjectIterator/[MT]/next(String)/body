{
  final Object value=underlying.next();
  if (value == null)   return null;
  if (value instanceof ODocument)   return (TYPE)database.getUserObjectByRecord((ODocument)value,iFetchPlan);
  return (TYPE)value;
}
