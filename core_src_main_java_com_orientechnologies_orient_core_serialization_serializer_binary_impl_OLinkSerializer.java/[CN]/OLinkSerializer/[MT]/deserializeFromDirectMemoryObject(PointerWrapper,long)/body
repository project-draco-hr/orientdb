{
  final int clusterId=OShortSerializer.INSTANCE.deserializeFromDirectMemory(wrapper,offset);
  final long clusterPosition=OLongSerializer.INSTANCE.deserialize(wrapper.get(offset + OShortSerializer.SHORT_SIZE,OLongSerializer.LONG_SIZE),0);
  return new ORecordId(clusterId,clusterPosition);
}
