{
  if (database.getClusterIdByName(CLUSTER_NAME) < 0)   database.addCluster(CLUSTER_NAME,OStorage.CLUSTER_TYPE.PHYSICAL);
  startRecordNumber=database.countClusterElements(CLUSTER_NAME);
  for (long i=startRecordNumber; i < startRecordNumber + TOT_RECORDS; ++i) {
    record.reset();
    record.value(i + "-binary test").save(CLUSTER_NAME);
  }
}
