{
  database.open("admin","admin");
  try {
    database.command(new OSQLSynchQuery<ODocument>("SELECT FLATTEN( outEdges ), inEdges FROM OGraphVertex WHERE outEdges TRAVERSE(1,1) (@class = 'OGraphEdge')")).execute();
  }
  finally {
    database.close();
  }
}
