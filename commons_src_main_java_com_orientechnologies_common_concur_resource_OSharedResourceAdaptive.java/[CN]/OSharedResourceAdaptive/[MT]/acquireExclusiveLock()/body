{
  if (users > 1) {
    lock.writeLock().lock();
    if (runningWithoutLock)     while (runningWithoutLock)     try {
      Thread.sleep(UNLOCKED_WAIT_TIME);
    }
 catch (    InterruptedException e) {
    }
    return true;
  }
  runningWithoutLock=true;
  return false;
}
