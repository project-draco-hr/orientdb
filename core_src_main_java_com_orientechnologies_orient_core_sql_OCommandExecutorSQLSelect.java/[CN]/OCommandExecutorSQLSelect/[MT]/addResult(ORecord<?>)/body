{
  ODocument doc=(ODocument)iRecord;
  if (orderedFields != null || flattenField != null) {
    if (tempResult == null)     tempResult=new ArrayList<ODocument>();
    tempResult.add(doc);
  }
 else   processRecordAsResult(doc);
}
