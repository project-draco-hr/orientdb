{
  if (diffClass.equals(BinaryDiff.class))   return 1;
  if (diffClass.equals(IntDiff.class))   return 2;
  if (diffClass.equals(LongDiff.class))   return 3;
  throw new IllegalArgumentException("Unknown Diff class " + diffClass);
}
