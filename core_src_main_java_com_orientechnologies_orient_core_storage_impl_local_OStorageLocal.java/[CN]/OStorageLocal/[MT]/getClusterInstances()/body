{
  final Set<OCluster> result=new HashSet<OCluster>();
  lock.acquireSharedLock();
  try {
    for (    OCluster c : clusters)     result.add(c);
  }
  finally {
    lock.releaseSharedLock();
  }
  return result;
}
