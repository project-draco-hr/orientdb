{
  final StringBuilder indexName=new StringBuilder();
  indexName.append(iClass.getName());
  indexName.append('.');
  boolean first=true;
  for (  String f : iFields) {
    if (first)     first=false;
 else     indexName.append('_');
    indexName.append(f);
  }
  return indexName.toString();
}
