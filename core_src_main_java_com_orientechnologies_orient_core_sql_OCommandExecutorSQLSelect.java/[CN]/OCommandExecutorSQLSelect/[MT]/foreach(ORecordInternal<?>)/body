{
  if (filter(iRecord)) {
    resultCount++;
    addResult(iRecord.copy());
    if (limit > -1 && resultCount >= limit)     return false;
    if (request.getLimit() > -1 && resultCount >= request.getLimit())     return false;
  }
  return true;
}
