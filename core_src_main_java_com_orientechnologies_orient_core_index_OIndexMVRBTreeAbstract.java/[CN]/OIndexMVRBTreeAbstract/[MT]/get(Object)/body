{
  acquireSharedLock();
  try {
    final List<ORecord<?>> values=map.get(iKey);
    if (values == null)     return Collections.EMPTY_LIST;
    return values;
  }
  finally {
    releaseSharedLock();
  }
}
