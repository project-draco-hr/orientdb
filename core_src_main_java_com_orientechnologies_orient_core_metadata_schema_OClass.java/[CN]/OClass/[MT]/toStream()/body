{
  field("name",name);
  field("id",id);
  field("defaultClusterId",defaultClusterId);
  field("clusterIds",clusterIds);
  field("properties",properties.values(),OType.EMBEDDEDSET);
  if (superClass != null)   field("superClass",superClass.getName());
  return super.toStream();
}
