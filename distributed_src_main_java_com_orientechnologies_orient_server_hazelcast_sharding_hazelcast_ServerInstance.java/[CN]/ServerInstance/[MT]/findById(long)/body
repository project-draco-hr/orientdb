{
  if (localNode.getNodeId() == id)   return localNode;
  final Member member=idMemberMap.get(id);
  if (member == null)   return null;
  return new OHazelcastDHTNodeProxy(id,member,hazelcastInstance);
}
