{
  final ODocument doc=(ODocument)getConfigurationMap().get(CONFIG_NODE_PREFIX + iNodeId);
  if (doc == null)   ODistributedServerLog.error(this,getLocalNodeName(),null,DIRECTION.OUT,"Cannot find node with id '%s'",iNodeId);
  return doc;
}
