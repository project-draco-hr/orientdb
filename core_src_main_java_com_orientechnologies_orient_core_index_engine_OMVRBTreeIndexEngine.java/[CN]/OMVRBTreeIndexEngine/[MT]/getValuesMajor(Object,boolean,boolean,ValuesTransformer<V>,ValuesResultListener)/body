{
  acquireExclusiveLock();
  try {
    if (ascSortOrder)     getValuesMajorAscOrder(fromKey,isInclusive,transformer,valuesResultListener);
 else     getValuesMajorDescOrder(fromKey,isInclusive,transformer,valuesResultListener);
  }
  finally {
    releaseExclusiveLock();
  }
}
