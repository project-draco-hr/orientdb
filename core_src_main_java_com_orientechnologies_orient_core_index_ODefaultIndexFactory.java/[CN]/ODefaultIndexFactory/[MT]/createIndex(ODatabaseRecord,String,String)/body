{
  if (SBTREE_ALGORITHM.equals(algorithm))   return createSBTreeIndex(indexType);
  if (MVRBTREE_ALGORITHM.equals(algorithm) || algorithm == null)   return createMRBTreeIndex(indexType);
  throw new OConfigurationException("Unsupported type : " + indexType);
}
