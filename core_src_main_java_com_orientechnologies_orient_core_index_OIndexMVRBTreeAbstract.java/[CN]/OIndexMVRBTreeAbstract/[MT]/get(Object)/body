{
  acquireSharedLock();
  try {
    final Set<ORecord<?>> values=map.get(iKey);
    if (values == null)     return Collections.EMPTY_SET;
    return values;
  }
  finally {
    releaseSharedLock();
  }
}
