{
  record.reset();
  record.value("id:" + data.getCyclesDone() + ",name:'Gipsy',type:'Cat',race:'European',country:'Italy',price:"+ (data.getCyclesDone() + 300)+ ".00").save("Animal");
  if (data.getCyclesDone() == data.getCycles() - 1)   database.commit();
}
