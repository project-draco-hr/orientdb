{
  if (walRecord instanceof OAddNewPageRecord)   return;
  if (walRecord instanceof OSetPageDataRecord)   restoreFromSetDataRecord((OSetPageDataRecord)walRecord);
 else   throw new IllegalStateException("Invalid WAL record type : " + walRecord);
}
