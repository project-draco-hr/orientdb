{
  final boolean locked=lock.acquireSharedLock();
  try {
    if (networkPool.size() == 0)     throw new ODatabaseException("Connection is closed");
  }
  finally {
    lock.releaseSharedLock(locked);
  }
}
