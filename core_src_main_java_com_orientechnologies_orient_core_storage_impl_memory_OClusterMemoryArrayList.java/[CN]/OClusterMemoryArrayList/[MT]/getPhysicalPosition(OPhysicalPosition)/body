{
  acquireSharedLock();
  try {
    if (iPPosition.clusterPosition.intValue() < 0 || iPPosition.clusterPosition.compareTo(getLastIdentity()) > 0)     return null;
    return entries.get((int)iPPosition.clusterPosition.intValue());
  }
  finally {
    releaseSharedLock();
  }
}
