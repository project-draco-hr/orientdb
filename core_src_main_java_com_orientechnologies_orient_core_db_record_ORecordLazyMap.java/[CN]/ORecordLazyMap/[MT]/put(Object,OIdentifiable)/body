{
  if (status == MULTIVALUE_CONTENT_TYPE.ALL_RIDS && iValue instanceof ORecord<?> && !((ORecord<?>)iValue).getIdentity().isNew())   iValue=((ORecord<?>)iValue).getIdentity();
 else   status=ORecordMultiValueHelper.updateContentType(status,iValue);
  return super.put(iKey,iValue);
}
