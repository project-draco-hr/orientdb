{
  try {
    while (!Thread.interrupted()) {
      try {
        printStats(databaseUrl);
        Thread.sleep(delayReader);
      }
 catch (      Exception e) {
        e.printStackTrace();
        break;
      }
    }
  }
  finally {
    printStats(databaseUrl);
  }
}
