{
  ODocument doc=(ODocument)iRecord;
  if (orderedFields != null) {
    if (tempResult == null)     tempResult=new ArrayList<ODocument>();
    tempResult.add(doc);
  }
 else   sendResultToListener(doc);
}
