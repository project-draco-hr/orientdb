{
  final OReplicationConflictResolver resolver=getDatabaseSynchronizer().getConflictResolver();
  final OPhysicalPosition remote=(OPhysicalPosition)remoteResult;
  resolver.handleCreateConflict(iRemoteNodeId,rid,version.getCounter(),new ORecordId(rid.getClusterId(),remote.clusterPosition),remote.recordVersion.getCounter());
}
