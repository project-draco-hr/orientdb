{
  final boolean txBegun=database.getTransaction().isActive();
  if (!autoStartTx) {
    if (settings.requireTransaction && !txBegun)     throw new OTransactionException("Transaction required to change the Graph");
    return;
  }
  if (!txBegun)   begin();
}
