{
  if (positionsToProcess.length > 0)   do {
    currentEntryPosition++;
  }
 while (currentEntryPosition < positionsToProcess.length && (iterateThroughTombstones || positionsToProcess[currentEntryPosition].recordVersion.isTombstone()));
}
