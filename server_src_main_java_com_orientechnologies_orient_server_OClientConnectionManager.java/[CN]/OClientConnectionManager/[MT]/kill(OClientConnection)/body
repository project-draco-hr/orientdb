{
  if (connection != null) {
    final ONetworkProtocol protocol=connection.protocol;
    try {
      protocol.interrupt();
    }
 catch (    Exception e) {
    }
    disconnect(connection);
    protocol.sendShutdown();
  }
}
