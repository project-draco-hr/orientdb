{
  final OServerHandler manager=OServerMain.server().getHandler(ODistributedServerDiscoveryManager.class);
  if (manager != null)   connection.database.registerHook(new ODistributedServerRecordHook(connection));
}
