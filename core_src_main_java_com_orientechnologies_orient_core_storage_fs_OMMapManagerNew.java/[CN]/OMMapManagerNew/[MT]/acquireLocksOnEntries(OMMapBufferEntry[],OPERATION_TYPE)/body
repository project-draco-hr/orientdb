{
  if (operationType == OPERATION_TYPE.WRITE)   for (  OMMapBufferEntry entry : entries) {
    entry.acquireLock();
    entry.setDirty();
  }
 else   for (  OMMapBufferEntry entry : entries)   entry.acquireLock();
}
