{
  if (iSize != size) {
    size=iSize;
    final ByteBuffer buffer=getWriteBuffer(OBinaryProtocol.SIZE_INT);
    buffer.putInt(size);
    writeBuffer(buffer,SIZE_OFFSET);
    setHeaderDirty();
  }
}
