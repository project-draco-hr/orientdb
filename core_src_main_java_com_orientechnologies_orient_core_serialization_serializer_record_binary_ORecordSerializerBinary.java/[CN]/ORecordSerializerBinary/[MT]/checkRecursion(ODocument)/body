{
  Set<ODocument> iMarshalledRecords=OSerializationSetThreadLocal.INSTANCE.get();
  if (iMarshalledRecords.contains(document)) {
    return false;
  }
 else   iMarshalledRecords.add((ODocument)document);
  return true;
}
