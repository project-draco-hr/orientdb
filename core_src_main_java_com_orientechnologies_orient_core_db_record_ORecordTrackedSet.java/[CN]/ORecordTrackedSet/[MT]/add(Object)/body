{
  final int h=e.hashCode();
  final Object previous=map.get(h);
  if (previous != null && previous != e)   map.put(System.currentTimeMillis(),e);
 else   map.put(h,e);
  setDirty();
  if (e instanceof ODocument)   ((ODocument)e).addOwner(this);
  return true;
}
