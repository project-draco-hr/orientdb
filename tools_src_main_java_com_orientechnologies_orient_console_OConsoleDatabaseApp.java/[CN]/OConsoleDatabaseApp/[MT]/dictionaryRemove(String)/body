{
  checkForDatabase();
  boolean result=currentDatabase.getDictionary().remove(iKey);
  if (!result)   out.println("Entry not found in dictionary.");
 else   out.println("Entry removed from the dictionary.");
}
