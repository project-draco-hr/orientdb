{
  OSBTreeBonsaiLocal<OIdentifiable,Integer> tree=new OSBTreeBonsaiLocal<OIdentifiable,Integer>(DEFAULT_EXTENSION,true,storage);
  tree.load(collectionPointer.getFileId(),collectionPointer.getRootPointer());
  return tree;
}
