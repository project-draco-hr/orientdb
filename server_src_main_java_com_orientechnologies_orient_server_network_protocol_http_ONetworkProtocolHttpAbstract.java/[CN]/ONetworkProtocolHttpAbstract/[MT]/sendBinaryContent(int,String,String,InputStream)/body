{
  sendStatus(iCode,iReason);
  sendResponseHeaders(iContentType);
  writeLine(null);
  while (iContent.available() > 0)   channel.outStream.write((byte)iContent.read());
  channel.outStream.flush();
  channel.flush();
}
