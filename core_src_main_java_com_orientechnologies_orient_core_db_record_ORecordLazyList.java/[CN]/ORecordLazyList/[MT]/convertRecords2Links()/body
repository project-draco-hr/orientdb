{
  lazyLoad(false);
  if (status == MULTIVALUE_STATUS.ALL_RIDS || sourceRecord == null || database == null)   return;
  for (int i=0; i < size(); ++i) {
    try {
      convertRecord2Link(i);
    }
 catch (    ORecordNotFoundException e) {
    }
  }
  status=MULTIVALUE_STATUS.ALL_RIDS;
}
