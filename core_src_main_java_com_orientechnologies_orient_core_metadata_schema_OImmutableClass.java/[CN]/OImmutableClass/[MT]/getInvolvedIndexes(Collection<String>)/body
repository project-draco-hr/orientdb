{
  final Set<OIndex<?>> result=new HashSet<OIndex<?>>(getClassInvolvedIndexes(fields));
  if (superClass != null)   result.addAll(schema.getClass(superClass).getInvolvedIndexes(fields));
  return result;
}
