{
  if (iIndex < start)   throw new IllegalStateException("Can't read backward");
  if (iIndex >= end)   read(iIndex);
  if (iIndex > current)   current=iIndex;
  return buffer[(int)(iIndex - start)];
}
