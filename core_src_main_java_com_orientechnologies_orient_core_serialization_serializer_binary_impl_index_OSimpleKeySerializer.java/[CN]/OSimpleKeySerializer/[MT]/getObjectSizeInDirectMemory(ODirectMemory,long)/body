{
  final byte serializerId=memory.getByte(pointer);
  init(serializerId);
  return OBinarySerializerFactory.TYPE_IDENTIFIER_SIZE + binarySerializer.getObjectSizeInDirectMemory(memory,pointer + OBinarySerializerFactory.TYPE_IDENTIFIER_SIZE);
}
