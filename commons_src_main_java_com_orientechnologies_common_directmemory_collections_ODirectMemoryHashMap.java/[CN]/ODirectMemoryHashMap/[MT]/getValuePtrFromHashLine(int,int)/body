{
  final long memoryOffset=OIntegerSerializer.INT_SIZE + OLongSerializer.LONG_SIZE * bucketSize + 2 * offset * OIntegerSerializer.INT_SIZE + OIntegerSerializer.INT_SIZE;
  return memory.getInt(ptr,(int)memoryOffset);
}
