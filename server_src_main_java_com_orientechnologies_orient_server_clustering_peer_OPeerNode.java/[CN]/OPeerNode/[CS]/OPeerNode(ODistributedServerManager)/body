{
  manager=iManager;
  OLogManager.instance().info(this,"Cluster <%s> joined",iManager.getConfig().name);
  leaderCheckerTask=new OLeaderCheckerTask(this);
  Orient.getTimer().schedule(leaderCheckerTask,manager.getConfig().networkHeartbeatDelay,manager.getConfig().networkHeartbeatDelay / 2);
  updateHeartBeatTime();
}
