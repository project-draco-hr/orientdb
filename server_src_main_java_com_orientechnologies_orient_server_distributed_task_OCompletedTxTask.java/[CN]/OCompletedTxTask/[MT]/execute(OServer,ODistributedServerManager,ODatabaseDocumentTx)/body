{
  ODistributedServerLog.debug(this,iManager.getLocalNodeName(),getNodeSource(),DIRECTION.IN,"completing transaction against db=%s locks=%s...",database.getName(),locks);
  ODatabaseRecordThreadLocal.INSTANCE.set(database);
  final ODistributedDatabase ddb=iManager.getMessageService().getDatabase(database.getName());
  for (  ORID r : locks)   ddb.unlockRecord(r);
  return Boolean.TRUE;
}
