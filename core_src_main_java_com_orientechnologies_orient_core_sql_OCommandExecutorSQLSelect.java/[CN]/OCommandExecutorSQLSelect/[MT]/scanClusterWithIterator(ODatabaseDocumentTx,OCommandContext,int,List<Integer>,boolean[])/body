{
  final ORecordIteratorCluster it=new ORecordIteratorCluster(localDatabase,localDatabase,clusterList.get(current),true,false);
  while (it.hasNext()) {
    final ORecord next=it.next();
    if (!executeSearchRecord(next,iContext)) {
      results[current]=false;
    }
  }
}
