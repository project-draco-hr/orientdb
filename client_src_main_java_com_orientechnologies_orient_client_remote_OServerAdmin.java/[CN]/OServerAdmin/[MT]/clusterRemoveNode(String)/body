{
  final ODocument response=sendRequest(OChannelBinaryProtocol.REQUEST_CLUSTER,new ODocument().field("operation","removeNode").field("node",iRemoteNode),"Cluster remove node");
  OLogManager.instance().debug(this,"Removed node '%s' from the cluster",iRemoteNode);
  return response;
}
