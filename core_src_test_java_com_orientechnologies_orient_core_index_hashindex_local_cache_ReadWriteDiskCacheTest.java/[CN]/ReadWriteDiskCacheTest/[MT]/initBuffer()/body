{
  writeBuffer=new OWOWCache(false,8 + systemOffset,-1,writeAheadLog,-1,15000 * (8 + systemOffset + 2 * OWOWCache.PAGE_PADDING),storageLocal,true,1);
  readBuffer=new O2QCache(4 * (8 + systemOffset + 2 * OWOWCache.PAGE_PADDING),8 + systemOffset,true);
}
