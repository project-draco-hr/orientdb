{
  try {
    acquireExclusiveLock();
    OStorage storage=storages.get(iDbName);
    if (storage == null)     storage=new OStorageLocal(iDbName,iDbName,iMode);
    return storage;
  }
  finally {
    releaseExclusiveLock();
  }
}
