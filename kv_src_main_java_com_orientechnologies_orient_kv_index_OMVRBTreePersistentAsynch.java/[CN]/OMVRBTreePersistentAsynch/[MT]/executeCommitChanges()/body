{
  ODatabaseBinary db=null;
  try {
    db=OSharedBinaryDatabase.acquire(database.getName());
    super.commitChanges(db);
  }
 catch (  InterruptedException e) {
    e.printStackTrace();
  }
 finally {
    if (db != null)     OSharedBinaryDatabase.release(db);
  }
}
