{
  final ORecordAbstract<?> record=(ORecordAbstract<?>)iRecord;
  if (record.getIdentity().isValid()) {
    record.setVersion(-1);
    record.delete();
    recordCount++;
    return true;
  }
  return false;
}
