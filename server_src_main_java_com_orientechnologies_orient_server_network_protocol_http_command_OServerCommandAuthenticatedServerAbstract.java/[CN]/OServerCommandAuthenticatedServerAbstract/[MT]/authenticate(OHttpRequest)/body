{
  if (iRequest.authorization != null) {
    String[] authParts=iRequest.authorization.split(":");
    if (OServerMain.server().authenticate(authParts[0],authParts[1],resource))     return true;
  }
  sendAuthorizationRequest(iRequest);
  return false;
}
