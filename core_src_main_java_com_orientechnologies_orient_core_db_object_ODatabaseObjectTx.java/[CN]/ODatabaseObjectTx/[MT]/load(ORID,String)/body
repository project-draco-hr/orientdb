{
  checkOpeness();
  if (iRecordId == null)   return this;
  final ODocument record=underlying.load(iRecordId);
  return record == null ? null : OObjectSerializerHelper.fromStream(record,newInstance(record.getClassName()),getEntityManager(),this,iFetchPlan);
}
