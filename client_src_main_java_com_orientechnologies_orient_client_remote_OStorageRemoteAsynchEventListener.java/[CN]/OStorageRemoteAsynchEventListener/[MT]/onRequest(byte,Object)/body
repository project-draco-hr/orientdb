{
  if (iRequestCode == OChannelBinaryProtocol.REQUEST_PUSH_RECORD)   storage.getLevel2Cache().updateRecord((ORecordInternal<?>)obj);
 else   if (iRequestCode == OChannelDistributedProtocol.PUSH_DISTRIBUTED_CONFIG)   storage.updateClusterConfiguration((ODocument)obj);
}
