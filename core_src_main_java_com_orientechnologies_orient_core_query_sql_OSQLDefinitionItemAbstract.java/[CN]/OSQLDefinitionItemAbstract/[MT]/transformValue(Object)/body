{
  if (iResult != null && operationsChain != null) {
    int operator;
    for (    OPair<Integer,String[]> op : operationsChain) {
      operator=op.key.intValue();
      if (operator == OSQLDefinitionFieldOperator.SIZE.id)       iResult=iResult != null ? ((Collection<?>)iResult).size() : 0;
 else       if (operator == OSQLDefinitionFieldOperator.LENGTH.id)       iResult=iResult != null ? iResult.toString().length() : 0;
 else       if (operator == OSQLDefinitionFieldOperator.TOUPPERCASE.id)       iResult=iResult != null ? iResult.toString().toUpperCase() : 0;
 else       if (operator == OSQLDefinitionFieldOperator.TOLOWERCASE.id)       iResult=iResult != null ? iResult.toString().toLowerCase() : 0;
 else       if (operator == OSQLDefinitionFieldOperator.TRIM.id)       iResult=iResult != null ? iResult.toString().trim() : null;
 else       if (operator == OSQLDefinitionFieldOperator.CHARAT.id) {
        int index=Integer.parseInt(op.value[0]);
        iResult=iResult != null ? iResult.toString().substring(index,index + 1) : null;
      }
 else       if (operator == OSQLDefinitionFieldOperator.INDEXOF.id && op.value[0].length() > 2) {
        String toFind=op.value[0].substring(1,op.value[0].length() - 1);
        int startIndex=op.value.length > 1 ? Integer.parseInt(op.value[1]) : 0;
        iResult=iResult != null ? iResult.toString().indexOf(toFind,startIndex) : null;
      }
 else       if (operator == OSQLDefinitionFieldOperator.SUBSTRING.id) {
        int endIndex=op.value.length > 1 ? Integer.parseInt(op.value[1]) : op.value[0].length();
        iResult=iResult != null ? iResult.toString().substring(Integer.parseInt(op.value[0]),endIndex) : null;
      }
 else       if (operator == OSQLDefinitionFieldOperator.FORMAT.id)       iResult=iResult != null ? String.format(op.value[0],iResult) : null;
 else       if (operator == OSQLDefinitionFieldOperator.LEFT.id)       iResult=iResult != null ? iResult.toString().substring(0,Integer.parseInt(op.value[0])) : null;
 else       if (operator == OSQLDefinitionFieldOperator.RIGHT.id)       iResult=iResult != null ? iResult.toString().substring(Integer.parseInt(op.value[0])) : null;
    }
  }
  return iResult;
}
