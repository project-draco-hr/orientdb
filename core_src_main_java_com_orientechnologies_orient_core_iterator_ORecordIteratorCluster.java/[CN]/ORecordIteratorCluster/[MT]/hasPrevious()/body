{
  checkDirection(false);
  updateRangesOnLiveUpdate();
  if (limit > -1 && browsedRecords >= limit)   return false;
  if (currentEntry > firstClusterEntry)   return true;
  if (currentPositionIndex > 0)   return true;
  return false;
}
