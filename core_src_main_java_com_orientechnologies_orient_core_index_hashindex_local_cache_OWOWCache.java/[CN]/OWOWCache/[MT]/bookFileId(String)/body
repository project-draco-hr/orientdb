{
  filesLock.acquireWriteLock();
  try {
    initNameIdMapping();
    Long fileId=nameIdMap.get(fileName);
    if (fileId != null && fileId < 0)     return -fileId;
    ++fileCounter;
    return fileCounter;
  }
  finally {
    filesLock.releaseWriteLock();
  }
}
