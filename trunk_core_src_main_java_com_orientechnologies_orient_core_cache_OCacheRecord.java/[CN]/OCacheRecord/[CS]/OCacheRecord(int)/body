{
  maxSize=iMaxSize;
  cache=new LinkedHashMap<String,Object[]>(iMaxSize,0.75f,true){
    @Override protected boolean removeEldestEntry(    java.util.Map.Entry<String,Object[]> iEldest){
      return size() > maxSize;
    }
  }
;
}
