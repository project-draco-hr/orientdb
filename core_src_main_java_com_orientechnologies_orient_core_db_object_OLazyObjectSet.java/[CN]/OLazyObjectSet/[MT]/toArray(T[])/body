{
  underlying.toArray(a);
  final ODatabasePojoAbstract<TYPE> database=getDatabase();
  for (int i=0; i < a.length; ++i)   a[i]=(T)database.getUserObjectByRecord((ORecordInternal<?>)a[i],fetchPlan);
  return a;
}
