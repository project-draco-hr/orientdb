{
  if (TRACK) {
synchronized (this) {
      for (      TrackedBufferReference reference : trackedReferences)       OLogManager.instance().error(this,"DIRECT-TRACK: unreleased direct memory buffer `%X` detected on shutdown.",reference.stackTrace,reference.id);
      checkTrackedBuffersLeaks();
    }
  }
}
