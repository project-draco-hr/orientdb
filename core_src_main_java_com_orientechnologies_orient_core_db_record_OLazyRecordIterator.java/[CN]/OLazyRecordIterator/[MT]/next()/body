{
  final Object value=underlying.next();
  if (value == null)   return null;
  if (sourceDatabase != null)   if (value instanceof ORecordId && convertToRecord)   return sourceDatabase.load((ORecordId)value);
  return value;
}
