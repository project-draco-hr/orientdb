{
  checkTransaction();
  final ORecordInternal<?> txRecord=getRecord(iRid);
  if (txRecord != null)   return txRecord;
  return database.executeReadRecord((ORecordId)iRid,iRecord,iFetchPlan,false);
}
