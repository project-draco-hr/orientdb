{
  if (beginRange != null && currentPosition < beginRange.getClusterPosition())   return true;
  if (endRange != null && currentPosition > endRange.getClusterPosition())   return true;
  return false;
}
