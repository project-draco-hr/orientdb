{
  getDatabase().getStorage().removeResource(OGlobalRecordCache.class.getSimpleName());
  globalCache=getDatabase().getStorage().getResource(OGlobalRecordCache.class.getSimpleName(),new Callable<OGlobalRecordCache>(){
    public OGlobalRecordCache call(){
      return iGlobalCache;
    }
  }
);
}
