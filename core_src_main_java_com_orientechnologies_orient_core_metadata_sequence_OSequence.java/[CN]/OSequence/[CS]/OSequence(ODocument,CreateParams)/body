{
  super(iDocument != null ? iDocument : new ODocument(CLASS_NAME));
  if (iDocument == null) {
    if (params == null) {
      params=new CreateParams().setDefaults();
    }
    initSequence(params);
  }
}
