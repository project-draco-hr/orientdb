{
  if (limit > 0 && returned >= limit)   return false;
  if (lastTraversed == null)   lastTraversed=next();
  if (lastTraversed == null && context.peek() != null)   throw new IllegalStateException("Traverse ended abnormally");
  return lastTraversed != null;
}
