{
  for (int i=0; i < CHUNKS_SIZE; i++) {
    final long chunk=chunksOne[i] & LONG_INT_MASK;
    final long otherChunk=chunksTwo[i] & LONG_INT_MASK;
    if (chunk == otherChunk)     continue;
    if (chunk > otherChunk)     return 1;
    return -1;
  }
  return 0;
}
