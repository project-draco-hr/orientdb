{
  if (id.equals("192.168.1.100:2425"))   return;
synchronized (this) {
    if (peer != null) {
      peer.shutdown();
      peer=null;
    }
    if (leader == null) {
      leader=new OLeaderNode(this);
      sendPresence();
    }
  }
}
