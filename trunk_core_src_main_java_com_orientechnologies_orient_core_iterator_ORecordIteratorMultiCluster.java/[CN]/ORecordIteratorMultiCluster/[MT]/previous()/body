{
  final REC record=getRecord();
  while (currentClusterIdx > -1) {
    while (hasPrevious()) {
      if (readCurrentRecord(record,-1) != null)       return record;
    }
    currentClusterIdx--;
  }
  return null;
}
