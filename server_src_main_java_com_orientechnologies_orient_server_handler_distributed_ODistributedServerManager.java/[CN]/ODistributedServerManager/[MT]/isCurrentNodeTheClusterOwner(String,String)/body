{
  final ODocument servers=getServersForCluster(iDatabaseName,iClusterName);
  if (servers == null)   return true;
  if (ODistributedRequesterThreadLocal.INSTANCE.get())   return true;
  return servers.field("owner").equals(getId());
}
