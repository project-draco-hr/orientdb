{
  Map.Entry<OIdentifiable,Integer> entry;
  while (iterator.hasNext()) {
    entry=iterator.next();
    OModifiableInteger changedCounter=changedValues.get(entry.getKey());
    if (changedCounter == null || changedCounter.intValue() > 0)     return entry;
  }
  return null;
}
