{
  dataProvider.load();
  setLastSearchNode(null,null);
  keySize=dataProvider.getKeySize();
  final ORID rootRid=dataProvider.getRoot();
  if (rootRid != null && rootRid.isValid())   root=loadEntry(null,rootRid);
  return this;
}
