{
  System.out.println("Graph " + (getGraph() instanceof OrientGraph ? "TX" : "NOTX") + " stats threads: "+ THREADS+ " create duplication exceptions: "+ indexCollisionsCreate.get()+ " create version exceptions: "+ versionCollisionsCreate.get()+ " delete version exceptions: "+ versionCollisionsDelete.get()+ " vertexes: "+ g.countVertices()+ " edges: "+ g.countEdges()+ " TIME: "+ (beginTime > 0 ? System.currentTimeMillis() - beginTime : "?"));
}
