{
  checkCurrentDatabase();
  if (iLocation != null)   out.println("Creating data-segment [" + iName + "] in database "+ currentDatabaseName+ " in path: "+ iLocation+ "...");
 else   out.println("Creating data-segment [" + iName + "] in database directory...");
  currentDatabase.addDataSegment(iName,iLocation);
  updateDatabaseInfo();
}
