{
  String[] parts=string.split("\\.");
  if (parts.length != 3)   throw new IllegalArgumentException("Not correct format of distributed version. Expected <recordVersion>.<timestamp>.<macAddress>");
  counter=Integer.valueOf(parts[0]);
  timestamp=Long.valueOf(parts[1]);
  macAddress=Long.valueOf(parts[2]);
}
