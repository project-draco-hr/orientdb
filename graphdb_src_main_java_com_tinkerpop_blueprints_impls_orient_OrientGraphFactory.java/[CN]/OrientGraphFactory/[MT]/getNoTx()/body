{
  final OrientGraphNoTx g;
  if (pool == null) {
    g=(OrientGraphNoTx)new OrientGraphNoTx(getDatabase(),user,password).configure(settings);
    initGraph(g);
  }
 else   return (OrientGraphNoTx)pool.getResource(url,0,user,password);
  initGraph(g);
  return g;
}
