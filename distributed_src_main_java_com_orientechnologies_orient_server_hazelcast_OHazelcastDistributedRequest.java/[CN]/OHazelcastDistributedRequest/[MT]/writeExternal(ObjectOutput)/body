{
  out.writeLong(id);
  out.writeUTF(senderNodeName);
  out.writeLong(senderThreadId);
  out.writeUTF(databaseName);
  if (requestLoginUserName != null && requestLoginUserName.length() > 0)   out.writeUTF(requestLoginUserName);
  out.writeObject(task);
}
