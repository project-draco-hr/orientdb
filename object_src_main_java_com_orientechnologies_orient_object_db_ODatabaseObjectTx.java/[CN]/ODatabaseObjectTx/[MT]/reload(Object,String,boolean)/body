{
  checkOpeness();
  if (iPojo == null)   return iPojo;
  final ODocument record=getRecordByUserObject(iPojo,true);
  underlying.reload(record,iFetchPlan,iIgnoreCache);
  stream2pojo(record,iPojo,iFetchPlan);
  return iPojo;
}
