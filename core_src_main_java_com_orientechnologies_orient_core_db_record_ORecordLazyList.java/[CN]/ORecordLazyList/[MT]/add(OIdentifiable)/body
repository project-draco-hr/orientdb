{
  if (status == MULTIVALUE_STATUS.ALL_RIDS && e instanceof ORecord<?> && !((ORecord<?>)e).getIdentity().isNew())   e=((ORecord<?>)e).getIdentity();
 else   status=ORecordMultiValueHelper.getStatus(status,e);
  return super.add(e);
}
