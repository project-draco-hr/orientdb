{
  readWriteLock.writeLock().lock();
  try {
    if (dirtyFile == null)     return;
    if (dirtyFile.exists())     dirtyFileData.close();
  }
  finally {
    readWriteLock.writeLock().unlock();
  }
}
