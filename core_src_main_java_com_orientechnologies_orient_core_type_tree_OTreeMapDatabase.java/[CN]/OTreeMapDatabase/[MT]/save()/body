{
  lock.acquireExclusiveLock();
  try {
    if (record.isDirty()) {
      record.fromStream(toStream());
      record.save(clusterName);
    }
    return this;
  }
  finally {
    lock.releaseExclusiveLock();
  }
}
