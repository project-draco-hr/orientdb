{
  if (dirtyFlag)   return;
  lock.lock();
  try {
    if (dirtyFlag)     return;
    dirtyFileData.seek(0);
    dirtyFileData.writeBoolean(true);
    dirtyFlag=true;
  }
  finally {
    lock.unlock();
  }
}
