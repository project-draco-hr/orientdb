{
  acquireExclusiveLock();
  try {
    if (ascSortOrder)     getValuesMinorAscOrder(toKey,isInclusive,transformer,valuesResultListener);
 else     getValuesMinorDescOrder(toKey,isInclusive,transformer,valuesResultListener);
  }
  finally {
    releaseExclusiveLock();
  }
}
