{
  final int[] clusters=iClass.getClusterIds();
  if (clusters.length == 1)   return clusters[0];
synchronized (this) {
    if (pointer >= clusters.length)     pointer=0;
    return clusters[pointer++];
  }
}
