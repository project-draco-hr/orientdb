{
  boolean modified=false;
  setDirty();
  final ODatabasePojoAbstract<TYPE> database=getDatabase();
  for (  Object o : c)   if (!underlying.add(database.getRecordByUserObject(o,false)))   modified=true;
  return modified;
}
