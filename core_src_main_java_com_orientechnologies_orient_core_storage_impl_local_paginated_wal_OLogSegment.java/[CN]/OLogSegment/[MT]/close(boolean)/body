{
  if (!closed) {
    lastReadRecord.clear();
    stopFlush(flush);
    rndFile.close();
    closed=true;
    if (!pagesCache.isEmpty()) {
      for (      OWALPage page : pagesCache)       byteBufferPool.release(page.getByteBuffer());
    }
    currentPage=null;
  }
}
