{
  int startIndex=-1;
  for (int i=0; i < parentNode.length; i++)   if (parentNode[i] < 0 && (parentNode[i] & Long.MAX_VALUE) >>> 8 == nodePath.nodeIndex) {
    startIndex=i;
    break;
  }
  return startIndex == calculatedIndex;
}
