{
  if (!firstAttribute)   out.append(", ");
  if (iNewLine)   out.append("\n");
  if (prettyPrint)   for (int i=0; i < iIdentLevel; ++i)   out.append("  ");
  out.append(iName != null ? iName.toString() : "null");
  out.append("{");
  firstAttribute=true;
}
