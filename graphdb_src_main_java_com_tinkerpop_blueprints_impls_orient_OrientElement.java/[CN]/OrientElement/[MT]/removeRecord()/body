{
  checkIfAttached();
  final OrientBaseGraph graph=getGraph();
  graph.setCurrentGraphInThreadLocal();
  graph.autoStartTransaction();
  if (checkDeletedInTx())   throw new ORecordNotFoundException("The graph element with id '" + getIdentity() + "' not found");
  getRecord().load();
  getRecord().delete();
}
