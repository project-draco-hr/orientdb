{
  out.writeInt(tasks.size());
  for (  OAbstractRecordReplicatedTask task : tasks)   out.writeObject(task);
  if (lastLSN != null) {
    out.writeBoolean(true);
    lastLSN.writeExternal(out);
  }
 else   out.writeBoolean(false);
}
