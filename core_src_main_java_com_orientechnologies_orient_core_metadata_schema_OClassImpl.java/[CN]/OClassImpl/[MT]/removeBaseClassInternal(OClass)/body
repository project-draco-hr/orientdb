{
  acquireSchemaWriteLock();
  try {
    checkEmbedded();
    if (baseClasses == null)     return this;
    if (baseClasses.remove(baseClass))     removePolymorphicClusterIds((OClassImpl)baseClass);
    return this;
  }
  finally {
    releaseSchemaWriteLock();
  }
}
