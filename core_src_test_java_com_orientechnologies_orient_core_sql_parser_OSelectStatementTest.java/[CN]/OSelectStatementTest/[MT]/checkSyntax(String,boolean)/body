{
  OrientSql osql=getParserFor(query);
  try {
    osql.OrientGrammar();
    if (!isCorrect) {
      fail();
    }
  }
 catch (  Exception e) {
    if (isCorrect) {
      e.printStackTrace();
      fail();
    }
  }
}
