{
  lazyLoad(true);
  final int pos=Collections.binarySearch(delegate,(OIdentifiable)o);
  if (pos > -1) {
    delegate.remove(pos);
    return true;
  }
  if (newItems != null)   return newItems.remove(o) != null;
  return false;
}
