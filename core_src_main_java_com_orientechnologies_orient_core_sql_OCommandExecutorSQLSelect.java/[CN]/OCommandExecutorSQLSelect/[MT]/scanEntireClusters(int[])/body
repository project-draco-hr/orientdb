{
  final ODatabaseRecord database=getDatabase();
  final ORID[] range=getRange();
  ((OStorageEmbedded)database.getStorage()).browse(clusterIds,range[0],range[1],this,(ORecordInternal<?>)database.newInstance(),false);
}
