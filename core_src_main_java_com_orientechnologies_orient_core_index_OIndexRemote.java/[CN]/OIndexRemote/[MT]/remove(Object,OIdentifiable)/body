{
  if (iKey instanceof String)   iKey="'" + iKey + "'";
  final OCommandRequest cmd=formatCommand(QUERY_REMOVE2,name,iKey,iRID.getIdentity());
  final int deleted=(Integer)getDatabase().command(cmd).execute(iKey,iRID);
  return deleted > 0;
}
