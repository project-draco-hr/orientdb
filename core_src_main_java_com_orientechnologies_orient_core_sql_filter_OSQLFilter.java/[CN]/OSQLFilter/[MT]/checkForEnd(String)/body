{
  if (iWord != null && (iWord.equals(OCommandExecutorSQLSelect.KEYWORD_ORDER) || iWord.equals(OCommandExecutorSQLSelect.KEYWORD_LIMIT))) {
    currentPos-=iWord.length();
    return true;
  }
  return false;
}
