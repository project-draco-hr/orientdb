{
  if (status == MULTIVALUE_STATUS.ALL_RIDS && iValue instanceof ORecord<?>)   iValue=((ORecord<?>)iValue).getIdentity();
 else   status=ORecordMultiValueHelper.getStatus(status,iValue);
  return super.put(iKey,iValue);
}
