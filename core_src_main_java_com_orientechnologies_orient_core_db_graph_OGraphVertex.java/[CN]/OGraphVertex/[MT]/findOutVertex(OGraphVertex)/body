{
  final List<ODocument> docs=document.field(FIELD_OUT_EDGES);
  if (docs == null || docs.size() == 0)   return -1;
  for (int i=0; i < docs.size(); ++i) {
    if (docs.get(i).field(OGraphEdge.OUT).equals(iVertexDocument.getDocument()))     return i;
  }
  return -1;
}
