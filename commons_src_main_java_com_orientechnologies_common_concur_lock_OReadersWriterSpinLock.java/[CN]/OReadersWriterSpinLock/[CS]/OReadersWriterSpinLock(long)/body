{
  this.writeDelay=writeDelay;
  final PaddedCounter[] counters=new PaddedCounter[COUNTERS_SIZE];
  for (int i=0; i < counters.length; i++)   counters[i]=new PaddedCounter();
  readCounters=counters;
}
