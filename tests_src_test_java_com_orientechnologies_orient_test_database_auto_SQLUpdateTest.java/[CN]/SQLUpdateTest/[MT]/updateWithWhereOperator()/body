{
  database.open("admin","admin");
  Integer records=(Integer)database.command(new OCommandSQL("update Profile set salary = 120.30, location = -3:2, salary_cloned = salary where surname = 'Smith'")).execute();
  Assert.assertTrue(records == 1);
  database.close();
}
