{
  final OClass cls=compiledFilter.getTargetClasses().keySet().iterator().next();
  if (searchForIndexes(cls))   Orient.instance().getProfiler().updateCounter("Query.indexUsage",1);
 else   super.searchInClasses();
}
