{
  final IQueue<ODistributedRequest>[] queues=new IQueue[iNodes.size()];
  for (int i=0; i < iNodes.size(); ++i)   queues[i]=getNodeQueue(getRequestQueueName(iNodes.get(i),iDatabaseName));
  return queues;
}
