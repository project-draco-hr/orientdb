{
  ODistributedServerLog.debug(this,requestChannel.getManager().getLocalNodeName(),requestChannel.getServer(),ODistributedServerLog.DIRECTION.OUT,"Closing remote channel to distributed server...");
  requestChannel.close();
  responseChannel.close();
}
