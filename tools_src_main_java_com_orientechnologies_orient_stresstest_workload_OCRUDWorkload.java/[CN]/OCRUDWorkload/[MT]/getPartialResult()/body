{
  final long current=createsResult.current.get() + readsResult.current.get() + updatesResult.current.get()+ deletesResult.current.get();
  return String.format("%d%% [Creates: %d%% - Reads: %d%% - Updates: %d%% - Deletes: %d%%]",((int)(100 * current / total)),100 * createsResult.current.get() / createsResult.total,100 * readsResult.current.get() / readsResult.total,100 * updatesResult.current.get() / updatesResult.total,100 * deletesResult.current.get() / deletesResult.total);
}
