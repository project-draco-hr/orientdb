{
  lock.acquireSharedLock();
  try {
    final OCluster cluster=clusterMap.get(iClusterName.toLowerCase());
    if (cluster == null)     throw new IllegalArgumentException("Cluster " + iClusterName + " not exists in storage '"+ name+ "'");
    return cluster;
  }
  finally {
    lock.releaseSharedLock();
  }
}
