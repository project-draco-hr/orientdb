{
  ORecordDocument cloned=new ORecordDocument();
  cloned.source=source;
  cloned.database=database;
  cloned.recordId=recordId.copy();
  cloned.version=version;
  cloned.dirty=dirty;
  cloned.pinned=pinned;
  cloned.clazz=clazz;
  cloned.status=status;
  cloned.recordFormat=recordFormat;
  if (fields != null)   cloned.fields=new LinkedHashMap<String,Object>(fields);
  return cloned;
}
