{
  if (result != null)   return false;
  if (currentValue == null)   throw new OCommandExecutionException("Can't execute operators if no value was selected. Use column() to obtain a value");
  return true;
}
