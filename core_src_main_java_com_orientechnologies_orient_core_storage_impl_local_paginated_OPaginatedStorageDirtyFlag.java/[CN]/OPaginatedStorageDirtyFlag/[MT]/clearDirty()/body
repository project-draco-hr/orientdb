{
  if (!dirtyFlag)   return;
  lock.lock();
  try {
    if (!dirtyFlag)     return;
    dirtyFileData.seek(0);
    dirtyFileData.writeBoolean(false);
    dirtyFlag=false;
  }
  finally {
    lock.unlock();
  }
}
