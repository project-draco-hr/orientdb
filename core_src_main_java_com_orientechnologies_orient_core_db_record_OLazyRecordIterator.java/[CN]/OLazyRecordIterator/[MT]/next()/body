{
  final OIdentifiable value=underlying.next();
  if (value == null)   return null;
  if (sourceDatabase != null)   if (value instanceof ORecordId && autoConvert2Record)   return (OIdentifiable)sourceDatabase.load((ORecordId)value);
  return value;
}
