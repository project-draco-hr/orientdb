{
  if (unaligned)   unsafe.putChar(pointer,value);
 else {
    if (alignedOrder.equals(ByteOrder.BIG_ENDIAN)) {
      unsafe.putByte(pointer++,(byte)(value >>> 8));
      unsafe.putByte(pointer,(byte)(value));
    }
 else {
      unsafe.putByte(pointer++,(byte)(value));
      unsafe.putByte(pointer,(byte)(value >>> 8));
    }
  }
}
