{
  OLogManager.instance().info(this,"Uninstalling dynamic plugin '%s'...",iName);
  final OServerPluginInfo removePlugin=activePlugins.remove(iName);
  if (removePlugin != null)   removePlugin.getInstance().shutdown();
}
