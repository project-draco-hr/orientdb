{
  final long mostSignificantBits=OLongSerializer.INSTANCE.deserializeFromDirectMemory(pointer,offset);
  final long leastSignificantBits=OLongSerializer.INSTANCE.deserializeFromDirectMemory(pointer,offset + OLongSerializer.LONG_SIZE);
  return new UUID(mostSignificantBits,leastSignificantBits);
}
