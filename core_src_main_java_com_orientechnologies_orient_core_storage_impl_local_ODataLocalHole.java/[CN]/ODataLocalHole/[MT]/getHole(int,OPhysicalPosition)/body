{
  final ODataHoleInfo hole=availableHolesList.get(iPosition);
  if (hole.dataOffset == -1)   return false;
  iPPosition.dataPosition=hole.dataOffset;
  iPPosition.recordSize=hole.size;
  return true;
}
