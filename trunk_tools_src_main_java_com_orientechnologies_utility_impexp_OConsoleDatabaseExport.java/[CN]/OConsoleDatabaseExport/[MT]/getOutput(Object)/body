{
  if (iValue instanceof ORecord<?>) {
    ORecord<?> linked=(ORecord<?>)iValue;
    if (linked.getIdentity().isValid())     iValue=linked.getIdentity().toString();
 else     iValue=linked.toString();
  }
  return iValue;
}
