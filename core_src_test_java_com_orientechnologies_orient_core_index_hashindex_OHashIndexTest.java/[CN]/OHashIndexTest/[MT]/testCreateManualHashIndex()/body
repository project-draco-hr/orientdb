{
  final OIndex<?> index=db.getMetadata().getIndexManager().createIndex("manualHashIndex",OClass.INDEX_TYPE.UNIQUE_HASH.toString(),new OSimpleKeyIndexDefinition(OType.STRING),null,null);
  Assert.assertNotNull(index);
}
