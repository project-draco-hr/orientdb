{
  acquireExclusiveLock();
  try {
    storeMetadata();
    storeHashTree();
    for (    OEHFileMetadata metadata : filesMetadata)     if (metadata != null)     metadata.getFile().synch();
    metadataStore.synch();
    treeStateStore.synch();
  }
  finally {
    releaseExclusiveLock();
  }
}
