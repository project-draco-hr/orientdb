{
  database.open("admin","admin");
  int i=0;
  for (  ORecordFlat rec : database.browseCluster(CLUSTER_NAME)) {
    if (i % 2 == 0) {
      rec.value(rec.value() + "+");
      rec.save();
    }
    i++;
  }
  database.close();
}
