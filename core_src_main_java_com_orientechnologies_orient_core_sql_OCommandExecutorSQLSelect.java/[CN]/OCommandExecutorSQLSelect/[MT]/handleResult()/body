{
  if (tempResult != null) {
    for (    ODocument doc : tempResult)     sendResultToListener(doc);
    tempResult.clear();
    tempResult=null;
    return null;
  }
  if (request instanceof OSQLSynchQuery)   return ((OSQLSynchQuery<ORecordSchemaAware<?>>)request).getResult();
  return null;
}
