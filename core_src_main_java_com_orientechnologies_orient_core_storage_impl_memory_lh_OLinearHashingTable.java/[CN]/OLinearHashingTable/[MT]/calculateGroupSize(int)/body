{
  int divisor=0;
  byte no=-1;
  double nogps;
  do {
    if (divisor < 128) {
      no++;
    }
    divisor=1 << (2 + no);
    nogps=(1 << currentLevel) / divisor;
  }
 while (!((nogps <= 31) || (divisor == 128)));
  return divisor;
}
