{
  if (typeId == 1)   return new OBinaryDiff();
  if (typeId == 2)   return new OIntDiff();
  if (typeId == 3)   return new OLongDiff();
  throw new IllegalArgumentException("Unknown Diff id " + typeId);
}
