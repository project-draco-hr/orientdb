{
  if (iStorageMode.equals(OEngineLocalPaginated.NAME))   return iStorageMode + ":" + server.getDatabaseDirectory()+ databaseName;
 else   if (iStorageMode.equals(OEngineMemory.NAME))   return iStorageMode + ":" + databaseName;
  return null;
}
