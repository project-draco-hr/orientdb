{
  final boolean locked=acquireSharedLock();
  try {
    for (int i=0; i < clusters.size(); ++i)     if (getClusterById(i).getName().equals(iClusterName))     return getClusterById(i);
    return null;
  }
  finally {
    releaseSharedLock(locked);
  }
}
