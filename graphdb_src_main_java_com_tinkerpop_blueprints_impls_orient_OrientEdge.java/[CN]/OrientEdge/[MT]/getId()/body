{
  final OrientBaseGraph graph=getGraph();
  if (rawElement == null)   return vOut.getIdentity() + "->" + vIn.getIdentity();
  if (graph != null)   graph.setCurrentGraphInThreadLocal();
  return super.getId();
}
