{
  if (this == o)   return true;
  if (o == null || getClass() != o.getClass())   return false;
  ONodeId oNodeId=(ONodeId)o;
  if (signum != oNodeId.signum)   return false;
  return Arrays.equals(chunks,oNodeId.chunks);
}
