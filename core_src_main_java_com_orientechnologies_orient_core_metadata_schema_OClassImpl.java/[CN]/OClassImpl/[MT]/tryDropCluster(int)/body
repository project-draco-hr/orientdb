{
  if (name.toLowerCase().equals(getDatabase().getClusterNameById(defaultClusterId))) {
    if (getDatabase().getClusterRecordSizeById(defaultClusterId) == 0)     getDatabase().dropCluster(defaultClusterId,true);
  }
}
