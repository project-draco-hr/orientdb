{
  acquireExclusiveLock();
  try {
    if (!isOpen)     return;
    serializeState();
    fileSegment.close();
    overflowSegment.close();
    overflowStatistic.close();
    isOpen=false;
  }
  finally {
    releaseExclusiveLock();
  }
}
