{
  if (!isRecording())   return null;
  final Map<String,Object> result=new HashMap<String,Object>();
  for (  Map.Entry<OProfilerHookValue,String> v : hooks.entrySet())   result.put(v.getValue(),v.getKey().getValue());
  return result;
}
