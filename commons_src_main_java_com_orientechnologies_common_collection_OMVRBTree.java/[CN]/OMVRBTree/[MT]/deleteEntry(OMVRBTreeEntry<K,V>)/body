{
  setSize(size() - 1);
  modCount++;
  if (pageIndex > -1) {
    p.remove();
    if (p.getSize() > 0)     return p;
  }
  final OMVRBTreeEntry<K,V> next=successor(p);
  removeNode(p);
  return next;
}
