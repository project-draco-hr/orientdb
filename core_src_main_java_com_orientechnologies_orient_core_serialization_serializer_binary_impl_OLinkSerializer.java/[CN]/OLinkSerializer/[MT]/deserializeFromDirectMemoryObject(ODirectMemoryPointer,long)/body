{
  final int clusterId=OShortSerializer.INSTANCE.deserializeFromDirectMemory(pointer,offset);
  final long clusterPosition=OLongSerializer.INSTANCE.deserialize(pointer.get(offset + OShortSerializer.SHORT_SIZE,OLongSerializer.LONG_SIZE),0);
  return new ORecordId(clusterId,clusterPosition);
}
