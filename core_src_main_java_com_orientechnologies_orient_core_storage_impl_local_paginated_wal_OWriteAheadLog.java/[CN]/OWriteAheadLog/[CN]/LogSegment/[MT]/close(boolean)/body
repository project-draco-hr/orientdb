{
  if (!closed) {
    stopFlush(flush);
    rndFile.close();
    closed=true;
    if (!pages.isEmpty()) {
      for (      OWALPage page : pages)       directMemory.free(page.getPagePointer());
    }
    currentPage=null;
  }
}
