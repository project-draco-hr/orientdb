{
  if (url.startsWith(OEngineRemote.NAME))   new OServerAdmin(url).connect("root","root").createDatabase("local").close();
 else {
    database=new ODatabaseObjectTx(url);
    database.create();
    database.close();
  }
}
