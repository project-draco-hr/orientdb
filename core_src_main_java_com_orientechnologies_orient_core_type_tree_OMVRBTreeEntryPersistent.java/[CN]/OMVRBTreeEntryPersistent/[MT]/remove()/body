{
  final int index=tree.getPageIndex();
  final K oldKey=index == 0 ? getKeyAt(0) : null;
  if (dataEntry.removeAt(index))   markDirty();
  tree.setPageIndex(0);
  if (index == 0)   pTree.updateEntryPoint(oldKey,this);
}
