{
  final ORID[] ridToRemove=new ORID[size() - iThreshold];
  int entryNum=0;
  int i=0;
  for (  ORID rid : keySet()) {
    if (entryNum++ >= iThreshold)     ridToRemove[i++]=rid;
    if (i >= ridToRemove.length)     break;
  }
  for (  ORID rid : ridToRemove)   remove(rid);
}
