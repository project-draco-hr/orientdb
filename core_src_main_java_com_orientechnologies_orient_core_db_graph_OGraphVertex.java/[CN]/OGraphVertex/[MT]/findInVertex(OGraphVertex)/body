{
  final Set<ODocument> docs=document.field(OGraphDatabase.VERTEX_FIELD_IN);
  if (docs == null || docs.size() == 0)   return null;
  for (  ODocument d : docs) {
    if (d.<ODocument>field(OGraphDatabase.EDGE_FIELD_IN).equals(iVertexDocument.getDocument()))     return d;
  }
  return null;
}
