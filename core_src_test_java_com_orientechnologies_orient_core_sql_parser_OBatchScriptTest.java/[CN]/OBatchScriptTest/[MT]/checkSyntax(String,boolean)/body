{
  OrientSql osql=getParserFor(query);
  try {
    List<OStatement> result=osql.parseScript();
    for (    OStatement stm : result) {
    }
    if (!isCorrect) {
      fail();
    }
    return result;
  }
 catch (  Exception e) {
    if (isCorrect) {
      e.printStackTrace();
      fail();
    }
  }
  return null;
}
