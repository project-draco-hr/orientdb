{
  this.active=false;
  if (protocol != null) {
    protocol.sendShutdown();
    protocol=null;
  }
  if (serverSocket != null)   try {
    serverSocket.close();
  }
 catch (  IOException e) {
  }
}
