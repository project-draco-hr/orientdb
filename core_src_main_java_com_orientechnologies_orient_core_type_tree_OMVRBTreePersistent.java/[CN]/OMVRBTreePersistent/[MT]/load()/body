{
  dataProvider.load();
  setLastSearchNode(null,null);
  final ORID rootRid=dataProvider.getRoot();
  if (rootRid != null && dataProvider.getRoot().isValid())   root=loadEntry(null,rootRid);
  return this;
}
