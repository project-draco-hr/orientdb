{
  T prev=tree.put(iKey,iValue);
  if (iValue instanceof ORecord<?> && ((ORecord<?>)iValue).getIdentity().isTemporary()) {
    if (transactionalEntries == null)     transactionalEntries=new HashSet<String>();
    transactionalEntries.add(iKey);
  }
  return prev;
}
