{
  if (converted || !convertToRecord)   return;
  HashSet<Object> copy=new HashSet<Object>();
  for (Iterator<Object> it=iterator(); it.hasNext(); )   copy.add(convert(it.next()));
  clear();
  addAll(copy);
  copy.clear();
  converted=true;
}
