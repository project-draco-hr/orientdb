{
  acquireReadLock();
  try {
    if (version == 0)     return headerBuffer.get(SOFTLY_CLOSED_OFFSET_V_0) > 0;
    return headerBuffer.get(SOFTLY_CLOSED_OFFSET) > 0;
  }
  finally {
    releaseReadLock();
  }
}
