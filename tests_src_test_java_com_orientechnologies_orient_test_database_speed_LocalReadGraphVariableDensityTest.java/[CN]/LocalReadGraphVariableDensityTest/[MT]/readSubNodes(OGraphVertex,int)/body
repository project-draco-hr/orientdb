{
  if (iCurrentDeepLevel > maxDeep)   maxDeep=iCurrentDeepLevel;
  nodeReadCounter++;
  for (  OGraphEdge edge : iNode.getOutEdges()) {
    arcReadCounter++;
    readSubNodes(edge.getOut(),iCurrentDeepLevel + 1);
  }
}
