{
  if (BOOLEAN_SERIALIZER.deserializeFromByteBuffer(buffer,walChanges,offset + EMBEDDED_OFFSET)) {
    return embeddedObjectSerializedSize(INT_SERIALIZER.deserializeFromByteBuffer(buffer,walChanges,offset + EMBEDDED_SIZE_OFFSET));
  }
 else {
    return SBTREE_CONTAINER_SIZE;
  }
}
