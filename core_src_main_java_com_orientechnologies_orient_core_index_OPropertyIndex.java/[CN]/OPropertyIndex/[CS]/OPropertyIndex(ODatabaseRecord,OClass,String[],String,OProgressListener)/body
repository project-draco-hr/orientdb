{
  fields=iFields;
  final String indexName=getIndexName(iClass,iFields);
  delegate=iDatabase.getMetadata().getIndexManager().createIndex(indexName,iType,iClass.getClusterIds(),this,iProgressListener);
}
