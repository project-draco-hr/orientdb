{
  acquireExclusiveLock();
  try {
    saveState();
    metadataStore.synch();
    treeStateStore.synch();
    for (int i=0; i < filesMetadata.length; i++)     if (filesMetadata[i] != null)     diskCache.flushFile(fileLevelIds[i]);
    if (nullKeyIsSupported)     diskCache.flushFile(nullBucketFileId);
  }
 catch (  IOException e) {
    throw new OIndexException("Error during hash table flush",e);
  }
 finally {
    releaseExclusiveLock();
  }
}
