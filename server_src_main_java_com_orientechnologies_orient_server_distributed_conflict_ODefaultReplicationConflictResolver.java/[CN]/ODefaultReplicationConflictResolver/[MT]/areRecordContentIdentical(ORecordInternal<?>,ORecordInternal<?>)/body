{
  final byte[] rec1Stream=rec1.toStream();
  final byte[] rec2Stream=rec2.toStream();
  if (rec1Stream.length != rec2Stream.length)   return false;
  for (int i=0; i < rec1Stream.length; ++i)   if (rec1Stream[i] != rec2Stream[i])   return false;
  return true;
}
