{
  if (iIndexRecord != null && iIndexRecord.getIdentity().isValid()) {
    if (iUnique == null)     iUnique=Boolean.TRUE;
    index=new OIndex(iUnique,document.getDatabase(),this,OStorage.CLUSTER_INDEX_NAME,new ORecordId(iIndexRecord.getIdentity().toString()));
  }
}
