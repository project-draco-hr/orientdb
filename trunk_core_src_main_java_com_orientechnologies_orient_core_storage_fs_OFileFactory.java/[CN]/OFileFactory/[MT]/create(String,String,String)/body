{
  if (iType.equals(MMAP))   return new OFileMMap(iFileName,iOpenMode);
 else   if (iType.equals(CLASSIC))   return new OFileClassic(iFileName,iOpenMode);
  throw new IllegalArgumentException("Type " + iType + " not supported");
}
