{
  try {
    super.create();
    metadata.create();
    createRolesAndUsers();
    metadata.getSecurity().save();
    dictionary.create();
  }
 catch (  Exception e) {
    throw new ODatabaseException("Can't create database",e);
  }
  return (DB)this;
}
