{
  ODatabaseRecordThreadLocal.INSTANCE.set(dbOne);
  if (!dbOne.exists())   dbOne.create();
 else   dbOne.open("admin","admin");
  ODatabaseRecordThreadLocal.INSTANCE.set(dbTwo);
  if (!dbTwo.exists())   dbTwo.create();
 else   dbTwo.open("admin","admin");
  initScheme();
}
