{
  OProfiler.getInstance().updateStatistic("OServer.threads.actives",-1);
  OClientConnection conn=connections.remove(iChannelId);
  if (conn == null)   return;
  if (conn.database != null)   conn.database.close();
  handlers.remove(iChannelId);
}
