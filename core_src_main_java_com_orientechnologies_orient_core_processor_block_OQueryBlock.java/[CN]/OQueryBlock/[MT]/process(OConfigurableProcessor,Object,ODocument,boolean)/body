{
  if (!(iContent instanceof ODocument))   throw new OTransactionException("QueryBlock: expected document as content");
  final String command=parse((ODocument)iContent,iContext);
  final OCommandRequestText cmd=iReadOnly ? new OSQLSynchQuery<OIdentifiable>(command.toString()) : new OCommandSQL(command.toString());
  return cmd.execute();
}
