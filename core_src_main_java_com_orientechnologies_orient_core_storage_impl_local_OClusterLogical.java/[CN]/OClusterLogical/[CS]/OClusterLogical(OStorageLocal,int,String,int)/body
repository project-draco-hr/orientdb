{
  this(iName,iId,iPhysicalClusterId);
  try {
    map=new OTreeMapStorage<Long,OPhysicalPosition>(iStorage,OStorage.CLUSTER_DEFAULT_NAME,OStreamSerializerLong.INSTANCE,OStreamSerializerAnyStreamable.INSTANCE);
    map.getRecord().setIdentity(iPhysicalClusterId,ORID.CLUSTER_POS_INVALID);
    total=new OPhysicalPosition(0,0,(byte)0);
    map.put(new Long(-1),total);
  }
 catch (  Exception e) {
    throw new ODatabaseException("Error on creating internal map for logical cluster: " + iName,e);
  }
  map.save();
}
