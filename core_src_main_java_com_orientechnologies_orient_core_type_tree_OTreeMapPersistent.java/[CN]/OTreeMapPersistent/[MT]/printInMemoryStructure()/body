{
  System.out.println("* Entrypoints (" + entryPoints.size() + "), in cache="+ cache.size()+ ": *");
  for (  OTreeMapEntryPersistent<K,V> entryPoint : entryPoints)   printInMemoryStructure(entryPoint);
}
