{
  if (positionsToProcess.length > 0)   if (iterateThroughTombstones)   currentEntryPosition++;
 else   do {
    currentEntryPosition++;
  }
 while (currentEntryPosition < positionsToProcess.length && ORecordVersionHelper.isTombstone(positionsToProcess[currentEntryPosition].recordVersion));
}
