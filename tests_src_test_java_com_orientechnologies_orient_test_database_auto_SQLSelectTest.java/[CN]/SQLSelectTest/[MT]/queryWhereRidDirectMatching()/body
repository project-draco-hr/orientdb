{
  List<OClusterPosition> positions=getValidPositions(4);
  List<ODocument> result=database.command(new OSQLSynchQuery<ODocument>("select * from OUser where roles in #4:" + positions.get(0))).execute();
  Assert.assertEquals(result.size(),1);
}
