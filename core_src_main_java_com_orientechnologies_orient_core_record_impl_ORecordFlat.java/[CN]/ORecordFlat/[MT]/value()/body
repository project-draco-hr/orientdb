{
  if (value == null) {
    if (_source == null && getIdentity() != null && getIdentity().isValid())     load();
    value=OBinaryProtocol.bytes2string(_source);
    _source=null;
  }
  return value;
}
