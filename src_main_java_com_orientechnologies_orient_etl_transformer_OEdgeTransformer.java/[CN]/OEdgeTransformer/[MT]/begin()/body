{
  if (graph == null) {
    graph=pipeline.getGraphDatabase();
    if (graph == null)     throw new OTransformException(getName() + ": graph instance not set");
    final OClass cls=graph.getEdgeType(edgeClass);
    if (cls == null)     graph.createEdgeType(edgeClass);
  }
  super.begin();
}
