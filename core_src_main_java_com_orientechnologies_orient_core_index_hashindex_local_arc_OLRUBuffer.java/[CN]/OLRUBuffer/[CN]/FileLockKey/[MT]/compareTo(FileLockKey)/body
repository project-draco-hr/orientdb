{
  int result=fileName.compareTo(otherKey.fileName);
  if (result != 0)   return result;
  if (pageIndex > otherKey.pageIndex)   return 1;
  if (pageIndex < otherKey.pageIndex)   return -1;
  return 0;
}
