{
  if (status == MULTIVALUE_STATUS.ALL_RIDS && iValue instanceof ORecord<?> && !((ORecord<?>)iValue).getIdentity().isNew())   iValue=((ORecord<?>)iValue).getIdentity();
 else   status=ORecordMultiValueHelper.getStatus(status,iValue);
  return super.put(iKey,iValue);
}
