{
  if (result != null && !(result instanceof OCommandRequest) && result instanceof Iterable<?>)   debug(iContext,"Returned %s",OCollections.toString((Iterable<?>)result));
 else   debug(iContext,"Returned %s",result);
}
