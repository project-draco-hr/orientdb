{
  setDataCommandInfo("Close Database");
  if (connection != null) {
    if (server.getClientConnectionManager().disconnect(connection.id) && Boolean.FALSE.equals(tokenBased))     sendShutdown();
  }
}
