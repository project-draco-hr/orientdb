{
  OLogManager.instance().error(this,iMessage,e,iParams);
  final Throwable t=e.getCause();
  if (t instanceof OException)   throw (OException)t;
  throw new OStorageException(String.format(iMessage,iParams));
}
