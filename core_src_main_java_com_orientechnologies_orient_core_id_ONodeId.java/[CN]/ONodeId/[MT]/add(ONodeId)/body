{
  if (idToAdd.signum == 0)   return new ONodeId(chunks,signum);
  final int[] result;
  if (signum == idToAdd.signum) {
    result=addArrays(chunks,idToAdd.chunks);
    return new ONodeId(result,signum);
  }
  final int cmp=compareChunks(chunks,idToAdd.chunks);
  if (cmp == 0)   return ZERO;
  if (cmp > 0)   result=substructArrays(chunks,idToAdd.chunks);
 else   result=substructArrays(idToAdd.chunks,chunks);
  if (Arrays.equals(ZERO.chunks,result))   return ZERO;
  return new ONodeId(result,cmp == signum ? 1 : 0);
}
