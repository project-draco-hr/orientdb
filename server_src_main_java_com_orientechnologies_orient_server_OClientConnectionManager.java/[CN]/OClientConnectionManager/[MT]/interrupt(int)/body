{
  acquireExclusiveLock();
  try {
    final OClientConnection connection=connections.get(iChannelId);
    if (connection != null) {
      final ONetworkProtocol protocol=connection.protocol;
      if (protocol != null)       protocol.interrupt();
    }
  }
  finally {
    releaseExclusiveLock();
  }
}
