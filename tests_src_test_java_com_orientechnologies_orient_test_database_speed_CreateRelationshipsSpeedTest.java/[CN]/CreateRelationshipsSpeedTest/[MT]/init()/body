{
  if (!database.getClusterNames().contains("Animal"))   database.getStorage().addCluster("Animal",OStorage.CLUSTER_TYPE.PHYSICAL);
  if (!database.getClusterNames().contains("Vaccinate"))   database.getStorage().addCluster("Vaccinate",OStorage.CLUSTER_TYPE.PHYSICAL);
}
