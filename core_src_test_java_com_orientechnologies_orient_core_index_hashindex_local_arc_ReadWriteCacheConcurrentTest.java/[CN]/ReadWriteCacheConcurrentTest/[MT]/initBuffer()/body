{
  writeBuffer=new OWOWCache(false,8 + systemOffset,10000,null,-1,15000 * (8 + systemOffset),4 * (8 + systemOffset) + 15000 * (8 + systemOffset),storageLocal,true,1);
  readBuffer=new O2QCache(4 * (8 + systemOffset),8 + systemOffset,true,20);
}
