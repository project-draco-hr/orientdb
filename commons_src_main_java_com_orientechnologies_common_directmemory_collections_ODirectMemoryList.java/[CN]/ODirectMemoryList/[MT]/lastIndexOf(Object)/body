{
  if (o == null) {
    for (int i=size - 1; i >= 0; i--)     if (getData(elementData,i) == null)     return i;
  }
 else {
    for (int i=size - 1; i >= 0; i--)     if (o.equals(getData(elementData,i)))     return i;
  }
  return -1;
}
