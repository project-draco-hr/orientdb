{
  List<ODocument> result=database.command(new OSQLSynchQuery<ODocument>("select from ORole where name = '" + iRoleName + "'")).execute();
  if (result != null && result.size() > 0)   return new ORole(result.get(0));
  return null;
}
