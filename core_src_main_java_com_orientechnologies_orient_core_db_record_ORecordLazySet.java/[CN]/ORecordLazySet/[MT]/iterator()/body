{
  if (newItems == null)   return delegate.iterator();
  lazyLoad(false);
  return new OLazyRecordMultiIterator(delegate.sourceRecord,delegate.database,delegate.recordType,new Object[]{delegate.iterator(),newItems.keySet().iterator()},delegate.autoConvertToRecord);
}
