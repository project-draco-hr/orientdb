{
  final HashMap<String,Object> map=new HashMap<String,Object>();
  if (inherited != null && !inherited.isEmpty())   for (  OCommandContext in : inherited)   map.putAll(in.getVariables());
  if (variables != null)   map.putAll(variables);
  return map;
}
