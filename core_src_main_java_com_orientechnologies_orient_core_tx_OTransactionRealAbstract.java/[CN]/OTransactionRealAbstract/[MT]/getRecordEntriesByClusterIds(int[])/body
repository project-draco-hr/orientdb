{
  final List<OTransactionRecordEntry> result=new ArrayList<OTransactionRecordEntry>();
  if (iIds == null)   for (  OTransactionRecordEntry entry : recordEntries.values()) {
    result.add(entry);
  }
 else   for (  OTransactionRecordEntry entry : recordEntries.values()) {
    for (    int id : iIds) {
      if (entry.getRecord() != null && entry.getRecord().getIdentity().getClusterId() == id) {
        result.add(entry);
        break;
      }
    }
  }
  return result;
}
