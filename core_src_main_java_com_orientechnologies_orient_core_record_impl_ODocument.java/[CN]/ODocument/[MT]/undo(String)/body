{
  if (!_trackingChanges)   throw new OConfigurationException("Cannot undo the document because tracking of changes is disabled");
  final Object value=_fieldOriginalValues.get(field);
  if (value == null)   _fieldValues.remove(field);
 else   _fieldValues.put(field,value);
  _fieldOriginalValues.remove(field);
  return this;
}
