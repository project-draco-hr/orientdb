{
  if (lowDiskSpace != null)   throw new OLowDiskSpaceException("Error occurred while executing a write operation to database '" + name + "' due to limited free space on the disk ("+ (lowDiskSpace.freeSpace / (1024 * 1024))+ " MB). The database is now working in read-only mode."+ " Please close the database (or stop OrientDB), make room on your hard drive and then reopen the database. "+ "The minimal required space is ("+ (lowDiskSpace.requiredSpace / (1024 * 1024))+ " MB). "+ "Required space is calculated as sum of disk space required by WAL (may be set using parameter "+ OGlobalConfiguration.WAL_MAX_SIZE.getKey()+ ") and space required for data.");
}
