{
  _recordId=new ORecordId();
  if (iFieldMap != null && iFieldMap.size() > 0)   for (  Entry<Object,Object> entry : iFieldMap.entrySet()) {
    field(entry.getKey().toString(),entry.getValue());
  }
}
