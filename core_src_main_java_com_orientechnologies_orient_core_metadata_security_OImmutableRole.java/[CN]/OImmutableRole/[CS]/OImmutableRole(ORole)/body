{
  if (role.getParentRole() == null)   parentRole=null;
 else   parentRole=new OImmutableRole(role.getParentRole());
  mode=role.getMode();
  name=role.getName();
  rid=role.getIdentity().getIdentity();
  rules.putAll(role.getRules());
}
