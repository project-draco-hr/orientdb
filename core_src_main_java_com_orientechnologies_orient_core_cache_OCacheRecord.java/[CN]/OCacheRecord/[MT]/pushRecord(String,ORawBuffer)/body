{
  if (maxSize == 0)   return;
  final boolean locked=acquireExclusiveLock();
  try {
    if (iContent == null || iContent.buffer.length == 0)     cache.remove(iRecord);
 else     cache.put(iRecord,iContent);
  }
  finally {
    releaseExclusiveLock(locked);
  }
}
