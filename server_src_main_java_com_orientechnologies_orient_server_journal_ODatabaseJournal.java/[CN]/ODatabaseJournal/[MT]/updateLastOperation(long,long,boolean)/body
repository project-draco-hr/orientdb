{
  lock.lock();
  try {
    if (!iForce && lastExecuted[0] == iRunId && lastExecuted[1] >= iOperationSerial) {
      ODistributedServerLog.debug(this,cluster.getLocalNodeId(),null,DIRECTION.NONE,"skipped updating journal last task db='%s' op=%d.%d found=%d.%d",storage.getName(),iRunId,iOperationSerial,lastExecuted[0],lastExecuted[1]);
      return;
    }
    ODistributedServerLog.debug(this,cluster.getLocalNodeId(),null,DIRECTION.NONE,"updating journal last task db='%s' op=%d.%d",storage.getName(),iRunId,iOperationSerial);
    lastExecuted[0]=iRunId;
    lastExecuted[1]=iOperationSerial;
  }
  finally {
    lock.unlock();
  }
}
