{
  if (iRequestCode == OChannelDistributedProtocol.PUSH_LEADER_AVAILABLE_DBS) {
    try {
      ODocument updateCfg=manager.getLeader().updatePeerDatabases(clientId,(ODocument)obj);
      manager.getReplicator().updateConfiguration(updateCfg);
    }
 catch (    Exception e) {
      OLogManager.instance().error(this,"Cluster <%s>: error on updating leader's database list for peer %s",e,manager.getConfig().name,clientId);
    }
  }
 else   if (wrapped != null)   wrapped.onRequest(iRequestCode,obj);
}
