{
  if (converted || !convertToRecord)   return;
  final Object o=list.get(iIndex);
  if (o != null && o instanceof ODocument)   list.set(iIndex,database.getUserObjectByRecord((ORecordInternal<?>)o,fetchPlan));
}
