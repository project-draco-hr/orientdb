{
  int index=0;
  long longestOffset=offset;
  while (longestOffset > 0 && index < num) {
    if (this.getOperationStatus(longestOffset))     longestOffset=this.getPreviousOperation(longestOffset);
    index++;
  }
  return longestOffset;
}
