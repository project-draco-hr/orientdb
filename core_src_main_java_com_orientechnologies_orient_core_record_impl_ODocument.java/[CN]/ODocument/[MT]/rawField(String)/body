{
  checkForLoading();
  checkForFields();
  final Object value=_fieldValues.get(iFieldName);
  if (value != null)   return (RET)value;
  return (RET)ODocumentHelper.getFieldValue(this,iFieldName);
}
