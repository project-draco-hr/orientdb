{
  int clusterId=database.getClusterIdByName(iClassName);
  if (clusterId == -1)   clusterId=database.addLogicalCluster(iClassName,database.getDefaultClusterId());
  return createClass(iClassName,clusterId);
}
