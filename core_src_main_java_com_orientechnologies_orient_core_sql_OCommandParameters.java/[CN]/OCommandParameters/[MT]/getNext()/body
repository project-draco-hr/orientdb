{
  if (parameters.size() <= counter)   throw new IndexOutOfBoundsException("No enough parameters are passed. You requested parameter " + counter + " but only "+ parameters.size()+ " was found");
  return parameters.get(counter++);
}
