{
  if (iClassName == null)   return false;
  if (iClassName.equals(name) || iClassName.equals(shortName))   return true;
  if (superClass == null)   return false;
  return isSubClassOf(owner.getClass(iClassName));
}
