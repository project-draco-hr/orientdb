{
  final OMemoryStream buffer=new OMemoryStream();
  buffer.set(text);
  buffer.set(limit);
  buffer.set(fetchPlan != null ? fetchPlan : "");
  buffer.set(serializeQueryParameters(parameters));
  return buffer;
}
