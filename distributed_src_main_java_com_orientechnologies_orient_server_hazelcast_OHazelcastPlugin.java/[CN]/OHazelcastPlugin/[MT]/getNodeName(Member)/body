{
  if (iMember == null)   return "?";
  final ODocument cfg=getNodeConfigurationByUuid(iMember.getUuid());
  if (cfg != null)   return cfg.field("name");
  if (iMember.getUuid().equals(nodeUuid))   return nodeName;
  return "ext:" + iMember.getUuid();
}
