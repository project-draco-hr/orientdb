{
  if (nativeAcceleration)   return CONVERTER.getLong(buffer,index * KEY_SIZE + BUCKET_SIZE_SIZE);
  long result=keys[index];
  if (result > -1)   return result;
  result=CONVERTER.getLong(buffer,index * KEY_SIZE + BUCKET_SIZE_SIZE);
  keys[index]=result;
  return result;
}
