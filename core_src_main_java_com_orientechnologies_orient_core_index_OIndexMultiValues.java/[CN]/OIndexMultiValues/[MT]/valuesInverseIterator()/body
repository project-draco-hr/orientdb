{
  checkForRebuild();
  acquireExclusiveLock();
  try {
    return new OIndexIterator(this,new OMultiCollectionIterator<OIdentifiable>(((OMVRBTree.Values)map.values()).inverseIterator()));
  }
  finally {
    releaseExclusiveLock();
  }
}
