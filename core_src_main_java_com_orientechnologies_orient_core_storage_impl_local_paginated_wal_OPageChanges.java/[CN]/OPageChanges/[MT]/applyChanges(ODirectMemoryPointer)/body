{
  for (  ChangeUnit changeUnit : changeUnits) {
    if (changeUnit.newValues != null)     pointer.set(changeUnit.pageOffset + ODurablePage.PAGE_PADDING,changeUnit.newValues,0,changeUnit.newValues.length);
  }
}
