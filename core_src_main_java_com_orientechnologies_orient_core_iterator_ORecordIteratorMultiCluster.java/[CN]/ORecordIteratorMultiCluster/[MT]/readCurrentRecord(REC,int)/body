{
  if (limit > -1 && browsedRecords >= limit)   return null;
  currentClusterPosition+=iMovement;
  if (lowLevelDatabase.executeReadRecord(clusterIds[currentClusterIdx],currentClusterPosition,iRecord,fetchPlan) != null)   browsedRecords++;
  return iRecord;
}
