{
  if (iNetwork == null)   return;
  try {
    iNetwork.flush();
  }
 catch (  IOException e) {
  }
  if (debug)   System.out.println("<- req: " + getSessionId());
  iNetwork.getLockWrite().unlock();
synchronized (this) {
    notifyAll();
  }
}
