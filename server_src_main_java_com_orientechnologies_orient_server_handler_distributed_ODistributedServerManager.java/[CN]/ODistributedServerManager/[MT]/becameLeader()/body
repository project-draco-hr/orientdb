{
synchronized (this) {
    if (peer != null) {
      peer.shutdown();
      peer=null;
    }
    if (leader == null) {
      leader=new OLeaderNode(this);
      sendPresence();
    }
  }
  setStatus(STATUS.LEADER);
}
