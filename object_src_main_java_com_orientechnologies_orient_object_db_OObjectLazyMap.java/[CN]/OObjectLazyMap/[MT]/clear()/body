{
  converted=true;
  if (orphanRemoval)   for (  OIdentifiable value : underlying.values())   ((OObjectProxyMethodHandler)sourceRecord.getHandler()).getOrphans().add(value.getIdentity());
  underlying.clear();
  super.clear();
  setDirty();
}
