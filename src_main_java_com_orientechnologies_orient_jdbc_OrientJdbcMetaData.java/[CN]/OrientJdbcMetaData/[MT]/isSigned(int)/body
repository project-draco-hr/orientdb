{
  ODocument currentRecord=this.resultSet.unwrap(ODocument.class);
  if (currentRecord == null)   return false;
 else   return this.isANumericColumn(currentRecord.fieldType(currentRecord.fieldNames()[column - 1]));
}
