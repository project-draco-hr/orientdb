{
  if (direction.equals(Direction.OUT))   return new OrientVertex(getOutVertex());
 else   if (direction.equals(Direction.IN))   return new OrientVertex(getInVertex());
 else   throw ExceptionFactory.bothIsNotSupported();
}
