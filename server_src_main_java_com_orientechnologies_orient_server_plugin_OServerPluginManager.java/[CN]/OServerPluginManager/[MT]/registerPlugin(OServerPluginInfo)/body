{
  final String pluginName=iPlugin.getName();
  if (activePlugins.contains(pluginName))   throw new IllegalStateException("Plugin '" + pluginName + "' already registered");
  activePlugins.putIfAbsent(pluginName,iPlugin);
}
