{
  if (_recordId == null || _recordId == ORecordId.EMPTY_RECORD_ID)   _recordId=new ORecordId(iClusterId,iPosition);
 else {
    _recordId.clusterId=iClusterId;
    _recordId.clusterPosition=iPosition;
  }
  return this;
}
