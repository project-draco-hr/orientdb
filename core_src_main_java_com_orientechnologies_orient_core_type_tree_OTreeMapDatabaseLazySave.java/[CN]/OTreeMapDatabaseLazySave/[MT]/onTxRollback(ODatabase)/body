{
  entryPoints.clear();
  try {
    if (root != null)     ((OTreeMapEntryDatabase<K,V>)root).load();
  }
 catch (  IOException e) {
    throw new OIndexException("Error on loading root node");
  }
}
