{
  if (iOptions == null || iOptions.size() == 0)   return;
  final Object connType=iOptions.get(PARAM_CONNECTION_MODE.toLowerCase());
  if (connType != null)   connectionMode=CONNECTION_MODE.valueOf(connType.toString().toUpperCase());
  connectionOptions=new HashMap<String,Object>(iOptions);
}
