{
  final List<ODatabase<?>> result=new ArrayList<ODatabase<?>>();
  for (  ODatabase<?> i : instances.keySet()) {
    if (i != null && i.getName().equals(iDatabaseName))     result.add(i);
  }
  return result;
}
