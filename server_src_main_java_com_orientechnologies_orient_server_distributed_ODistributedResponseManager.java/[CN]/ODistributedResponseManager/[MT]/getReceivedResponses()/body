{
  final List<ODistributedResponse> parsed=new ArrayList<ODistributedResponse>();
  for (  OResponseData r : responses.values())   if (r != NO_RESPONSE)   parsed.add((ODistributedResponse)r.response);
  return parsed;
}
