{
  checkForDatabase();
  currentRecord=currentDatabase.executeReadRecord(new ORecordId(iRecordId),null,iFetchPlan,true,false,OStorage.LOCKING_STRATEGY.NONE);
  displayRecord(null);
  message("\nOK");
}
