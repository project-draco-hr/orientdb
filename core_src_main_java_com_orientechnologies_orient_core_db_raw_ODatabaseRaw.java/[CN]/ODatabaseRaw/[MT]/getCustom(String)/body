{
  if (storage.getConfiguration().properties == null)   return null;
  for (  OStorageEntryConfiguration e : storage.getConfiguration().properties) {
    if (e.name.equals(iName))     return e.value;
  }
  return null;
}
