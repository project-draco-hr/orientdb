{
  final boolean locked=lock.acquireSharedLock();
  try {
    for (OMVRBTreeEntry<K,V> e=getFirstEntry(); e != null; e=successor(e))     if (valEquals(value,e.getValue()))     return true;
    return false;
  }
  finally {
    lock.releaseSharedLock(locked);
  }
}
