{
  final OUser replUser=database.getMetadata().getSecurity().getUser(replicatorUser.name);
  if (replUser == null)   database.getMetadata().getSecurity().createUser(replicatorUser.name,replicatorUser.password,"admin");
}
