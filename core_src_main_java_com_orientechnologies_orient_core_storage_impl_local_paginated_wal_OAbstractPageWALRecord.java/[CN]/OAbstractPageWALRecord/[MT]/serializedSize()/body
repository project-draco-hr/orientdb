{
  if (prevUnitRecord == null)   return OLongSerializer.LONG_SIZE + OIntegerSerializer.INT_SIZE + OByteSerializer.BYTE_SIZE;
  return 2 * (OLongSerializer.LONG_SIZE + OIntegerSerializer.INT_SIZE) + OByteSerializer.BYTE_SIZE;
}
