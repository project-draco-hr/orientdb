{
  if (!optimizeExecution()) {
    fetchLimit=getQueryFetchLimit();
    executeSearch(iArgs);
    applyFlatten();
    handleNoTarget();
    applyOrderBy();
    applyLimitAndSkip();
  }
  return getResult();
}
