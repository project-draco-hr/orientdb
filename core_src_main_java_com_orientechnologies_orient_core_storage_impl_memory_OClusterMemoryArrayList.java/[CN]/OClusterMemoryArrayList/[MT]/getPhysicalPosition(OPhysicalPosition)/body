{
  acquireSharedLock();
  try {
    if (iPPosition.clusterPosition.intValue() < 0 || iPPosition.clusterPosition.compareTo(getLastPosition()) > 0)     return null;
    return entries.get(iPPosition.clusterPosition.intValue());
  }
  finally {
    releaseSharedLock();
  }
}
