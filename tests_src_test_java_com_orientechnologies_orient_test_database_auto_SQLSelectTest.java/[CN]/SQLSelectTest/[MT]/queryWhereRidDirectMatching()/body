{
  List<Long> positions=getValidPositions(3);
  List<ODocument> result=database.command(new OSQLSynchQuery<ODocument>("select * from OUser where roles in #3:" + positions.get(0))).execute();
  Assert.assertEquals(result.size(),1);
}
