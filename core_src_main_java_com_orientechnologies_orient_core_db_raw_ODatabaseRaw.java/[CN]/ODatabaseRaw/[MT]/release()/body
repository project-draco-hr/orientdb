{
  final OStorageLocal storage;
  if (getStorage() instanceof OStorageLocal)   storage=((OStorageLocal)getStorage());
 else {
    OLogManager.instance().error(this,"We can not freeze non local storage.");
    return;
  }
  storage.release();
}
