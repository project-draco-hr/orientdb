{
  if (iCurrent instanceof ODocument) {
    final ODocument current=(ODocument)iCurrent;
    if (iOther instanceof ORID) {
      if (!current.isDirty()) {
        if (!current.getIdentity().equals(iOther))         return false;
      }
 else {
        final ODocument otherDoc=iOtherDb.load((ORID)iOther);
        if (!ODocumentHelper.hasSameContentOf(current,iMyDb,otherDoc,iOtherDb))         return false;
      }
    }
 else     if (!ODocumentHelper.hasSameContentOf(current,iMyDb,(ODocument)iOther,iOtherDb))     return false;
  }
 else   if (!compareScalarValues(iCurrent,iOther))   return false;
  return true;
}
