{
  ODatabaseDocumentInternal db=getDatabase();
  if (db.getStorage() instanceof OStorageProxy) {
    getDatabase().command(new OCommandSQL("delete cluster :1")).execute(clusterName);
    reload();
  }
 else   removeBlobClusterInternal(clusterName);
}
