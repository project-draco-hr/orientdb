{
  final List<String> recordColumns=new ArrayList<String>();
  if (iRecord instanceof ODocument)   for (  String fieldName : ((ODocument)iRecord).fieldNames())   recordColumns.add(fieldName);
  dumpRecordInTable(iIndex,iRecord,recordColumns,iColumns);
}
