{
  if (isWriteLockedByCurrentThread())   return;
  if (throwException) {
    if (!readLock().tryLock()) {
      throw new OModificationOperationProhibitedException();
    }
  }
 else {
    readLock().lock();
  }
}
