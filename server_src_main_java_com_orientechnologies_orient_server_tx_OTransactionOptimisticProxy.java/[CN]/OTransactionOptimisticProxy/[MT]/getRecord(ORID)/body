{
  ORecordInternal<?> record=super.getRecord(rid);
  if (record == OTransactionRealAbstract.DELETED_RECORD)   return null;
 else   if (record == null && rid.isNew())   record=(ORecordInternal<?>)createdRecords.get(rid);
  return record;
}
