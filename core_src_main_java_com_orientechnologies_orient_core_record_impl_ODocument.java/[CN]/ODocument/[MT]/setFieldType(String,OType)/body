{
  if (iFieldType != null) {
    if (_fieldTypes == null)     _fieldTypes=new HashMap<String,OType>();
    _fieldTypes.put(iFieldName,iFieldType);
  }
 else   if (_fieldTypes != null) {
    _fieldTypes.remove(iFieldName);
    if (_fieldTypes.size() == 0)     _fieldTypes=null;
  }
  return this;
}
