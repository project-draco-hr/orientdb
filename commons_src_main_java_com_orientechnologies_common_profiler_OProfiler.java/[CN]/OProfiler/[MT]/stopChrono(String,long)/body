{
  if (recordingFrom < 0)   return -1;
  acquireSharedLock();
  try {
    if (lastSnapshot != null)     lastSnapshot.stopChrono(iName,iStartTime);
    return realTime.stopChrono(iName,iStartTime);
  }
  finally {
    releaseSharedLock();
  }
}
