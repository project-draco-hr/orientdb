{
  final OTransactionIndexChanges indexChanges=database.getTransaction().getIndexChanges(delegate.getName());
  long tot;
  if (indexChanges == null || !indexChanges.cleared)   tot=delegate.getSize();
 else   tot=0;
  return tot;
}
