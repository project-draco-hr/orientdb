{
  final OMemoryStream out=new OMemoryStream();
  try {
    int b;
    while ((b=in.read()) > -1) {
      out.write(b);
    }
    out.flush();
    _source=out.toByteArray();
  }
  finally {
    out.close();
  }
  _size=_source.length;
  return _size;
}
