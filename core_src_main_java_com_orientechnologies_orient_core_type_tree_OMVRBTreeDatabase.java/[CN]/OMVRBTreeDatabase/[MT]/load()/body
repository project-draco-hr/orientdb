{
  if (!record.getIdentity().isValid())   return this;
  lock.acquireExclusiveLock();
  try {
    record.load();
    fromStream(record.toStream());
    return this;
  }
  finally {
    lock.releaseExclusiveLock();
  }
}
