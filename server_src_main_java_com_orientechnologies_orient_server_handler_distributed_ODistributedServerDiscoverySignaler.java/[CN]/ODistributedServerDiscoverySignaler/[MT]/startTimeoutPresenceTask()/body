{
  Orient.getTimer().schedule(new TimerTask(){
    @Override public void run(){
      try {
        if (running)         manager.becameLeader(forceLeadership);
        shutdown();
        cancel();
      }
 catch (      Exception e) {
      }
    }
  }
,manager.networkTimeoutLeader);
}
