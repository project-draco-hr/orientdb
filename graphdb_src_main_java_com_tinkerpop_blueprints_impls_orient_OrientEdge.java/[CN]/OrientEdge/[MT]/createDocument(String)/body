{
  final String className=getClassName(iLabel);
  final ODocument doc=new ODocument(className);
  if (iLabel != null && !settings.useClassForEdgeLabel)   doc.field(OrientElement.LABEL_FIELD_NAME,iLabel);
  return doc;
}
