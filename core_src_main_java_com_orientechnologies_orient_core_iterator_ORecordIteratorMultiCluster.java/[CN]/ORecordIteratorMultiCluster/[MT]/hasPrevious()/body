{
  if (limit > -1 && browsedRecords >= limit)   return false;
  if (liveUpdated)   return currentClusterPosition > database.getStorage().getClusterDataRange(lastClusterId)[0];
  return currentClusterPosition > firstClusterPosition;
}
