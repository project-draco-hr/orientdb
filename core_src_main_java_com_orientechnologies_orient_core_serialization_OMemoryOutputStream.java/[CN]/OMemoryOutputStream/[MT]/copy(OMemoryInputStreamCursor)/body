{
  if (iInput == null)   return -1;
  final int begin=position;
  final int size=iInput.getSize();
  assureSpaceFor(OConstants.SIZE_INT + size);
  add(size);
  for (int i=0; i < size; ++i)   write(iInput.read());
  return begin;
}
