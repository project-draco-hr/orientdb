{
  OLogManager.instance().warn(this,"-> %s (%s mode) CONFLICT record %s (cannot be deleted on other node)",this,iRequestType,iRecord.getIdentity());
  final ODocument doc=createConflictDocument(iOperation,iRecord);
  doc.save();
}
