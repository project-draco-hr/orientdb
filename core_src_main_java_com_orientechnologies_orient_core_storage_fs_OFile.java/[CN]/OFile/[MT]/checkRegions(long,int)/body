{
  if (iOffset + iLength > filledUpTo)   throw new OIOException("You cannot access outside the file size (" + filledUpTo + " bytes). You have requested portion "+ iOffset+ "-"+ (iOffset + iLength)+ " bytes. File: "+ toString());
  return iOffset;
}
