{
  OServerCommand cmd=(OServerCommand)iServerCommandInstance;
  for (  String name : cmd.getNames())   if (name.contains("*"))   wildcardCommands.put(name,cmd);
 else   exactCommands.put(name,cmd);
}
