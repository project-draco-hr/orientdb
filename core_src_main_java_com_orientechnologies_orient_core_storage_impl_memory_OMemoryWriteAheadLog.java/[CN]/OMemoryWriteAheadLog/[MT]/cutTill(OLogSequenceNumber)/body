{
synchronized (syncObject) {
    if (records.isEmpty())     return;
    long index=records.get(0).getLsn().getPosition() - lsn.getPosition();
    if (index < 0)     return;
    if (index > records.size())     index=records.size();
    for (int i=0; i < index; i++)     records.remove(0);
  }
}
