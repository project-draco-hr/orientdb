{
  database.open("admin","admin");
  database.getMetadata().getIndexManager().createIndex("idxTxAwareMultiValueGetEntriesTest","NOTUNIQUE",new OSimpleKeyIndexDefinition(OType.INTEGER),null,null);
  database.close();
}
