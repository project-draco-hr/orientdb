{
  return networkOperation(new OStorageRemoteOperation<OStorageOperationResult<Boolean>>(){
    @Override public OStorageOperationResult<Boolean> execute(    final OChannelBinaryAsynchClient network,    OStorageRemoteSession session) throws IOException {
      int mode;
      if (iMode == 1 && iCallback == null)       mode=2;
 else       mode=iMode;
      return new OStorageOperationResult<Boolean>(deleteRecord(OChannelBinaryProtocol.REQUEST_RECORD_DELETE,iRid,iVersion,mode,iCallback,network,session));
    }
  }
,"Error on delete record " + iRid);
}
