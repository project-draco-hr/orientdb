{
  writeByte(iCommand);
  writeInt(session.sessionId);
  if (token != null) {
    if (!session.has(this)) {
      writeBytes(token);
      session.add(this);
    }
 else     writeBytes(new byte[]{});
  }
}
