{
  if (binarySizeKnown)   return binarySize;
  binarySizeKnown=true;
  binarySize=OBinaryProtocol.SIZE_LONG + OBinaryProtocol.SIZE_BYTE + OVersionFactory.instance().getVersionSize()+ OBinaryProtocol.SIZE_INT;
  return binarySize;
}
