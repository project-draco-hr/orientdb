{
  lock.acquireExclusiveLock();
  try {
    if (pendingLogs == 1)     if (file.getFreeSpace() < RECORD_SIZE)     reset();
 else     pendingLogs=0;
  }
  finally {
    lock.releaseExclusiveLock();
  }
}
