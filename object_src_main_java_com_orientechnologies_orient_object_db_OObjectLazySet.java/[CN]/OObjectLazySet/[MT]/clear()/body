{
  setDirty();
  super.clear();
  if (orphanRemoval)   for (  OIdentifiable value : underlying)   ((OObjectProxyMethodHandler)sourceRecord.getHandler()).getOrphans().add(value.getIdentity());
  underlying.clear();
}
