{
  checkForDatabase();
  currentRecord=currentDatabase.executeReadRecord(new ORecordId(iRecordId),null,iFetchPlan,true,false,OStorage.LOCKING_STRATEGY.DEFAULT);
  displayRecord(null);
  message("\nOK");
}
