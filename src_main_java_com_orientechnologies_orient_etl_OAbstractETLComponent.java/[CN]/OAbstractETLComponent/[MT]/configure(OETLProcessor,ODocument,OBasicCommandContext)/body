{
  processor=iProcessor;
  context=iContext;
  final String ifExpression=iConfiguration.field("if");
  if (ifExpression != null)   ifFilter=new OSQLFilter(ifExpression,iContext,null);
  if (iConfiguration.containsField("verbose"))   verbose=iConfiguration.field("verbose");
}
