{
  if (iFuncParams[0] == null)   return null;
  final StringBuilder buffer=new StringBuilder(iFuncParams[0].toString());
  for (int i=1; i < iFuncParams.length; ++i)   if (iFuncParams[i] != null)   buffer.append(iFuncParams[i]);
  return buffer.toString();
}
