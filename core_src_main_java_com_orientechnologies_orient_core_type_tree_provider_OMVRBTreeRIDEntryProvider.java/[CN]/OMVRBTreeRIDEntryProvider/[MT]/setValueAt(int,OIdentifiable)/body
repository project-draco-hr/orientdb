{
  if (iValue == null)   return false;
  try {
    itemToStream(iValue,iIndex);
  }
 catch (  IOException e) {
    throw OException.wrapException(new OSerializationException("Cannot serialize entryRID object: " + this),e);
  }
  if (rids != null)   rids[iIndex]=iValue;
  return setDirty();
}
