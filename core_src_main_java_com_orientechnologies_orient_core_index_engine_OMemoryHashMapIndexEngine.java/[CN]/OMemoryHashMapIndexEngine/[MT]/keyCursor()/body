{
  return new OIndexKeyCursor(){
    private Iterator<Object> keysIterator=concurrentHashMap.keySet().iterator();
    @Override public Object next(    int prefetchSize){
      if (!keysIterator.hasNext())       return null;
      return keysIterator.next();
    }
  }
;
}
