{
  if (componentName == null)   return performanceCountersHolder.getReadSpeedFromCacheInMB();
  final PerformanceCountersHolder cHolder=countersByComponent.get(componentName);
  if (cHolder != null)   return cHolder.getReadSpeedFromCacheInMB();
  return -1;
}
