{
  if (subIterator != null)   while (subIterator.hasNext()) {
    final Collection<OIdentifiable> next=subIterator.next();
    if (next != null && !next.isEmpty()) {
      partialIterator=next.iterator();
      return true;
    }
  }
  return false;
}
