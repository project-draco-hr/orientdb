{
  if (getDatabase().getTransaction().isActive())   throw new IllegalStateException("Cannot create a new class inside a transaction");
  int clusterId=getDatabase().getClusterIdByName(iClassName);
  if (clusterId == -1)   clusterId=createCluster(iType.toString(),iClassName);
  return createClass(iClassName,iSuperClass,clusterId);
}
