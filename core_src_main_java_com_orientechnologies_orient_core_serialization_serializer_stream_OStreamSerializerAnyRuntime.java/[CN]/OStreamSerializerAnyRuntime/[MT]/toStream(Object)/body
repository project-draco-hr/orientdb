{
  if (iObject == null)   return new byte[0];
  final ByteArrayOutputStream os=new ByteArrayOutputStream();
  final ObjectOutputStream oos=new ObjectOutputStream(os);
  oos.writeObject(iObject);
  oos.close();
  return os.toByteArray();
}
