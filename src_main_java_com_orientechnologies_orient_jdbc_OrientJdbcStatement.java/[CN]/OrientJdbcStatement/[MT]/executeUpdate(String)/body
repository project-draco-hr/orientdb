{
  OCommandSQL command=new OCommandSQL(sql);
  Object response=database.command(command).execute();
  if (response instanceof ODocument)   return 1;
 else   if (response instanceof Integer)   return (Integer)response;
 else   return 0;
}
