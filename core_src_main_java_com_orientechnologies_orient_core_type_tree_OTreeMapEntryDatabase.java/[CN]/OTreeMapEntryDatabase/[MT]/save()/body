{
  if (!record.isDirty())   return this;
  flush2Record();
  if (parent != null)   if (!parent.record.getIdentity().equals(parentRid))   OLogManager.instance().error(this,"checkEntryStructure: Wrong parent node loaded: " + parentRid);
  checkEntryStructure();
  return this;
}
