{
  final OHazelcastDistributedRequest req=new OHazelcastDistributedRequest(getLocalNodeName(),iDatabaseName,iTask.getClusterName(),iTask,iExecutionMode);
  final ODistributedResponse response=messageService.send(req);
  if (response != null)   return response.getPayload();
  return null;
}
