{
  final Collection<OServerPluginInfo> handlers=iServer.getPlugins();
  if (handlers != null)   for (  OServerPluginInfo handler : handlers) {
    handler.getInstance().onAfterClientRequest(connection,iRequestType);
  }
}
