{
  addUser();
  level2cache.startup();
  if (open)   return 0;
  if (!exists())   throw new OStorageException("Can't open the storage '" + name + "' because it not exists in path: "+ url);
  open=true;
  return 0;
}
