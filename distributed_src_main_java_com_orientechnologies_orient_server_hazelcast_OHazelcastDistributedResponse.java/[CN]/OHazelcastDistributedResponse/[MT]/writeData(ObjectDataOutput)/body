{
  out.writeLong(requestId);
  out.writeUTF(executorNodeName);
  out.writeUTF(senderNodeName);
  if (!(payload instanceof DataSerializable))   out.writeObject(new OSerializableWrappedResponse((Serializable)payload));
 else   out.writeObject(payload);
}
