{
  if (iNewGraph == null)   throw new IllegalArgumentException("Graph is null");
  final OrientBaseGraph graph=OrientBaseGraph.getActiveInstance();
  if (graph != iNewGraph)   throw new IllegalStateException("New and attached graphs should match");
  settings=iNewGraph.settings;
  return this;
}
