{
  acquireExclusiveLock();
  try {
    flush();
    for (    final OIndexInternal<?> idx : indexes.values())     if (idx instanceof OCloseable)     ((OCloseable)idx).close();
    indexes.clear();
    classPropertyIndex.clear();
  }
  finally {
    releaseExclusiveLock();
  }
}
