{
  if (arrayOne.length < arrayTwo.length)   return -1;
  if (arrayOne.length > arrayTwo.length)   return 1;
  for (int i=0; i < arrayOne.length; i++) {
    if (arrayOne[i] > arrayTwo[i])     return 1;
    if (arrayOne[i] < arrayTwo[i])     return -1;
  }
  return 0;
}
