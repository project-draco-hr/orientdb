{
synchronized (databaseJsonConfiguration) {
    ODocument cfg=databaseJsonConfiguration.get(iDatabaseName);
    if (cfg == null)     cfg=databaseJsonConfiguration.get("*");
    return new ODistributedConfiguration(cfg);
  }
}
