{
  if (!liveUpdated)   return lastClusterPosition + 1;
  final long limit=database.getStorage().getClusterDataRange(current.clusterId)[1] + 1;
  if (rangeTo > -1)   return Math.min(rangeTo,limit);
  return limit;
}
