{
  final ODatabaseDocumentTx rawGraph=this.graph.getRawGraph();
  return new OrientElementIterator<T>(this.graph,new ORecordIteratorClass<ORecord>(rawGraph,(ODatabaseRecordAbstract)rawGraph.getUnderlying(),elementClass,polymorphic));
}
