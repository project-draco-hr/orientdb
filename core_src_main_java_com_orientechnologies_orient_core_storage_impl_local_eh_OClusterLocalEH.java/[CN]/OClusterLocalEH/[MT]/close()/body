{
  acquireExclusiveLock();
  try {
    for (    OEHFileMetadata metadata : files)     if (metadata.getFile().isOpen())     metadata.getFile().close();
  }
  finally {
    releaseExclusiveLock();
  }
}
