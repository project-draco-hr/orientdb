{
  if (iMode == EXECUTION_MODE.ASYNCHRONOUS && callback != null)   task.setExecutionCallback(callback);
  hazelcastInstance.getExecutorService().execute(task);
  if (iMode == EXECUTION_MODE.SYNCHRONOUS)   return task.get();
  return null;
}
