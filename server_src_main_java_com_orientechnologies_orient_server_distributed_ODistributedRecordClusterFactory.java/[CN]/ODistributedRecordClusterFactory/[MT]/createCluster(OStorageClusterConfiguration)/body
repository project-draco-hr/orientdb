{
  if (iConfig instanceof OStoragePhysicalClusterConfiguration)   return new ODistributedRecordClusterLocal();
 else   if (iConfig instanceof OStorageMemoryClusterConfiguration)   return new ODistributedRecordClusterMemory();
 else   return super.createCluster(iConfig);
}
