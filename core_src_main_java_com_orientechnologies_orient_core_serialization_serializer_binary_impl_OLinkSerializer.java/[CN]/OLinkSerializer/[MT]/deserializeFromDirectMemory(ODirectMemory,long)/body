{
  int clusterId=OShortSerializer.INSTANCE.deserializeFromDirectMemory(memory,pointer);
  OClusterPosition clusterPosition=OClusterPositionFactory.INSTANCE.fromStream(memory.get(pointer + OShortSerializer.SHORT_SIZE,CLUSTER_POS_SIZE));
  return new ORecordId(clusterId,clusterPosition);
}
