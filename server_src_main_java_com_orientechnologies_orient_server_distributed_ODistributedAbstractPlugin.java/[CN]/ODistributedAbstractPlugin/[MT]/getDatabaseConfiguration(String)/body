{
synchronized (cachedDatabaseConfiguration) {
    ODocument cfg=cachedDatabaseConfiguration.get(iDatabaseName);
    if (cfg == null)     cfg=cachedDatabaseConfiguration.get("*");
    return new ODistributedConfiguration(cfg);
  }
}
