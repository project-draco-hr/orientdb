{
  if (!database.getClusterNames().contains("Animal"))   database.addCluster("Animal",OStorage.CLUSTER_TYPE.PHYSICAL);
  if (!database.getClusterNames().contains("Vaccinate"))   database.addCluster("Vaccinate",OStorage.CLUSTER_TYPE.PHYSICAL);
}
