{
  try {
    OStorageRemoteThread a=new OStorageRemoteThread(delegate);
    a.pushSession();
    delegate.reopenRemoteDatabase();
    a.popSession();
    return a;
  }
 catch (  IOException e) {
    e.printStackTrace();
  }
  return null;
}
