{
  if (iRecord == null)   throw new OCommandExecutionException("Context variable '$" + name + "' cannot be resolved because record is null");
  if (iContext == null)   throw new OCommandExecutionException("Context variable '$" + name + "' cannot be resolved because context is null");
  return transformValue(iRecord,iContext.getVariable(name));
}
