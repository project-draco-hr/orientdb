{
  Constructor<?> defaultConstructor=null;
  for (  Constructor<?> c : iClass.getDeclaredConstructors()) {
    if (c.getParameterTypes().length == 0) {
      defaultConstructor=c;
      break;
    }
  }
  if (defaultConstructor == null)   throw new IllegalArgumentException("Can't create an object of class '" + iClass.getName() + "' because has no default constructor. Please define the method: "+ iClass.getSimpleName()+ "()");
  if (!defaultConstructor.isAccessible())   defaultConstructor.setAccessible(true);
  return defaultConstructor.newInstance();
}
