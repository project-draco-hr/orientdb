{
  clearLock.writeLock().lock();
  try {
    for (    OCachePointer pointer : content.values())     pointer.decrementReferrer();
    content.clear();
  }
  finally {
    clearLock.writeLock().unlock();
  }
}
