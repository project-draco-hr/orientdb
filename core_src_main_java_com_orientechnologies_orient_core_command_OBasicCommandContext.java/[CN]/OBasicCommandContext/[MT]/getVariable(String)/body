{
  Object result=null;
  if (inherited != null && !inherited.isEmpty())   for (  OCommandContext in : inherited) {
    result=in.getVariable(iName);
    if (result != null)     break;
  }
  if (variables != null && variables.containsKey(iName))   result=variables.get(iName);
  return result;
}
