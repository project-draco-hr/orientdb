{
  lock.acquireSharedLock();
  try {
    if (iDataId < 0 || iDataId > data.size() - 1)     throw new IllegalArgumentException("Invalid data segment id " + iDataId + ". Range is 0-"+ (data.size() - 1));
    return data.get(iDataId);
  }
  finally {
    lock.releaseSharedLock();
  }
}
