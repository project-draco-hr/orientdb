{
  final StringBuilder buffer=new StringBuilder();
  List<String> path=memory.peek().getPath();
  for (  String status : path) {
    if (buffer.length() > 0 && !status.startsWith("["))     buffer.append('.');
    buffer.append(status);
  }
  return buffer.toString();
}
