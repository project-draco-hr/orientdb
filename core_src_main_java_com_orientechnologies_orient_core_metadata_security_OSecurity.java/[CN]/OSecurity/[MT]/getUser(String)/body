{
  List<ODocument> result=database.command(new OSQLSynchQuery<ODocument>("select from OUser where name = '" + iUserName + "'")).execute();
  if (result != null && result.size() > 0)   return new OUser(result.get(0));
  return null;
}
