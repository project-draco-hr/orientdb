{
  if (this == obj)   return true;
  if (obj == null)   return false;
  if (getClass() == obj.getClass()) {
    final ORecordAbstract<?> other=(ORecordAbstract<?>)obj;
    if (_recordId == null) {
      if (other._recordId != null)       return false;
    }
 else     if (!_recordId.equals(other._recordId))     return _recordId.isValid();
  }
 else   if (obj instanceof ORID) {
    final ORID other=(ORID)obj;
    if (_recordId == null) {
      if (other != null)       return false;
    }
 else     if (!_recordId.equals(other))     return _recordId.isValid();
  }
 else   return false;
  return true;
}
