{
  if (input == null)   return null;
  if (cmd == null)   cmd=new OCommandExecutorScript().parse(new OCommandScript(language,code));
  params.put("record",input);
  return cmd.executeInContext(iContext,params);
}
