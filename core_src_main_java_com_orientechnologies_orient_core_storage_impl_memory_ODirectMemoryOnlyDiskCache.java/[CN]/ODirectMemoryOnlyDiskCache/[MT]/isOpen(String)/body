{
  metadataLock.lock();
  try {
    Integer result=fileNameIdMap.get(fileName);
    if (result == null)     return null;
    return composeFileId(id,result);
  }
  finally {
    metadataLock.unlock();
  }
}
