{
  if (url.startsWith("remote"))   new OServerAdmin(url).connect().createDatabase("admin","admin",STORAGE_MODE).close();
 else {
    database=new ODatabaseRecordFlat(url);
    database.create(STORAGE_MODE);
    database.close();
  }
}
