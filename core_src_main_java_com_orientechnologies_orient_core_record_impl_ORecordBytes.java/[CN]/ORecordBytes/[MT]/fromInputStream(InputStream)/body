{
  ByteArrayOutputStream out=new ByteArrayOutputStream();
  try {
    while (in.available() > 0) {
      out.write(in.read());
    }
    out.flush();
    _source=out.toByteArray();
  }
  finally {
    out.close();
  }
}
