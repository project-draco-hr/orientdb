{
  final long start=System.currentTimeMillis();
  for (int i=0; i < 1000000000; ++i)   Orient.instance().getMemoryWatchDog().getUsedHeapMemory();
  System.out.println("Elapsed " + (System.currentTimeMillis() - start) + "ms");
}
