{
  final Collection<OServerPlugin> handlers=iServer.getPlugins();
  if (handlers != null)   for (  OServerPlugin handler : handlers) {
    handler.onBeforeClientRequest(connection,iRequestType);
  }
}
