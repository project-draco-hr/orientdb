{
  if (status != STATUS.OPEN)   return;
  for (  ODatabaseLifecycleListener listener : listeners)   try {
    listener.onClose(this);
  }
 catch (  Throwable t) {
  }
  if (storage != null)   storage.removeUser();
  status=STATUS.CLOSED;
}
