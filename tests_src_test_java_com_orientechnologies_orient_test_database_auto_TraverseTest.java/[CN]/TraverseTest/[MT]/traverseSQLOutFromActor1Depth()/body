{
  List<ODocument> result1=database.command(new OSQLSynchQuery<ODocument>("traverse out from " + tomCruise.getIdentity() + " where $depth <= 1")).execute();
  Assert.assertTrue(result1.size() != 0);
  for (  ODocument d : result1) {
  }
}
