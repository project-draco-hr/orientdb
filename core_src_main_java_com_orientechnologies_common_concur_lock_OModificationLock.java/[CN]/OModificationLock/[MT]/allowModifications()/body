{
  final int currentVetos=vetos.decrementAndGet();
  if (currentVetos < 0)   throw new IllegalStateException("Bad state of modification lock. " + "Modifications were prohibited less times than they will be allowed.");
  for (  Thread thread : waiters)   LockSupport.unpark(thread);
}
