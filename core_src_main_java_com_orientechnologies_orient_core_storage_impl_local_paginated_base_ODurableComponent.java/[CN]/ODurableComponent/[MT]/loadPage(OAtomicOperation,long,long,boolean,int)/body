{
  if (atomicOperation == null)   return readCache.load(fileId,pageIndex,checkPinnedPages,writeCache,prefetchPages);
  return atomicOperation.loadPage(fileId,pageIndex,checkPinnedPages,prefetchPages);
}
