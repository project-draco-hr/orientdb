{
  if (iRecord != null) {
    final ODocument doc=iRecord.getRecord();
    if (doc != null && doc.getImmutableSchemaClass() != null && doc.getImmutableSchemaClass().isSubClassOf(OrientEdgeType.CLASS_NAME))     doc.delete();
  }
}
