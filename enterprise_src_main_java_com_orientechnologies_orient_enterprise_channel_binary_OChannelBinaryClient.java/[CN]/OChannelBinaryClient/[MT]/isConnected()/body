{
  if (socket != null && socket.isConnected() && !socket.isInputShutdown() && !socket.isOutputShutdown()) {
    try {
      out.flush();
      return true;
    }
 catch (    IOException e) {
    }
  }
  return false;
}
