{
  currentTx.commit();
  for (  ODatabaseLifecycleListener listener : underlying.getListeners())   try {
    listener.onTxCommit(underlying);
  }
 catch (  Throwable t) {
  }
  setDefaultTransactionMode();
  return this;
}
