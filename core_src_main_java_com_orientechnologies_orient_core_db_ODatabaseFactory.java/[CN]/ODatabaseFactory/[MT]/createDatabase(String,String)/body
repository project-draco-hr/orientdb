{
  if (iType.equals("graph"))   return new ODatabaseDocumentTx(url){
    @Override public <THISDB extends ODatabase>THISDB create(){
      final THISDB db=super.create();
      checkSchema((ODatabase<?>)db);
      return db;
    }
    @Override public <THISDB extends ODatabase>THISDB open(    final String iUserName,    final String iUserPassword){
      final THISDB db=super.open(iUserName,iUserPassword);
      checkSchema((ODatabase<?>)db);
      return db;
    }
    @Override public <THISDB extends ODatabase>THISDB open(    final OToken iToken){
      final THISDB db=super.open(iToken);
      checkSchema((ODatabase<?>)db);
      return db;
    }
  }
;
  return new ODatabaseDocumentTx(url);
}
