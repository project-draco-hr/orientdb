{
  final Map<OMVRBTree<?,?>,Object[]> map=get();
  Object[] value=map.get(iTree);
  if (value == null) {
    value=new Object[]{null,null};
    map.put(iTree,value);
  }
  value[0]=iKey;
  value[1]=iValue;
  return (RET)iValue;
}
