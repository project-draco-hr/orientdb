{
  if (url.startsWith(OEngineRemote.NAME))   new OServerAdmin(url).connect().createDatabase("admin","admin","local").close();
 else {
    database=new ODatabaseFlat(url);
    database.create();
    database.close();
  }
}
