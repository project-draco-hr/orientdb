{
  beginRange=iBegin;
  endRange=iEnd;
  if (currentRecord != null && outsideOfTheRange(currentRecord.getIdentity())) {
    currentRecord=null;
  }
  updateClusterRange();
  begin();
  return this;
}
