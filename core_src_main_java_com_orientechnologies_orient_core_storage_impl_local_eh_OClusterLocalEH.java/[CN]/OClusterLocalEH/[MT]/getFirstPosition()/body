{
  acquireSharedLock();
  try {
    final OHashIndexBucket.Entry<OClusterPosition,OPhysicalPosition> entry=localHashTable.firstEntry();
    if (entry == null)     return OClusterPosition.INVALID_POSITION;
    return entry.key;
  }
  finally {
    releaseSharedLock();
  }
}
