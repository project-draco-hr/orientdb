{
  final OrientBaseGraph graph=getGraph();
  if (graph == null)   throw new IllegalStateException("There is no active graph instance for current element. Please either open connection to your storage, or use detach/attach methods instead.");
  return graph;
}
