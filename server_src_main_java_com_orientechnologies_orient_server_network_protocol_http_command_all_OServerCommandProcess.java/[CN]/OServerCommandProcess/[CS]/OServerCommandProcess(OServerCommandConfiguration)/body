{
  for (  OServerEntryConfiguration cfg : iConfig.parameters) {
    if ("path".equalsIgnoreCase(cfg.name))     path=cfg.value;
 else     if ("extension".equalsIgnoreCase(cfg.name))     extension=cfg.value;
  }
  OProcessorManager.getInstance().register("configurable",new OConfigurableProcessor());
}
