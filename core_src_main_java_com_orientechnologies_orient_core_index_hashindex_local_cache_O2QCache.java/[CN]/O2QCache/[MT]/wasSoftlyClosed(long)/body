{
synchronized (syncObject) {
    OMultiFileSegment multiFileSegment=files.get(fileId);
    if (multiFileSegment == null)     return false;
    return multiFileSegment.wasSoftlyClosedAtPreviousTime();
  }
}
