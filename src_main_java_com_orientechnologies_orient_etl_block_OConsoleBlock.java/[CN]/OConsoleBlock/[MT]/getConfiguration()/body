{
  return new ODocument().fromJSON("{parameters:[" + getCommonConfigurationParameters() + "{file:{optional:true,description:'Input filename with commands to execute'}}"+ "{commands:{optional:true,description:'Commands to execute in sequence as an array of strings'}}"+ "]}");
}
