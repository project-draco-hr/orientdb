{
  acquireSharedLock();
  try {
    final Collection<OIndexInternal<?>> rawResult=indexes.values();
    final List<OIndex<?>> result=new ArrayList<OIndex<?>>(rawResult.size());
    for (    final OIndexInternal<?> index : rawResult)     result.add(preProcessBeforeReturn(index));
    return result;
  }
  finally {
    releaseSharedLock();
  }
}
