{
  if (ppos.dataSegmentId > 0)   return false;
  lock.acquireSharedLock();
  try {
    if (ppos.dataChunkPosition >= data.count())     return false;
  }
  finally {
    lock.releaseSharedLock();
  }
  return true;
}
