{
  super.sendShutdown();
  try {
    channel.socket.close();
  }
 catch (  IOException e) {
  }
  if (OLogManager.instance().isDebugEnabled())   OLogManager.instance().debug(this,"Connection shutdowned");
}
