{
  checkOpeness();
  if (iClusterId < -1) {
    int index=getLogicalClusterIndex(iClusterId);
    return logicalClusters.get(index).getName();
  }
  for (  OCluster cluster : physicalClusters)   if (cluster.getId() == iClusterId)   return cluster.getName();
  return null;
}
