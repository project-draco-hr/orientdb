{
  for (  OOrientListener l : listeners)   l.onStorageRegistered(iStorage);
  acquireExclusiveLock();
  try {
    if (!storages.containsKey(iStorage.getName()))     storages.put(iStorage.getURL(),iStorage);
  }
  finally {
    releaseExclusiveLock();
  }
}
