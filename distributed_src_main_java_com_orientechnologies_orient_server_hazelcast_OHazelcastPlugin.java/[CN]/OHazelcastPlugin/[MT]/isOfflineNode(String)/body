{
synchronized (pendingAlignments) {
    if (pendingAlignments.containsKey(iNodeId))     return true;
  }
  final ODocument cfg=getNodeConfiguration(iNodeId);
  return cfg == null || !cfg.field("status").equals("online");
}
