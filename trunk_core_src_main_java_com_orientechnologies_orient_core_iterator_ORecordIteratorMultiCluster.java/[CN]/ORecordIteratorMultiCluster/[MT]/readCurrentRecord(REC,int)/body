{
  if (limit > -1 && browsedRecords >= limit)   return null;
  currentClusterPosition+=iMovement;
  if (lowLevelDatabase.executeReadRecord(clusterIds[currentClusterIdx],currentClusterPosition,iRecord) != null)   browsedRecords++;
  return iRecord;
}
