{
  checkSecurity(OUser.CLASS + "." + iClassName,OUser.READ);
  return new ORecordIteratorMultiCluster<ORecordVObject>(this,underlying,getMetadata().getSchema().getClass(iClassName).getClusterIds());
}
