{
  final String fullName=fileName + fileExtension;
synchronized (syncObject) {
    LRUEntry lruEntry=lruList.get(fullName,pageIndex);
    if (lruEntry != null)     lruEntry.managedExternally=false;
  }
}
