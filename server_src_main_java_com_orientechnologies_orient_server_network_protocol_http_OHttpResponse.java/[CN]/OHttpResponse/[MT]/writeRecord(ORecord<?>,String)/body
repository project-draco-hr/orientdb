{
  final String format=iFetchPlan != null ? JSON_FORMAT + ",fetchPlan:" + iFetchPlan : JSON_FORMAT;
  if (iRecord != null)   send(OHttpUtils.STATUS_OK_CODE,"OK",OHttpUtils.CONTENT_JSON,iRecord.toJSON(format),null);
}
