{
  checkTransaction();
  status=TXSTATUS.ROLLBACKING;
  database.getLevel1Cache().invalidate();
  for (  OTransactionRecordEntry v : recordEntries.values())   v.getRecord().unload();
  for (  OTransactionRecordEntry v : allEntries.values())   v.getRecord().unload();
  indexEntries.clear();
}
