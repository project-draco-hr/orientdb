{
  if (currentIndex >= size)   throw new NoSuchElementException("Iterator was reached last element");
  final OPhysicalPosition position=getEntry(currentIndex);
  currentIndex++;
  return position;
}
