{
  super(nodeSource,iDbName,iMode,iRid,iVersion);
  content=iContent;
  recordType=iRecordType;
  OLogManager.instance().debug(this,"DISTRIBUTED -> route CREATE RECORD in %s mode to %s %s{%s} v.%d",iMode,nodeSource,iDbName,iRid,iVersion);
}
