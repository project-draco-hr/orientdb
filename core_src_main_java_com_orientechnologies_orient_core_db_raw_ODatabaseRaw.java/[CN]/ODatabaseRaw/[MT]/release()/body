{
  final OStorageLocalAbstract storage;
  if (getStorage() instanceof OStorageLocalAbstract)   storage=((OStorageLocalAbstract)getStorage());
 else {
    OLogManager.instance().error(this,"We can not freeze non local storage.");
    return;
  }
  storage.release();
}
