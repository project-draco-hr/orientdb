{
  final int index=index(value.hashCode());
  if (useSpinLock)   throw new UnsupportedOperationException();
  final ReadWriteLock rwLock=locks[index];
  final Lock lock=rwLock.writeLock();
  if (lock.tryLock(timeOut,TimeUnit.MILLISECONDS))   return lock;
  return null;
}
