{
  OSBTreeBonsai<OIdentifiable,Boolean> tree=loadTree();
  try {
    final int size=addValues.size();
    if (tree != null)     return size + (int)tree.size() - removedValues.size();
    return size;
  }
  finally {
    releaseTree();
  }
}
