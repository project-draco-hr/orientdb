{
  if (iRequest.authorization == null)   throw new OSecurityAccessException("No user and password received");
  return OSharedDocumentDatabase.acquireDatabase(iDatabaseURL + ":" + iRequest.authorization);
}
