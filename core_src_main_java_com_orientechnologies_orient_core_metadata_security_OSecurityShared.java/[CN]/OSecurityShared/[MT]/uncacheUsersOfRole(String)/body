{
  if (cachedUsers != null) {
    for (final Iterator<Map.Entry<String,OUser>> it=cachedUsers.entrySet().iterator(); it.hasNext(); ) {
      if (it.next().getValue().hasRole(iName,true))       it.remove();
    }
  }
}
