{
  checkCurrentDatabase();
  currentRecord=((ODatabaseRecordAbstract)currentDatabase.getUnderlying()).executeReadRecord(new ORecordId(iRecordId),null,iFetchPlan,true);
  displayRecord(null);
  out.println("OK");
}
