{
  if (t == null)   return null;
  final int index=t.getTree().getPageIndex();
  if (index <= 0) {
    t=predecessor(t);
    t.tree.pageIndex=t.size - 1;
  }
 else   t.tree.pageIndex=index - 1;
  return t;
}
