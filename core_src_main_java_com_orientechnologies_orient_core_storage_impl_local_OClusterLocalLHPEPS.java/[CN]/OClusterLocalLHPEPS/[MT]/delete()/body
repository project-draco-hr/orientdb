{
  acquireExclusiveLock();
  try {
    truncate();
    for (    OFile f : fileSegment.files)     f.delete();
    fileSegment.files=null;
    overflowSegment.delete();
    overflowStatistic.delete();
  }
  finally {
    releaseExclusiveLock();
  }
}
