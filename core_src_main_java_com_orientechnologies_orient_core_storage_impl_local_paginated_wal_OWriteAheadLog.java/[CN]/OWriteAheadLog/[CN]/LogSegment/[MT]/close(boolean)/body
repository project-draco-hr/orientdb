{
  if (!closed) {
    stopFlush(flush);
    rndFile.close();
    closed=true;
    if (!pagesCache.isEmpty()) {
      for (      OWALPage page : pagesCache)       page.getPagePointer().free();
    }
    currentPage=null;
  }
}
