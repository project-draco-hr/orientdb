{
  OClass currentClass=this;
  do {
    if (currentClass.properties != null)     for (    OProperty prop : currentClass.properties.values())     if (prop.getId() == iIndex)     return prop;
    currentClass=currentClass.getSuperClass();
  }
 while (currentClass != null);
  return null;
}
