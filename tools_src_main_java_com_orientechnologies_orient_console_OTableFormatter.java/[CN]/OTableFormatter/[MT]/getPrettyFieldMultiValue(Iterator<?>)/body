{
  String value="";
  while (iterator.hasNext()) {
    value+="," + getPrettyFieldValue(iterator.next());
  }
  if (value.length() > 0) {
    value=value.substring(1);
  }
  return "[" + value + "]";
}
