{
  try {
    OrientEdge e=graph.addEdge(null,new OrientVertex(new ORecordId("9:9999")),new OrientVertex(new ORecordId("9:99999")),"E");
    Assert.assertTrue(false);
  }
 catch (  IllegalArgumentException e) {
    Assert.assertTrue(true);
  }
}
