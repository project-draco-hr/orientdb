{
  makeActive();
  if (isAutoStartTx() && database.getTransaction().isActive())   throw new OTransactionException("A mixture of auto started and manually started transactions is not allowed. " + "Disable auto transactions for the graph before starting a manual transaction.");
  database.begin();
  database.getTransaction().setUsingLog(settings.isUseLog());
}
