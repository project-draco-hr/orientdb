{
  long size=0;
  lock.acquireSharedLock();
  try {
    size+=data.getSize();
    for (    OClusterMemory c : clusters)     size+=c.getSize();
  }
  finally {
    lock.releaseSharedLock();
  }
  return size;
}
