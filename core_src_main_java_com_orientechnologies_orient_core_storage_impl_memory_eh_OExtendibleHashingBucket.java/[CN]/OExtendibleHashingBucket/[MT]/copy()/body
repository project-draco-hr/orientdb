{
  final OExtendibleHashingBucket bucket=new OExtendibleHashingBucket(depth,maxBucketSize,Arrays.copyOf(splitHistory,splitHistory.length));
  bucket.keys=Arrays.copyOf(this.keys,this.keys.length);
  bucket.values=Arrays.copyOf(this.values,this.values.length);
  bucket.size=size;
  bucket.nextRemovedBucketPair=nextRemovedBucketPair;
  return bucket;
}
