{
  begin();
  Object current=null;
  for (  OExtractor ex : extractors) {
    ex.extract(current);
  }
  while (extractor.hasNext())   current=extractor.next();
  loader.load(transformer.transform(extractor.next()));
  end();
}
