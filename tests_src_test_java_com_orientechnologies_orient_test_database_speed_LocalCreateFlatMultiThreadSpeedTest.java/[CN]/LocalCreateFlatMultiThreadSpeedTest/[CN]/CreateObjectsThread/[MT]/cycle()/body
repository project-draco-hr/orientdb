{
  record.reset();
  record.value("id:" + data.getCyclesDone() + ",name:'Luca',surname:'Garulli',salary:"+ (data.getCyclesDone() + 3000)+ ".00").save("flat");
  if (data.getCyclesDone() == data.getCycles() - 1)   database.commit();
}
