{
  if (iLeft == getLeft())   return;
  markDirty();
  this.left=(OTreeMapEntryPersistent<K,V>)iLeft;
  this.leftRid=iLeft == null ? ORecordId.EMPTY_RECORD_ID : left.record.getIdentity();
  if (iLeft != null && iLeft.getParent() != this)   iLeft.setParent(this);
}
