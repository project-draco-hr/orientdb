{
  if (isRebuiding())   return;
  acquireExclusiveLock();
  try {
    map.commitChanges(true);
    map.unload();
    Orient.instance().getMemoryWatchDog().removeListener(watchDog);
  }
  finally {
    releaseExclusiveLock();
  }
}
