{
  final long timer=Orient.instance().getProfiler().startChrono();
  try {
    if (((OMVRBTreeMapProvider<K,V>)treeDataProvider).valueSerializer instanceof OBinarySerializer)     toStreamUsingBinarySerializer();
 else     toStreamUsingBinaryStreamSerializer();
    record.fromStream(buffer);
    return buffer;
  }
 catch (  IOException e) {
    throw new OSerializationException("Cannot marshall RB+Tree node",e);
  }
 finally {
    Orient.instance().getProfiler().stopChrono("OMVRBTreeMapEntry.toStream",timer);
  }
}
