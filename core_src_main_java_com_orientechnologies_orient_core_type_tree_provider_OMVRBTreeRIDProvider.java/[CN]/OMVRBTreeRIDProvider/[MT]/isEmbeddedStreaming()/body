{
  if (embeddedStreaming && !marshalling) {
    if (getDatabase().getTransaction().isActive())     return true;
    if (binaryThreshold > 0 && getSize() > binaryThreshold && tree != null) {
      tree.setDirtyOwner();
      setEmbeddedStreaming(false);
    }
  }
  return embeddedStreaming;
}
