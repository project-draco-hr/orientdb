{
  OStorage storage=iDatabase.getStorage();
  if (storage.getType().equals(OEngineLocal.NAME) || storage.getType().equals(OEngineLocalPaginated.NAME))   return createSBTreeIndex(indexType);
  return createMRBTreeIndex(indexType);
}
