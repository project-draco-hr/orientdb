{
  if (maxSize == 0)   return;
  final boolean locked=acquireExclusiveLock();
  try {
    for (    Object id : iRecords)     cache.remove(id.toString());
  }
  finally {
    releaseExclusiveLock(locked);
  }
}
