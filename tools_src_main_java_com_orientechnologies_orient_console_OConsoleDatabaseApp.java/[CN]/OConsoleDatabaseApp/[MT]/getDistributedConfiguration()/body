{
  if (currentDatabase != null) {
    final OStorage stg=currentDatabase.getStorage();
    if (stg instanceof OStorageRemoteThread)     return ((OStorageRemoteThread)stg).getClusterConfiguration();
  }
  return null;
}
