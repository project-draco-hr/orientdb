{
  if (limit > -1 && browsedRecords >= limit)   return false;
  if (currentClusterIdx < clusterIds.length - 1)   return true;
  if (liveUpdated)   return currentClusterPosition < database.countClusterElements(lastClusterId) - 1;
  return currentClusterPosition < lastClusterSize - 1;
}
