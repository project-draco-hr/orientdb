{
  try {
    lock.acquireExclusiveLock();
    OLogManager.instance().warn(this,"Removed server node %s:%d from distributed cluster",iNode.networkAddress,iNode.networkPort);
    nodes.remove(iNode.toString());
  }
  finally {
    lock.releaseExclusiveLock();
  }
}
