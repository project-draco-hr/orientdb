{
  Object result;
  if (indexResult instanceof Collection)   result=(Collection<?>)indexResult;
 else   if (indexResult == null)   result=Collections.emptyList();
 else   if (indexResult instanceof OIdentifiable)   result=Collections.singletonList((OIdentifiable)indexResult);
 else   result=indexResult;
  return result;
}
