{
  if (!iVertex.getSchemaClass().isSubClassOf(vertexBaseClass))   throw new IllegalArgumentException("The document received is not a vertex");
  return filterEdgesByProperties((ORecordLazySet)iVertex.field(VERTEX_FIELD_OUT_EDGES),iProperties);
}
