{
  try {
    acquireSharedLock();
    final int[] pos=getRelativePosition(iPosition);
    final OFile file=files[pos[0]];
    return file.readInt(pos[1]);
  }
  finally {
    releaseSharedLock();
  }
}
