{
  if (parts.length < 2) {
    onInvalidAttributeDefinition(attDef);
  }
  String trimmed=parts[1].trim();
  if (trimmed.length() == 0) {
    onInvalidAttributeDefinition(attDef);
  }
  return trimmed;
}
