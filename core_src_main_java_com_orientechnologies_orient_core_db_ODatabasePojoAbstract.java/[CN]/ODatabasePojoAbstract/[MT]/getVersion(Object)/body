{
  checkOpeness();
  final ODocument record=getRecordByUserObject(iPojo,true);
  if (record == null)   throw new OObjectNotManagedException("The object " + iPojo + " is not managed by the current database");
  return record.getVersion();
}
