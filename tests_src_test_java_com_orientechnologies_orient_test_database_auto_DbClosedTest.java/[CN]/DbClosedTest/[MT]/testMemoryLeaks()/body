{
  System.gc();
  Thread.sleep(1000);
  Assert.assertEquals(0,ODirectMemoryPointerFactory.instance().getDetectedLeaks(),0,"Memory leaks are detected. " + "For more details check console output.");
}
