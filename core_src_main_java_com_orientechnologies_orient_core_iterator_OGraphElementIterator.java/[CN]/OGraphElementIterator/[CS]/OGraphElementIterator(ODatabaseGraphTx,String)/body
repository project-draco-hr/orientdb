{
  underlying=new ORecordIteratorMultiCluster<ODocument>((ODatabaseRecord<ODocument>)iDatabase.getUnderlying(),(ODatabaseRecordAbstract<ODocument>)iDatabase.getUnderlying(),iDatabase.getMetadata().getSchema().getClass(iClassName).getClusterIds());
}
