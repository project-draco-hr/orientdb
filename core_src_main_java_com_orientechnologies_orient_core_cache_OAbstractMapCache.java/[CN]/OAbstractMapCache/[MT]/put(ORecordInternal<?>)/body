{
  if (!isEnabled())   return null;
  if (!Orient.instance().getMemoryWatchDog().isMemoryAvailable())   return null;
  lock.acquireExclusiveLock();
  try {
    return cache.put(record.getIdentity(),record);
  }
  finally {
    lock.releaseExclusiveLock();
  }
}
