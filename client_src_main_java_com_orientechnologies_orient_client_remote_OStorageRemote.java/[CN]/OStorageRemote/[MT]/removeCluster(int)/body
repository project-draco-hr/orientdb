{
  checkConnection();
  do {
    boolean locked=acquireExclusiveLock();
    try {
      writeCommand(OChannelBinaryProtocol.DATACLUSTER_REMOVE);
      network.writeShort((short)iClusterId);
      network.flush();
      readStatus();
      if (network.readByte() == '1') {
        for (        Entry<String,Integer> entry : clustersIds.entrySet())         if (entry.getValue() != null && entry.getValue().intValue() == iClusterId) {
          clustersIds.remove(entry.getKey());
          clustersTypes.remove(entry.getKey());
          break;
        }
        return true;
      }
      return false;
    }
 catch (    Exception e) {
      if (handleException("Error on removing of cluster",e))       break;
    }
 finally {
      releaseExclusiveLock(locked);
    }
  }
 while (true);
  return false;
}
