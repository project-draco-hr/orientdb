{
  database.open("admin","admin");
  List<ODocument> result=database.command(new OSQLSynchQuery<ODocument>("select union(outEdges, inEdges) as name from OGraphVertex")).execute();
  Assert.assertTrue(result.size() > 1);
  database.close();
}
