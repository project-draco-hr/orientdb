{
  this.serverName=serverName;
  this.delay=DOCUMENT_WRITE_TIMEOUT;
  OLogManager.instance().info(this,"Thread [%s-%d] delayer created with " + delay + "ms of delay",serverName,Thread.currentThread().getId());
}
