{
  final List<OTransactionRecordEntry> result=new ArrayList<OTransactionRecordEntry>();
  if (iClassName == null || iClassName.length() == 0)   for (  OTransactionRecordEntry entry : recordEntries.values()) {
    result.add(entry);
  }
 else   for (  OTransactionRecordEntry entry : recordEntries.values()) {
    if (entry.getRecord() != null && entry.getRecord() instanceof ODocument && iClassName.equals(((ODocument)entry.getRecord()).getClassName()))     result.add(entry);
  }
  return result;
}
