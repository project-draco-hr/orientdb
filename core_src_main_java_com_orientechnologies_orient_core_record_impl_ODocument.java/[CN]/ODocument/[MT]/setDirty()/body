{
  if (_owners != null) {
    ORecordElement e;
    for (    WeakReference<ORecordElement> o : _owners) {
      e=o.get();
      if (e != null)       e.setDirty();
    }
  }
  checkForFields();
  return super.setDirty();
}
