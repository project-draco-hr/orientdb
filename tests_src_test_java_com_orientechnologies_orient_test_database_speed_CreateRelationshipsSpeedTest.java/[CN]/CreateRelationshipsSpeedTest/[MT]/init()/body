{
  if (!database.getClusterNames().contains("Animal"))   database.getStorage().addCluster("Animal");
  if (!database.getClusterNames().contains("Vaccinate"))   database.getStorage().addCluster("Vaccinate");
}
