{
  if (_listeners != null)   for (  final ORecordListener listener : _listeners)   if (listener != null) {
    if (iEvent.equals(ORecordListener.EVENT.IDENTITY_CHANGED))     listener.onEvent(this,iEvent,_oldRid,getIdentity().copy());
 else     listener.onEvent(this,iEvent);
  }
}
