{
switch (iStatus) {
case CLEAR:
    delegate.clear();
  break;
case PUT:
delegate.put(iKey,iValue);
break;
case REMOVE:
if (iKey == null) throw new OIndexException("Key value should be provided to remove index entry.");
 else delegate.remove(iKey,iValue);
break;
}
}
