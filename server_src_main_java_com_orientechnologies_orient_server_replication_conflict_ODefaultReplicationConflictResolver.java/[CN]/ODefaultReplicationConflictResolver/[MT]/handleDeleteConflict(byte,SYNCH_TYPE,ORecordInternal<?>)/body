{
  OLogManager.instance().warn(this,"-> %s (%s mode) CONFLICT %s record %s (cannot be deleted on other node)",this,iRequestType,ORecordOperation.getName(iOperation),iRecord.getIdentity());
  final ODocument doc=createConflictDocument(iOperation,iRecord);
  doc.save();
}
