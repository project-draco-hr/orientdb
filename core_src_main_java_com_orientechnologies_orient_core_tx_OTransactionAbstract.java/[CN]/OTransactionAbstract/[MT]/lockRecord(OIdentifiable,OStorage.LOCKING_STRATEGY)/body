{
  final OStorage stg=database.getStorage();
  if (!(stg.getUnderlying() instanceof OStorageEmbedded))   throw new OLockException("Cannot lock record across remote connections");
  final ORID rid=iRecord.getIdentity();
  if (iLockingStrategy == OStorage.LOCKING_STRATEGY.KEEP_EXCLUSIVE_LOCK)   ((OStorageEmbedded)stg).acquireWriteLock(rid);
 else   ((OStorageEmbedded)stg).acquireReadLock(rid);
  locks.put(rid,iLockingStrategy);
  return this;
}
