{
  acquireExclusiveLock();
  try {
    fileSegment.open();
    overflowSegment.open();
    overflowStatistic.open();
    deserializeState();
    clearCache();
  }
  finally {
    releaseExclusiveLock();
  }
}
