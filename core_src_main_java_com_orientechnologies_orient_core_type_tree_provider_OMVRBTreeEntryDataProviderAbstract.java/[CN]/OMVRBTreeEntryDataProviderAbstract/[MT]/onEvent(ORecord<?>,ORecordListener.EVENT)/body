{
  if (ORecordListener.EVENT.IDENTITY_CHANGED.equals(event))   if (identityChangedListener != null) {
    final OIdentityChangedListener listener=identityChangedListener.get();
    if (listener != null)     listener.onIdentityChanged(record.getIdentity());
  }
}
