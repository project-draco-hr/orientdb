{
  final int bufferLenght=buffer != null ? buffer.length : 0;
  iOutput.writeInt(bufferLenght);
  if (bufferLenght > 0)   iOutput.write(buffer);
  iOutput.writeInt(version);
  iOutput.write(recordType);
}
