{
  acquireExclusiveLock();
  try {
    writeCache.close(fileId,flush);
    if (nullPointerSupport)     writeCache.close(nullBucketFileId,flush);
  }
 catch (  IOException e) {
    throw new OSBTreeException("Error during close of index " + name,e);
  }
 finally {
    releaseExclusiveLock();
  }
}
