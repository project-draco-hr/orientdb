{
  int counter=0;
  for (int i=0; i < 1000000; i++) {
    for (    ODocument doc : database.browseClass("Account"))     if (doc != null)     counter++;
  }
  System.out.println(counter);
  if (database != null)   database.close();
}
