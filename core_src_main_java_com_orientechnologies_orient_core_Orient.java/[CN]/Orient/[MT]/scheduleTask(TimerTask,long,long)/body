{
  engineLock.readLock().lock();
  try {
    if (active)     timer.schedule(task,delay,period);
 else     OLogManager.instance().warn(this,"OrientDB engine is down. Task will not be scheduled.");
  }
  finally {
    engineLock.readLock().unlock();
  }
}
