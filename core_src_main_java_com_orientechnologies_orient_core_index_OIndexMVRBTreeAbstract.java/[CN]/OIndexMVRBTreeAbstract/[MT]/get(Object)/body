{
  acquireExclusiveLock();
  try {
    final ORecordLazySet values=map.get(iKey);
    if (values == null)     return ORecordLazySet.EMPTY_SET;
    return values;
  }
  finally {
    releaseExclusiveLock();
  }
}
