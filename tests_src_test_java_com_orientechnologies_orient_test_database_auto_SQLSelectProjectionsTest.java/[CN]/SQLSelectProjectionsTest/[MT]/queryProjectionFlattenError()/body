{
  database.open("admin","admin");
  try {
    database.command(new OSQLSynchQuery<ODocument>("SELECT FLATTEN( out ), in FROM V WHERE out TRAVERSE(1,1) (@class = 'E')")).execute();
  }
  finally {
    database.close();
  }
}
