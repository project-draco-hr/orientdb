{
  if (database == null)   throw new ODatabaseException("No database assigned to current record. Create it using the <DB>.newInstance()");
  OSerializationThreadLocal.INSTANCE.get().clear();
  database.save(this,iClusterName);
  return this;
}
