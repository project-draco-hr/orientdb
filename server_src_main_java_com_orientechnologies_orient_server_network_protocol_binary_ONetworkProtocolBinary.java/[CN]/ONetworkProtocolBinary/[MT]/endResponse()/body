{
  if (connection != null && connection.database != null && connection.database.getTransaction() != null)   connection.database.getTransaction().close();
  channel.flush();
  channel.releaseWriteLock();
}
