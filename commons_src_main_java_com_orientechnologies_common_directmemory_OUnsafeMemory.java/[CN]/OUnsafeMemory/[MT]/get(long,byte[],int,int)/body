{
  pointer+=arrayOffset;
  if (copyMemoryAllowed) {
    unsafe.copyMemory(null,pointer,array,arrayOffset + NATIVE_BYTE_ARRAY_OFFSET,length);
  }
 else {
    for (int i=arrayOffset; i < length + arrayOffset; i++)     array[i]=unsafe.getByte(pointer++);
  }
}
