{
  if (_fieldValues == null)   _fieldValues=_ordered ? new LinkedHashMap<String,T>() : new HashMap<String,T>();
  if (_status == STATUS.LOADED && (_fieldValues == null || size() == 0))   deserializeFields();
}
