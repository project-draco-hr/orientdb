{
  if (!entryPoints.isEmpty()) {
    final Entry<K,OMVRBTreeEntryPersistent<K,V>> closerNode=entryPoints.floorEntry(iKey);
    if (closerNode != null)     return closerNode.getValue();
  }
  return super.getBestEntryPoint(iKey);
}
