{
  if (debug)   OLogManager.instance().debug(this,"Writing bytes (4+%d=%d bytes): %s",iLength,iLength + 4,iContent);
  if (iContent == null) {
    out.writeInt(-1);
  }
 else {
    out.writeInt(iLength);
    out.write(iContent,0,iLength);
  }
  return this;
}
