{
  stateLock.lock();
  try {
    long acquireCount=state >>> ACQUIRED_OFFSET;
    if (acquireCount < 1)     return false;
    acquireCount++;
    state=acquireCount << ACQUIRED_OFFSET;
    return true;
  }
  finally {
    stateLock.unlock();
  }
}
