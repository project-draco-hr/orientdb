{
  Object result;
  if (isBlock(iValue))   result=delegate(iName,iManager,(ODocument)iValue,iContext,iOutput,iReadOnly);
 else {
    try {
      result=iManager.processFromFile(iName,iContext,iReadOnly);
    }
 catch (    Exception e) {
      throw new OProcessException("Error on processing '" + iName + "' field of '"+ getName()+ "' block",e);
    }
  }
  if ("last".equalsIgnoreCase(returnType))   returnValue=result;
 else   if ("list".equalsIgnoreCase(returnType) || "set".equalsIgnoreCase(returnType))   ((Collection<Object>)returnValue).add(result);
  return returnValue;
}
