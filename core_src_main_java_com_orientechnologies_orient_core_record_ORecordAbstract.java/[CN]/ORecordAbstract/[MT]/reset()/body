{
  _status=ORecordElement.STATUS.LOADED;
  _recordVersion.reset();
  _size=0;
  setDirty();
  if (_recordId != null)   _recordId.reset();
  invokeListenerEvent(ORecordListener.EVENT.RESET);
  return this;
}
