{
  final List<String> methods=new ArrayList<String>();
  for (  OSQLMethodFactory factory : CACHED_METHODS)   methods.addAll(factory.getMethodNames());
  return methods.toArray(new String[methods.size()]);
}
