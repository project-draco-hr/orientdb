{
  acquireExclusiveLock();
  try {
    if (ascOrder)     getEntriesMinorAscOrder(toKey,isInclusive,transformer,entriesResultListener);
 else     getEntriesMinorDescOrder(toKey,isInclusive,transformer,entriesResultListener);
  }
  finally {
    releaseExclusiveLock();
  }
}
