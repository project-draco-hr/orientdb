{
  acquireWriteLock();
  try {
    final int filledUpTo=getFilledUpTo();
    if (filledUpTo < iSizeToShrink)     iSizeToShrink=0;
    setFilledUpTo(filledUpTo - iSizeToShrink);
  }
  finally {
    releaseWriteLock();
  }
}
