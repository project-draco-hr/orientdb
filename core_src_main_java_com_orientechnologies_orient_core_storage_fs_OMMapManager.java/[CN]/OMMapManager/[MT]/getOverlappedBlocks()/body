{
  lock.readLock().lock();
  try {
    int count=0;
    for (    OFile f : bufferPoolPerFile.keySet()) {
      count+=getOverlappedBlocks(f);
    }
    return count;
  }
  finally {
    lock.readLock().unlock();
  }
}
