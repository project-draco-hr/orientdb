{
  final ODatabaseDocumentTx rawGraph=this.graph.getRawGraph();
  return new OrientElementIterator<T>(this.graph,new ORecordIteratorClass<ORecord>(rawGraph,(ODatabaseRecordTx)rawGraph.getUnderlying(),elementClass,polymorphic));
}
