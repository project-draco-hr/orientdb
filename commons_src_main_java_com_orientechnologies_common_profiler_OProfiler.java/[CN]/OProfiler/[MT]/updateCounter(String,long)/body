{
  if (iStatName == null || recordingFrom < 0)   return;
  acquireSharedLock();
  try {
    lastSnapshot.updateCounter(iStatName,iPlus);
    realTime.updateCounter(iStatName,iPlus);
  }
  finally {
    releaseSharedLock();
  }
}
