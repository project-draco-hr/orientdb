{
  OPhysicalPosition goodResult=(OPhysicalPosition)iGoodResponse.getPayload();
  return new OFixCreateRecordTask(rid,content,version,recordType,new ORecordId(rid.getClusterId(),goodResult.clusterPosition));
}
