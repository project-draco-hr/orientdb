{
  if (positionsToProcess.length > 0)   do {
    currentEntryPosition--;
  }
 while (currentEntryPosition > 0 && (iterateThroughTombstones || positionsToProcess[currentEntryPosition].recordVersion.isTombstone()));
}
