{
synchronized (configuration) {
    final List<String> serverList=getClusterConfiguration(iClusterName).field(SERVERS);
    if (serverList != null)     return serverList.contains(iServer);
    return false;
  }
}
