{
  if (signum > o.signum)   return 1;
 else   if (signum < o.signum)   return -1;
  if (signum == 0 && o.signum == 0)   return 0;
  final int result=compareChunks(chunks,o.chunks);
  if (signum < 0)   return -result;
  return result;
}
