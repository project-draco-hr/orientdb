{
  if (_clazz != null && getIdentity().isNew())   return save(getDatabase().getClusterNameById(_clazz.getClusterForNewInstance()),forceCreate);
  convertAllMultiValuesToTrackedVersions();
  validate();
  return (ODocument)super.save(forceCreate);
}
