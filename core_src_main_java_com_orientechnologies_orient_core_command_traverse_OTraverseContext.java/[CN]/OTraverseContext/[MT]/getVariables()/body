{
  final HashMap<String,Object> map=new HashMap<String,Object>();
  map.put("depth",depth);
  map.put("path",getPath());
  map.put("stack",stack);
  if (nestedStack != null)   map.putAll(nestedStack.getVariables());
  return map;
}
