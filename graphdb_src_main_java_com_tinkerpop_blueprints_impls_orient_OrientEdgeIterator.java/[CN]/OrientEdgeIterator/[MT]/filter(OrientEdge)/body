{
  if (targetVertex != null && !targetVertex.equals(iObject.getVertex(connection.getKey().opposite())))   return false;
  return this.sourceVertex.settings.isUseVertexFieldsForEdgeLabels() || iObject.isLabeled(labels);
}
