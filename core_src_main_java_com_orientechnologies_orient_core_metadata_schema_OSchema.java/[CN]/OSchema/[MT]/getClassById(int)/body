{
  if (iClassId == -1)   return null;
  OClass cls=classes.get(iClassId);
  if (cls == null)   throw new OSchemaException("Class #" + iClassId + " was not found in current database");
  return cls;
}
