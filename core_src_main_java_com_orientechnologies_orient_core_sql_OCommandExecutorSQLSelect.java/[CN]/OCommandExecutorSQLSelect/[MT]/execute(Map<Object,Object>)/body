{
  if (iArgs != null)   for (  Entry<Object,Object> arg : iArgs.entrySet())   context.setVariable(arg.getKey().toString(),arg.getValue());
  if (!optimizeExecution()) {
    fetchLimit=getQueryFetchLimit();
    executeSearch(iArgs);
    applyFlatten();
    handleNoTarget();
    applyOrderBy();
    applyLimitAndSkip();
  }
  return getResult();
}
