{
  final byte[] record=storage.readRecord(CONFIG_RID,null,null).buffer;
  if (record == null)   throw new OStorageException("Cannot load database's configuration. The database seems to be corrupted.");
  fromStream(record);
  return this;
}
