{
synchronized (this) {
    final OOperationLog log=localLogs.get(iDatabaseName);
    if (log == null)     localLogs.put(iDatabaseName,new OOperationLog(manager.getId(),iDatabaseName,true));
 else     log.reset();
  }
}
