{
  acquireExclusiveLock();
  try {
    final Set<ORecord<?>> recs=get(iKey);
    if (recs != null && !recs.isEmpty()) {
      if (recs.remove(value))       map.put(iKey,recs);
    }
    return this;
  }
  finally {
    releaseExclusiveLock();
  }
}
