{
  final StringBuilder builder=new StringBuilder();
  if (signum < 0)   builder.append("-");
  for (  int chunk : chunks)   builder.append(String.format("%1$08x",chunk));
  return builder.toString();
}
