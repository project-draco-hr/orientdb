{
  if (messageService.getDatabase(databaseName) != null)   updateCachedDatabaseConfiguration(databaseName,config,true,false);
  installNewDatabase(false,databaseName,config);
  dumpServersStatus();
}
