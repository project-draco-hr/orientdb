{
  networkPoolLock.lock();
  try {
    if (networkPool.isEmpty())     createNetworkConnection();
    for (int i=networkPool.size(); i < minPool; ++i)     createNetworkConnection();
    return networkPool.size();
  }
  finally {
    networkPoolLock.unlock();
  }
}
