{
  identity=indexRid;
  hashTable.load(indexName,indexDefinition != null ? indexDefinition.getTypes() : null,(OAbstractPaginatedStorage)getDatabase().getStorage().getUnderlying(),indexDefinition != null && !indexDefinition.isNullValuesIgnored());
  hashFunction.setValueSerializer(hashTable.getKeySerializer());
}
