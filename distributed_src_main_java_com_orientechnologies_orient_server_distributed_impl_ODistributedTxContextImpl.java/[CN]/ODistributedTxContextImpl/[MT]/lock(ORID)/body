{
  if (!rid.isPersistent())   rid=new ORecordId(rid.getClusterId(),-1l);
  if (db.lockRecord(rid,reqId,OGlobalConfiguration.DISTRIBUTED_ATOMIC_LOCK_TIMEOUT.getValueAsInteger()))   acquiredLocks.add(rid);
}
