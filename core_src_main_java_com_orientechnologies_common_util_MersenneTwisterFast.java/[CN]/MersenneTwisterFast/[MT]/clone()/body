{
  try {
    MersenneTwisterFast f=(MersenneTwisterFast)(super.clone());
    f.mt=(int[])(mt.clone());
    f.mag01=(int[])(mag01.clone());
    return f;
  }
 catch (  CloneNotSupportedException e) {
    throw OException.wrapException(new OSystemException("Can not clone " + MersenneTwisterFast.class.getSimpleName() + " instance"),e);
  }
}
