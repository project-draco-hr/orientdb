{
  ODocument polygon=new ODocument("Rectangle");
  polygon.field("coordinates",new ArrayList<Double>(){
{
      add(-45d);
      add(-30d);
      add(45d);
      add(30d);
    }
  }
);
  return polygon;
}
