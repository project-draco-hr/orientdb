{
  if (iValue == null || "null".equalsIgnoreCase(iValue))   storage.getConfiguration().removeProperty(iName);
 else   storage.getConfiguration().setProperty(iName,iValue);
  storage.getConfiguration().update();
}
