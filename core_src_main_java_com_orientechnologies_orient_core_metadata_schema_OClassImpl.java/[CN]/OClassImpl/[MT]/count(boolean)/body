{
  if (iPolymorphic)   return getDatabase().countClusterElements(readableCluster(getDatabase(),polymorphicClusterIds));
  return getDatabase().countClusterElements(readableCluster(getDatabase(),clusterIds));
}
