{
  int index=0;
  long longestOffset=offset;
  long temp=offset;
  while (temp > 0 && index < num) {
    if (!this.getOperationStatus(temp))     longestOffset=this.getPreviousOperation(temp);
    temp=this.getPreviousOperation(temp);
    index++;
  }
  return longestOffset;
}
