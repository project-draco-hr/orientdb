{
  if (orderedFields != null) {
    if (tempResult == null)     return;
    ODocumentSorter.sort(tempResult,orderedFields);
    for (    ODocument doc : tempResult)     request.getResultListener().result(doc);
    orderedFields.clear();
    tempResult.clear();
  }
}
