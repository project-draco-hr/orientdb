{
  final ORawBuffer raw=iStorage.readRecord((ORecordId)record.getIdentity(),null,false,null);
  record.fill((ORecordId)record.getIdentity(),raw.version,raw.buffer,false);
  fromStream(raw.buffer);
}
