{
  OIdentifiable minimum=null;
  for (  OIdentifiable vertex : vertexes) {
    if (minimum == null || getShortestDistance(vertex).compareTo(getShortestDistance(minimum)) < 0)     minimum=vertex;
  }
  return minimum;
}
