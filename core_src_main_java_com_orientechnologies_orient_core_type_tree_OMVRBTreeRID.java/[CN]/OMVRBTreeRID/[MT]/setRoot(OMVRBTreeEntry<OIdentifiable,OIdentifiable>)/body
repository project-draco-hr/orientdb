{
  lock.acquireExclusiveLock();
  try {
    int size=0;
    if (iRoot != null)     size=getTreeSize();
    super.setRoot(iRoot);
    if (iRoot != null)     setSize(size);
  }
  finally {
    lock.releaseExclusiveLock();
  }
}
