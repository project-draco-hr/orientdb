{
  acquireSharedLock();
  try {
    int index=entries.size() - 1;
    while (index >= 0 && entries.get(index) == null)     index--;
    return OClusterPositionFactory.INSTANCE.valueOf(index);
  }
  finally {
    releaseSharedLock();
  }
}
