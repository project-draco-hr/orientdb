{
  try {
    return storage.hideRecord(rid,mode,null);
  }
 catch (  OException e) {
    throw e;
  }
catch (  Exception e) {
    throw new ODatabaseException("Error on deleting record " + rid,e);
  }
}
