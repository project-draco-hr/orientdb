{
  try {
    record.setDatabase(ODatabaseRecordThreadLocal.INSTANCE.get());
    record.reload();
  }
 catch (  Exception e) {
  }
  record.recycle(this);
  fromStream(record.toStream());
  return this;
}
