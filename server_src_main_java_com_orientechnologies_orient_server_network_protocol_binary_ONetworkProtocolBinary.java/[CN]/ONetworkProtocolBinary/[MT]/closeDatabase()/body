{
  setDataCommandInfo("Close Database");
  if (connection != null) {
    if (Boolean.FALSE.equals(tokenBased) && server.getClientConnectionManager().disconnect(connection.id))     sendShutdown();
  }
}
