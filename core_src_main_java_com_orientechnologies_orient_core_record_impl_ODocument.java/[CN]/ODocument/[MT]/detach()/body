{
  if (_fieldValues != null) {
    Object fieldValue;
    for (    Map.Entry<String,Object> entry : _fieldValues.entrySet()) {
      fieldValue=entry.getValue();
      if (fieldValue instanceof ORecord<?>)       _fieldValues.put(entry.getKey(),((ORecord<?>)fieldValue).getIdentity());
    }
  }
  return this;
}
