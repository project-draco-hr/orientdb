{
  try {
    if (this.currentGraph != null) {
      if (this.currentGraph.isClosed())       currentGraph=new OrientGraph("plocal:" + graphDirectoryName);
      this.currentGraph.drop();
    }
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
  deleteDirectory(new File(graphDirectoryName));
}
