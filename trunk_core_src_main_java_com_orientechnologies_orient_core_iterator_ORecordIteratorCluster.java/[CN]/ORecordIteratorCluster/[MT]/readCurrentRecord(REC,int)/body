{
  if (limit > -1 && browsedRecords >= limit)   return null;
  currentClusterPosition+=iMovement;
  if (lowLevelDatabase.executeReadRecord(currentClusterId,currentClusterPosition,iRecord) != null)   browsedRecords++;
  return iRecord;
}
