{
  int clusterId=document.getDatabase().getClusterIdByName(iClassName);
  if (clusterId == -1)   clusterId=document.getDatabase().addLogicalCluster(iClassName,document.getDatabase().getClusterIdByName(OStorage.CLUSTER_INTERNAL_NAME));
  return createClass(iClassName,clusterId);
}
