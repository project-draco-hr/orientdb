{
  acquireSharedLock();
  try {
    if (iPPosition.clusterPosition < 0 || iPPosition.clusterPosition > getLastEntryPosition())     return null;
    return entries.get((int)iPPosition.clusterPosition);
  }
  finally {
    releaseSharedLock();
  }
}
