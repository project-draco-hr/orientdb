{
  this.reader=iReader;
  reader.setConsole(this);
  if (reader.hasPromptSupport())   reader.setPrompt("orientdb> ");
  out=new PrintWriter(reader.getOut());
  err=new PrintWriter(reader.getErr());
}
