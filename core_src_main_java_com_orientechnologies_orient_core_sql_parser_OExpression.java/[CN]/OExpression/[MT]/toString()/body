{
  if (value == null) {
    return "null";
  }
 else   if (value instanceof SimpleNode) {
    return value.toString();
  }
 else   if (value instanceof String) {
    return "\"" + encode("" + value) + "\"";
  }
 else {
    return "" + value;
  }
}
