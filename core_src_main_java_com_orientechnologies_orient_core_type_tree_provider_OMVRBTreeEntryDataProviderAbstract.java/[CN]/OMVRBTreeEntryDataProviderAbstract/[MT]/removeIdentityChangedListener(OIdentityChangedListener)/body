{
  if (identityChangedListener != null) {
    final OIdentityChangedListener identityListener=identityChangedListener.get();
    if (identityListener != null && identityListener.equals(listener))     identityChangedListener=null;
  }
}
