{
  if (obj == null || !obj.getClass().equals(getClass()))   return false;
  final String myMsg=getMessage();
  final String otherMsg=((OException)obj).getMessage();
  if (myMsg == null || otherMsg == null)   return false;
  return myMsg.equals(otherMsg);
}
