{
  if (tempResult != null) {
    for (    OIdentifiable d : tempResult)     if (d != null)     request.getResultListener().result(d);
  }
  if (request instanceof OSQLSynchQuery)   return ((OSQLSynchQuery<ORecordSchemaAware<?>>)request).getResult();
  if (request.getResultListener() != null && request.getResultListener() instanceof OAggregatorResultListener) {
    return ((OAggregatorResultListener)request.getResultListener()).getResult();
  }
  return null;
}
