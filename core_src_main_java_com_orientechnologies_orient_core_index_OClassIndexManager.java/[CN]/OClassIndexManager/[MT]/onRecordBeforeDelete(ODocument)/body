{
  final int version=iDocument.getVersion();
  if (iDocument.fields() == 0) {
    iDocument.reload();
    if (version > -1 && iDocument.getVersion() != version)     throw new OConcurrentModificationException("Cannot delete the record " + iDocument.getIdentity() + "' because the version is not the latest. Probably you are deleting an old record or it has been modified by another user (db=v"+ iDocument.getVersion()+ " your=v"+ version+ ")",iDocument.getIdentity(),iDocument.getVersion(),version);
  }
  return false;
}
