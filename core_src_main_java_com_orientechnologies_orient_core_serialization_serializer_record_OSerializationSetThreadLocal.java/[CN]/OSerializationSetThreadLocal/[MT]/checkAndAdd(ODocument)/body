{
  final Map<ODocument,Boolean> iMarshalledRecords=INSTANCE.get();
  if (iMarshalledRecords.containsKey(document)) {
    return false;
  }
 else   iMarshalledRecords.put(document,Boolean.FALSE);
  return true;
}
