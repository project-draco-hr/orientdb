{
  if (iFieldValue == null)   return;
  if (OMultiValue.isMultiValue(iFieldValue))   for (  Object o : OMultiValue.getMultiValueIterable(iFieldValue)) {
    traverse(o,iCondition);
  }
 else   if (iFieldValue instanceof OIdentifiable)   traverse(iFieldValue,iCondition);
}
