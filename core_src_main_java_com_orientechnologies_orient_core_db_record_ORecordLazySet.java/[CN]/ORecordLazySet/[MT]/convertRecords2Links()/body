{
  if (status == MULTIVALUE_STATUS.ALL_RIDS || database == null)   return;
  final HashSet<Object> copy=new HashSet<Object>();
  for (  Object k : map.keySet())   copy.add(convertRecord2Link(k));
  clear();
  addAll(copy);
  copy.clear();
  status=MULTIVALUE_STATUS.ALL_RIDS;
}
