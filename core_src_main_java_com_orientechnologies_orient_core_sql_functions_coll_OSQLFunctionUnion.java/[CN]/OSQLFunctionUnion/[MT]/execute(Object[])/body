{
  if (iParameters.length == 1) {
    final Object value=iParameters[0];
    if (value != null) {
      if (context == null)       context=new HashSet<Object>();
      if (value instanceof Collection<?>)       context.addAll((Collection<?>)value);
 else       context.add(value);
    }
    return null;
  }
 else {
    final HashSet<Object> result=new HashSet<Object>();
    for (    Object value : iParameters) {
      if (value != null)       if (value instanceof Collection<?>)       result.addAll((Collection<?>)value);
 else       result.add(value);
    }
    return result;
  }
}
