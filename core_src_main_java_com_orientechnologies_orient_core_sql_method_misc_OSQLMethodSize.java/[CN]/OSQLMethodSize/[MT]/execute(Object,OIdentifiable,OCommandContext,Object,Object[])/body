{
  final Number size;
  if (ioResult != null) {
    if (ioResult instanceof ORecord<?>) {
      size=1;
    }
 else {
      size=OMultiValue.getSize(ioResult);
    }
  }
 else {
    size=0;
  }
  return size;
}
