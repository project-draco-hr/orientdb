{
  if (map.containsKey(e))   return false;
  if (e instanceof ORecord)   map.put(e,e);
 else   map.put(e,ENTRY_REMOVAL);
  setDirty();
  if (e instanceof ODocument)   ((ODocument)e).addOwner(this);
  return true;
}
