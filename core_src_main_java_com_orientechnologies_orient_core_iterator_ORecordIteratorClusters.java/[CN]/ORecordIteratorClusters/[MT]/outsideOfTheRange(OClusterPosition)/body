{
  if (beginRange != null && currentPosition.compareTo(beginRange.getClusterPosition()) < 0)   return true;
  if (endRange != null && currentPosition.compareTo(endRange.getClusterPosition()) > 0)   return true;
  return false;
}
