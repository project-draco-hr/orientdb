{
  if (embeddedFields.get(iPojoClass) != null && embeddedFields.get(iPojoClass).contains(iFieldName))   return OType.EMBEDDED;
 else   if (iEntityManager.getEntityClass(iFieldClass.getSimpleName()) != null)   return OType.LINK;
  return null;
}
