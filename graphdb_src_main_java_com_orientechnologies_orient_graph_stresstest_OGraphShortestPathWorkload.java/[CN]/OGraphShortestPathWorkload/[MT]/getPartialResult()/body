{
  return String.format("%d%% [Shortest paths blocks (block size=%d) executed: %d/%d]",((100 * result.current.get() / result.total)),startingVertices.size(),result.current.get(),startingVertices.size());
}
