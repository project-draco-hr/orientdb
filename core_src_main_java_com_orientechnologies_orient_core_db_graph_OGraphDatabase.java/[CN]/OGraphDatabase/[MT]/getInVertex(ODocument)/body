{
  checkEdgeClass(iEdge);
  OIdentifiable v=iEdge.field(EDGE_FIELD_IN);
  if (v != null && v instanceof ORID) {
    v=v.getRecord();
    final boolean wasDirty=iEdge.isDirty();
    iEdge.field(EDGE_FIELD_IN,v);
    if (!wasDirty)     iEdge.unsetDirty();
  }
  return (ODocument)v;
}
