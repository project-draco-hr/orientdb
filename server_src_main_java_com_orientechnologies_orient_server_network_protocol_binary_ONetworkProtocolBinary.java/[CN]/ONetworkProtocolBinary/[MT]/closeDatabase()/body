{
  setDataCommandInfo("Close Database");
  if (connection != null) {
    if (server.getClientConnectionManager().disconnect(connection.getId()) && Boolean.FALSE.equals(connection.getTokenBased()))     sendShutdown();
  }
}
