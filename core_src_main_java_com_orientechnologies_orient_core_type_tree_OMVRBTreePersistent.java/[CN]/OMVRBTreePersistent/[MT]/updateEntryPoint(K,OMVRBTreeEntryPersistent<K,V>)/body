{
  final OMVRBTreeEntryPersistent<K,V> node=entryPoints.remove(iOldKey);
  if (node != iNode)   OLogManager.instance().warn(this,"Entrypoints nodes are different during update: old %s <-> new %s",node,iNode);
  addNodeAsEntrypoint(node);
}
