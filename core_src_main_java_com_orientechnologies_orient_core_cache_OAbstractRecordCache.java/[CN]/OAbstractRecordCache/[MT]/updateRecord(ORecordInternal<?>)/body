{
  if (!enabled)   return;
  acquireExclusiveLock();
  try {
    if (iRecord.isPinned())     entries.put(iRecord.getIdentity(),iRecord);
 else     entries.remove(iRecord.getIdentity());
  }
  finally {
    releaseExclusiveLock();
  }
}
