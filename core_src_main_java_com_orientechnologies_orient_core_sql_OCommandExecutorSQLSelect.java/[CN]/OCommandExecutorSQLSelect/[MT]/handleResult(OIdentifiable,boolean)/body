{
  lastRecord=null;
  if ((orderedFields.isEmpty() || fullySortedByIndex) && skip > 0) {
    skip--;
    return true;
  }
  if (iCloneIt)   lastRecord=iRecord instanceof ORecord<?> ? ((ORecord<?>)iRecord).copy() : iRecord.getIdentity().copy();
 else   lastRecord=iRecord;
  resultCount++;
  boolean result=addResult(lastRecord);
  if (!result)   return false;
  if ((orderedFields.isEmpty() || fullySortedByIndex) && !isAnyFunctionAggregates() && fetchLimit > -1 && resultCount >= fetchLimit)   return false;
  return true;
}
