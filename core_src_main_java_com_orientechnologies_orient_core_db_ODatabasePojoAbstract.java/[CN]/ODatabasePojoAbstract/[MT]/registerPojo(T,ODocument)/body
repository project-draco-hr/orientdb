{
  if (retainObjects) {
    objects2Records.put(System.identityHashCode(iObject),iRecord);
    records2Objects.put(iRecord,iObject);
    final ORID rid=iRecord.getIdentity();
    if (rid.isValid())     rid2Records.put(rid,iRecord);
  }
}
