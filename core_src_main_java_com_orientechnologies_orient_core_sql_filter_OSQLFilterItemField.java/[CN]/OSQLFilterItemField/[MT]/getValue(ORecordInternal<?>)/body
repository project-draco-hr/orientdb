{
  if (iRecord == null)   throw new OCommandExecutionException("expression item '" + name + "' can't be resolved");
  return transformValue(iRecord.getDatabase(),((ODocument)iRecord).rawField(name));
}
