{
  final ODistributedServerManager dManager=getDistributedServerManager();
  if (aligned == -1) {
    ODistributedServerLog.info(this,dManager.getLocalNodeId(),getNodeSource(),DIRECTION.IN,"alignment postponed for db '%s'",databaseName);
    dManager.postponeAlignment(getNodeSource(),databaseName);
  }
 else {
    ODistributedServerLog.info(this,dManager.getLocalNodeId(),getNodeSource(),DIRECTION.IN,"alignment ended against db '%s': %d operation(s)",databaseName,aligned);
    dManager.endAlignment(getNodeSource(),databaseName,aligned);
  }
  return null;
}
