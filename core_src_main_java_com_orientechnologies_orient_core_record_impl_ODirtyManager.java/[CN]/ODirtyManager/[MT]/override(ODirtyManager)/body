{
  if (this == oDirtyManager)   return;
  if (this.overrider != null)   this.overrider.override(oDirtyManager);
 else {
    this.overrider=oDirtyManager;
    this.newRecord=null;
    this.updateRecord=null;
    this.references=null;
  }
}
