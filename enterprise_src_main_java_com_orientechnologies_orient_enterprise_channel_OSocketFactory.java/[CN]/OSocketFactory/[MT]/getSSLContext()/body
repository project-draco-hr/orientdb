{
  if (context == null) {
    try {
      context=SSLContext.getDefault();
    }
 catch (    NoSuchAlgorithmException e) {
      OLogManager.instance().error(this,"Error creating ssl context",e);
    }
  }
  return context;
}
