{
  lock.readLock().lock();
  try {
    int count=0;
    for (    OFileMMap f : bufferPoolPerFile.keySet()) {
      count+=getOverlappedBlocks(f);
    }
    return count;
  }
  finally {
    lock.readLock().unlock();
  }
}
