{
  if (!database.getClusterNames().contains("Animal"))   database.getStorage().addCluster("Animal",OClusterPhysical.TYPE);
  if (!database.getClusterNames().contains("Vaccinate"))   database.getStorage().addCluster("Vaccinate",OClusterPhysical.TYPE);
}
