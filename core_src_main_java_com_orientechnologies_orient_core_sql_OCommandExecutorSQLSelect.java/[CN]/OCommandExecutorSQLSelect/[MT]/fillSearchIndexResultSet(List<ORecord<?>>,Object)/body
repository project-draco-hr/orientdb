{
  if (indexResult != null) {
    if (indexResult instanceof Collection<?>) {
      Collection<OIdentifiable> indexResultSet=(Collection<OIdentifiable>)indexResult;
      if (indexResultSet.size() > 0)       for (      OIdentifiable o : indexResultSet)       fillResultSet(resultSet,o);
    }
 else     fillResultSet(resultSet,(OIdentifiable)indexResult);
  }
}
