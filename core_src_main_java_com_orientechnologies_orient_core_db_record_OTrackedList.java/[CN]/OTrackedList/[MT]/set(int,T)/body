{
  final T oldValue=super.set(index,element);
  if (!oldValue.equals(element))   fireCollectionChangedEvent(new OMultiValueChangeEvent<Integer,T>(OMultiValueChangeEvent.OChangeType.UPDATE,index,element,oldValue));
  return oldValue;
}
