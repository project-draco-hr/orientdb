{
  if (!hasNext())   throw new NoSuchElementException("EOF");
  try {
    final String line=reader.readLine();
    progressBytes+=line.length();
    currentRow++;
    if (reader.ready())     progressBytes++;
    return line;
  }
 catch (  IOException e) {
    throw new OExtractorException(e);
  }
}
