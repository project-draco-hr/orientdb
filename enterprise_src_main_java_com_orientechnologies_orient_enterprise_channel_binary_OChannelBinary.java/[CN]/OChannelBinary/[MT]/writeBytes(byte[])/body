{
  if (debug)   OLogManager.instance().debug(this,"Writing bytes (4+%d=%d bytes): %s",iContent != null ? iContent.length : 0,iContent != null ? iContent.length + 4 : 4,iContent);
  if (iContent == null) {
    out.writeInt(-1);
  }
 else {
    out.writeInt(iContent.length);
    out.write(iContent);
  }
  return this;
}
