{
  acquireExclusiveLock();
  try {
    diskCache.truncateFile(fileId);
    clusterStateHolder.truncate();
    size=0;
    recordsSize=0;
    for (int i=0; i < freePageIndexes.length; i++)     freePageIndexes[i]=-1;
  }
  finally {
    releaseExclusiveLock();
  }
}
