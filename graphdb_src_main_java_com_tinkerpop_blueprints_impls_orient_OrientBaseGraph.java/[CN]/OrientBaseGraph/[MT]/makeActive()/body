{
  currentGraph.set(this);
  if (settings.threadMode == THREAD_MODE.MANUAL)   return;
  final ODatabaseDocument tlDb=ODatabaseRecordThreadLocal.INSTANCE.getIfDefined();
  if (settings.threadMode == THREAD_MODE.ALWAYS_AUTOSET || tlDb == null) {
    if (database != null && tlDb != database)     ODatabaseRecordThreadLocal.INSTANCE.set(database);
  }
}
