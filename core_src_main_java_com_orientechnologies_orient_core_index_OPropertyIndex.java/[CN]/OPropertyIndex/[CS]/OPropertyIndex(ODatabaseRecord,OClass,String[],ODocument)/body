{
  fields=iFields;
  final String indexName=getIndexName(iClass,iFields);
  delegate=iDatabase.getMetadata().getIndexManager().loadIndex(indexName,iConfiguration);
  if (delegate != null)   delegate.setCallback(this);
}
