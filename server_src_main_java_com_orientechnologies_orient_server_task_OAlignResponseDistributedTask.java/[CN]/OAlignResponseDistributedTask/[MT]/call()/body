{
  OLogManager.instance().warn(this,"DISTRIBUTED <- alignment ended from %s database %s: %d operations",nodeSource,databaseName,aligned);
  final ODistributedServerManager dManager=getDistributedServerManager();
  dManager.endAlignment(nodeSource,databaseName);
  return null;
}
