{
  acquireWriteLock();
  try {
    if (headerBuffer != null) {
      headerBuffer.putLong(HEADER_DATA_OFFSET + iPosition,iValue);
      setHeaderDirty();
    }
  }
  finally {
    releaseWriteLock();
  }
}
