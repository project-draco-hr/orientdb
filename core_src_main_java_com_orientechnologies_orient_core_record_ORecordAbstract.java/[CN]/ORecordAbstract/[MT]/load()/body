{
  if (database == null)   throw new ODatabaseException("No database assigned to current record");
  if (database.load(this) == null)   throw new ORecordNotFoundException("Record id: " + getIdentity());
  return this;
}
