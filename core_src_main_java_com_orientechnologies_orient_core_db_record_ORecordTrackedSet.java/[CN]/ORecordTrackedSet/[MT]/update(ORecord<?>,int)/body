{
  Object entry;
  for (Iterator<Object> it=map.keySet().iterator(); it.hasNext(); ) {
    entry=it.next();
    if ((Integer)entry == iOldHashCode) {
      it.remove();
      add(iRecord);
      setDirty();
      return true;
    }
  }
  return false;
}
