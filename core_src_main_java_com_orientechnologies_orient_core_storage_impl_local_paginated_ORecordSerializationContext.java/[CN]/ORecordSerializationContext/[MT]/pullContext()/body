{
  final Deque<ORecordSerializationContext> stack=SERIALIZATION_CONTEXT_STACK.get();
  if (stack.isEmpty())   throw new IllegalStateException("Can not find current serialization context");
  final ORecordSerializationContext result=stack.poll();
  if (stack.isEmpty())   currentSchema.set(null);
  return result;
}
