{
  if (!jumpWhiteSpaces())   return null;
  int begin=currentPos;
  for (; currentPos < text.length(); ++currentPos)   if (Character.isWhitespace(text.charAt(currentPos)))   break;
  return iForceUpperCase ? textUpperCase.substring(begin,currentPos) : text.substring(begin,currentPos);
}
