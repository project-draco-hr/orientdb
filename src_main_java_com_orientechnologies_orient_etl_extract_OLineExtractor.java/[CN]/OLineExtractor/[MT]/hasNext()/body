{
  if (reader == null)   return false;
  try {
    final boolean res=reader.ready();
    if (!res)     end();
    return res;
  }
 catch (  IOException e) {
    throw new OExtractorException(e);
  }
}
