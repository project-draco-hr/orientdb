{
  final Set<OIndex<?>> indexes=getClassIndexes();
  if (superClass == null)   return indexes;
  final Set<OIndex<?>> result=new HashSet<OIndex<?>>(indexes);
  result.addAll(superClass.getIndexes());
  return result;
}
