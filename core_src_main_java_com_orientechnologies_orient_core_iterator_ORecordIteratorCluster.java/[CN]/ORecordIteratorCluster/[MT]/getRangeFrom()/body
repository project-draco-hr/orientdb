{
  if (!liveUpdated)   return firstClusterPosition - 1;
  final long limit=database.getStorage().getClusterDataRange(currentClusterId)[0] - 1;
  if (rangeFrom > -1)   return Math.max(rangeFrom,limit);
  return limit;
}
