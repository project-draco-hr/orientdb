{
  database.open("admin","admin");
  List<ODocument> result=database.command(new OCommandSQL("select from index:MapPoint.x where key between 150.00 and 155.45")).execute();
  Assert.assertTrue(result.size() > 0);
  database.close();
}
