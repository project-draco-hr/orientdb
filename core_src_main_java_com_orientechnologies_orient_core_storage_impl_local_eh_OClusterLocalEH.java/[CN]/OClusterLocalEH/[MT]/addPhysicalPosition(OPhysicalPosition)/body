{
  acquireExclusiveLock();
  try {
    if (localHashTable.get(iPPosition.clusterPosition) != null)     return false;
    localHashTable.put(iPPosition.clusterPosition,iPPosition);
    return true;
  }
  finally {
    releaseExclusiveLock();
  }
}
