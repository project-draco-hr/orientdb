{
  if (iRecord == null)   throw new OCommandExecutionException("expression item '" + name + "' cannot be resolved because current record is NULL");
  return ORecordSerializerBinary.INSTANCE.getCurrentSerializer().deserializeField(new BytesContainer(iRecord.getRecord().toStream()).skip(1),name);
}
