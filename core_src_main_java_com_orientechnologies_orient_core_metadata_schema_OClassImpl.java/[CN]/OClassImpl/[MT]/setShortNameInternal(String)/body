{
  getDatabase().checkSecurity(ODatabaseSecurityResources.SCHEMA,ORole.PERMISSION_UPDATE);
  if (this.shortName != null)   owner.classes.remove(this.shortName);
  this.shortName=iShortName;
  if (null != iShortName)   owner.classes.put(iShortName.toLowerCase(),this);
}
