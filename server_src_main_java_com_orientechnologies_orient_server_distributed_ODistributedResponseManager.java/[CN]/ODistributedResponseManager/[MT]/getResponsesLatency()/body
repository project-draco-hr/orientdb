{
  final Map<String,Long> latencies=new HashMap<String,Long>();
  for (  Map.Entry<String,OResponseData> entry : responses.entrySet())   if (entry.getValue() != NO_RESPONSE)   latencies.put(entry.getKey(),entry.getValue().receivedOn);
  return latencies;
}
