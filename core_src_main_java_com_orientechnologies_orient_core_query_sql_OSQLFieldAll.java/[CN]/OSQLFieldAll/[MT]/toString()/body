{
  StringBuilder buffer=new StringBuilder();
  buffer.append("ALL(");
  if (names != null) {
    int i=0;
    for (    String n : names) {
      if (i++ > 0)       buffer.append(", ");
      buffer.append(n);
    }
  }
  buffer.append(")");
  return buffer.toString();
}
