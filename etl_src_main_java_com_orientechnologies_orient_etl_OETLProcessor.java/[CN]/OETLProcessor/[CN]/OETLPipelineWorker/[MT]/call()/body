{
  pipeline.getDocumentDatabase();
  while (!extractionFinished.get() || !queue.isEmpty()) {
    try {
      final OExtractedItem content=queue.poll();
      if (content != null)       pipeline.execute(content);
    }
 catch (    OException e) {
      throw e;
    }
  }
  return Boolean.TRUE;
}
