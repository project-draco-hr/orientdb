{
  keys=(K[])new Object[iSource.keys.length];
  for (int i=0; i < iSource.keys.length; ++i)   keys[i]=iSource.keys[i];
  values=(V[])new Object[iSource.values.length];
  for (int i=0; i < iSource.values.length; ++i)   values[i]=iSource.values[i];
  size=iSource.size;
}
