{
  acquireSharedLock();
  try {
    if (writeAheadLog == null)     return;
    writeAheadLog.logRecord(new OClusterStateRecord(size,recordsSize,name));
  }
  finally {
    releaseSharedLock();
  }
}
