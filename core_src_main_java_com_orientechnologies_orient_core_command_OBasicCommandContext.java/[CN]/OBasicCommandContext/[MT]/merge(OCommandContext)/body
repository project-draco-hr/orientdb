{
  if (iContext == null)   return this;
  if (inherited != null) {
    if (!inherited.contains(iContext))     inherited.add(iContext);
  }
 else {
    inherited=new ArrayList<OCommandContext>();
    inherited.add(iContext);
  }
  return this;
}
