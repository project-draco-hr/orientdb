{
  if (typeId == 1)   return new BinaryDiff();
  if (typeId == 2)   return new IntDiff();
  if (typeId == 3)   return new LongDiff();
  throw new IllegalArgumentException("Unknown Diff id " + typeId);
}
