{
  final ODatabaseRecord db=ODatabaseRecordThreadLocal.INSTANCE.get();
  final OStorageSynchronizer replicator=replicators.get(db.getName());
  if (replicator != null)   return replicator.distributeOperation(iType,iRecord);
  return false;
}
