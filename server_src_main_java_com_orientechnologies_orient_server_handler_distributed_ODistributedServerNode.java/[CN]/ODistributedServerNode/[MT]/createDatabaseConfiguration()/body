{
  final ODocument config=new ODocument();
  config.field("servers",new ODocument(discoveryManager.getName(),new ODocument("update-delay",discoveryManager.getServerUpdateDelay())));
  config.field("clusters",new ODocument("*",new ODocument("owner",discoveryManager.getName())));
  return config;
}
