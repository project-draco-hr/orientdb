{
  acquireSharedLock();
  try {
    if (iPPosition.clusterPosition.intValue() < 0 || iPPosition.clusterPosition.intValue() > getLastEntryPosition())     return null;
    return entries.get((int)iPPosition.clusterPosition.intValue());
  }
  finally {
    releaseSharedLock();
  }
}
