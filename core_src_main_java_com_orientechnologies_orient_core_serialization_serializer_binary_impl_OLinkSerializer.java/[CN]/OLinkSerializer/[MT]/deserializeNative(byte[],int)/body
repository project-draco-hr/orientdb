{
  int clusterId=OShortSerializer.INSTANCE.deserializeNative(stream,startPosition);
  OClusterPosition clusterPosition=OClusterPositionFactory.INSTANCE.fromStream(stream,startPosition + OShortSerializer.SHORT_SIZE);
  return new ORecordId(clusterId,clusterPosition);
}
