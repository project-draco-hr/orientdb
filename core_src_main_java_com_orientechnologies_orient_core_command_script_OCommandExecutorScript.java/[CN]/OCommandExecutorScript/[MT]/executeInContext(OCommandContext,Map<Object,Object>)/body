{
  final String language=request.getLanguage();
  parserText=request.getText();
  parameters=iArgs;
  if (language.equalsIgnoreCase("SQL"))   return executeSQL();
 else   return executeJsr223Script(language,iContext,iArgs);
}
