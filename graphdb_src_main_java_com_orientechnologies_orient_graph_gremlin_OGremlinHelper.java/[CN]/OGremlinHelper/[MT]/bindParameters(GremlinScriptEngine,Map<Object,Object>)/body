{
  String output=null;
  final Map<Object,Object> params=iParameters;
  if (params != null) {
    for (    Entry<Object,Object> param : params.entrySet()) {
      if (param.getKey().toString().trim().equals(PARAM_OUTPUT)) {
        output=param.getValue().toString();
        continue;
      }
      iEngine.getBindings(ScriptContext.ENGINE_SCOPE).put(param.getKey().toString(),param.getValue());
    }
  }
  return output;
}
