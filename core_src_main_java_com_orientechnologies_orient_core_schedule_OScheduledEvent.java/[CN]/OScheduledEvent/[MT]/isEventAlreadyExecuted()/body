{
  document.reload();
  if (getNextExecutionId() >= nextExecutionId) {
    OLogManager.instance().info(this,"Scheduled event '%s' with id %d is already running (current id=%d)",getName(),nextExecutionId,getNextExecutionId());
    return true;
  }
  return false;
}
