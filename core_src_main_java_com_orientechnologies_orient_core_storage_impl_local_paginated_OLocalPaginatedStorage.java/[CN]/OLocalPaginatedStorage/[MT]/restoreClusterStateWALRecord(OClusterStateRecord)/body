{
  int clusterId=clusterStateRecord.getClusterId();
  OLocalPaginatedCluster cluster=getClusterById(clusterId);
  if (cluster == null)   OLogManager.instance().error(this,"Cluster with id %d is absent, log record will be skipped.",clusterId);
 else   cluster.restoreClusterState(clusterStateRecord);
}
