{
  List<ODocument> result=database.command(new OSQLSynchQuery<ODocument>("traverse out from " + tomCruise.getIdentity())).execute();
  Assert.assertTrue(result.size() != 0);
  for (  ODocument d : result) {
  }
}
