{
  super(iStorage);
  segment=new OSingleFileSegment((OStorageLocalAbstract)storage,new OStorageFileConfiguration(null,getDirectory() + "/database.ocf","classic",fileTemplate.maxSize,fileTemplate.fileIncrementSize));
}
