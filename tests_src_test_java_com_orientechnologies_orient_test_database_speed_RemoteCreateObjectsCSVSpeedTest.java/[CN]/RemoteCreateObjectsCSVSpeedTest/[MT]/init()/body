{
  Orient.instance().registerEngine(new OEngineRemote());
  if (!database.getStorage().getClusterNames().contains("Animal"))   database.getStorage().addCluster("Animal",OStorage.CLUSTER_TYPE.PHYSICAL);
}
