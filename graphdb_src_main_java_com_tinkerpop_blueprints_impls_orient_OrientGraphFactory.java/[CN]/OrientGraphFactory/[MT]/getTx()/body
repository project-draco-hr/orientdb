{
  final OrientGraph g;
  if (pool == null) {
    g=(OrientGraph)new OrientGraph(getDatabase()).configure(settings);
    initGraph(g);
  }
 else   return (OrientGraph)pool.getResource(url,0,user,password);
  return g;
}
