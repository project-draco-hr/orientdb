{
  final String format=iFetchPlan != null ? JSON_FORMAT + ",fetchPlan:" + iFetchPlan : JSON_FORMAT;
  if (iRecord != null)   sendTextContent(OHttpUtils.STATUS_OK_CODE,"OK",null,OHttpUtils.CONTENT_JSON,iRecord.toJSON(format));
}
