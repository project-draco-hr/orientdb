{
  if (inEdges != null && inEdges.get() != null) {
    while (!inEdges.get().isEmpty())     inEdges.get().get(0).delete();
  }
  if (outEdges != null && outEdges.get() != null) {
    while (!outEdges.get().isEmpty())     outEdges.get().get(0).delete();
  }
  document.delete();
}
