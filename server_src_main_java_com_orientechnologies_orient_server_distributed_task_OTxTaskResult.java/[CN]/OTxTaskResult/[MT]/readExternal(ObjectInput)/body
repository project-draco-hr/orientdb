{
  final int resultSize=in.readInt();
  for (int i=0; i < resultSize; ++i)   results.add(in.readObject());
  final int locksSize=in.readInt();
  for (int i=0; i < locksSize; ++i)   locks.add((ORID)in.readObject());
}
