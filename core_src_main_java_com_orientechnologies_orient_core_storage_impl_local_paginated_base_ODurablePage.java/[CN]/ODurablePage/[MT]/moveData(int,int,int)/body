{
  if (len == 0)   return;
  if (changes != null) {
    changes.moveData(buffer,from,to,len);
  }
 else {
    final ByteBuffer rb=buffer.asReadOnlyBuffer();
    rb.position(from);
    rb.limit(from + len);
    buffer.put(rb);
  }
  cacheEntry.markDirty();
}
