{
  if (cachedClusterNodes.containsKey(iNodeName)) {
    final Boolean nodeStatus=(Boolean)getConfigurationMap().get(OHazelcastPlugin.CONFIG_STATUS_PREFIX + iNodeName + "."+ iDatabaseName);
    if (nodeStatus != null && nodeStatus)     return nodeStatus;
  }
  return false;
}
