{
  if (url.startsWith(OEngineRemote.NAME))   new OServerAdmin(url).connect().createDatabase("local").close();
 else {
    database=new ODatabaseObjectTx(url);
    database.create();
    database.close();
  }
}
