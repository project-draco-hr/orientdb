{
  final OIndex<?> index=db.getMetadata().getIndexManager().createIndex("manualHashIndex",OClass.INDEX_TYPE.UNIQUE_HASH_INDEX.toString(),new OSimpleKeyIndexDefinition(OType.STRING),null,null,null);
  Assert.assertNotNull(index);
}
