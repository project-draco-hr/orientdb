{
  setDirty();
  boolean modified=false;
  final ODatabasePojoAbstract<TYPE> database=getDatabase();
  for (  Object o : c)   if (!underlying.remove(database.getRecordByUserObject(o,false)))   modified=true;
  return modified;
}
