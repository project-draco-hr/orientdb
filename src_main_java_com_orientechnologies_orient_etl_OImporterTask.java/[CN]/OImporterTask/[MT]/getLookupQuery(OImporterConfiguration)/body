{
  final OPair<String,String> key=template.getKey();
  return key != null && key.getValue().equals("overwrite") ? new OSQLSynchQuery<ODocument>("select from " + template.getClassName() + " where "+ key.getKey()+ " = ?") : null;
}
