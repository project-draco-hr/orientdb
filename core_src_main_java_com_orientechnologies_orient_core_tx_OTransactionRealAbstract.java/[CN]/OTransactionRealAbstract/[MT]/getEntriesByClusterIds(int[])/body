{
  final List<OTransactionEntry> result=new ArrayList<OTransactionEntry>();
  if (iIds == null)   for (  OTransactionEntry entry : entries.values()) {
    result.add(entry);
  }
 else   for (  OTransactionEntry entry : entries.values()) {
    for (    int id : iIds) {
      if (entry.getRecord() != null && entry.getRecord().getIdentity().getClusterId() == id) {
        result.add(entry);
        break;
      }
    }
  }
  return result;
}
