{
  if (compressions.containsKey(compression.name()))   throw new IllegalArgumentException("Compression with name " + compression.name() + " was already registered.");
  compressions.put(compression.name(),compression);
}
