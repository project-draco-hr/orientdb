{
  if (iStartSize == -1)   iStartSize=DEFAULT_SIZE;
  openChannel(iStartSize);
  setFilledUpTo(0);
  setSize(maxSize > 0 && iStartSize > maxSize ? maxSize : iStartSize);
  setSoftlyClosed(!failCheck);
}
