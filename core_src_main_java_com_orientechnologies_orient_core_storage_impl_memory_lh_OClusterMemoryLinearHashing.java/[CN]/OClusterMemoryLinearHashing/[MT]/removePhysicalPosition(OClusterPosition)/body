{
  acquireExclusiveLock();
  try {
    final OPhysicalPosition physicalPosition=content.delete(clusterPosition);
    if (physicalPosition != null && physicalPosition.recordVersion.isTombstone())     tombstonesCount--;
  }
  finally {
    releaseExclusiveLock();
  }
}
