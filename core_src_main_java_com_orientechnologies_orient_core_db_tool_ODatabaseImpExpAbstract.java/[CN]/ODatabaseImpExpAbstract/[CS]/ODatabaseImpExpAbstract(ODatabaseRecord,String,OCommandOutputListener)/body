{
  database=iDatabase;
  fileName=iFileName;
  if (fileName.indexOf('.') == -1)   fileName+=DEFAULT_EXT;
  listener=iListener;
  excludeClusters=new LinkedHashSet<String>();
  excludeClusters.add("index");
}
