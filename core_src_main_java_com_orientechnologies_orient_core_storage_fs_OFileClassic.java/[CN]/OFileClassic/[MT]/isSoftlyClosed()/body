{
  acquireReadLock();
  try {
    final ByteBuffer buffer;
    if (version == 0)     buffer=readData(SOFTLY_CLOSED_OFFSET_V_0,1);
 else     buffer=readData(SOFTLY_CLOSED_OFFSET,1);
    return buffer.get(0) > 0;
  }
  finally {
    releaseReadLock();
  }
}
