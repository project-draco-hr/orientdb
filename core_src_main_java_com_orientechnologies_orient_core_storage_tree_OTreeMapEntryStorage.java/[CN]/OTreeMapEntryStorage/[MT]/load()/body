{
  ORawBuffer raw=((OTreeMapStorage<K,V>)tree).storage.readRecord(null,-1,record.getIdentity().getClusterId(),record.getIdentity().getClusterPosition(),null);
  record.setVersion(raw.version);
  fromStream(raw.buffer);
  return this;
}
