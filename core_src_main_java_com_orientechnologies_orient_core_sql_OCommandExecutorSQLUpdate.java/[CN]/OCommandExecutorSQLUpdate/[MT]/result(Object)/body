{
  ORecordSchemaAware<?> record=(ORecordSchemaAware<?>)iRecord;
  for (  Map.Entry<String,Object> entry : fieldEntries.entrySet())   record.field(entry.getKey(),entry.getValue());
  record.save();
  recordCount++;
  return true;
}
