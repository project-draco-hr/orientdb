{
  acquireSharedLock();
  try {
    return OClusterPositionFactory.INSTANCE.valueOf(endOffsetData > -1 ? endOffsetData : fileSegment.getFilledUpTo() / RECORD_SIZE - 1);
  }
  finally {
    releaseSharedLock();
  }
}
