{
  if (indexResultSet != null && indexResultSet.size() > 0)   for (  OIdentifiable o : indexResultSet) {
    if (rangeFrom != FIRST && o.getIdentity().compareTo(rangeFrom) <= 0)     continue;
    if (rangeTo != LAST && o.getIdentity().compareTo(rangeTo) > 0)     continue;
    if (o instanceof ORID)     resultSet.add(database.load((ORID)o));
 else     resultSet.add((ORecord<?>)o);
  }
}
