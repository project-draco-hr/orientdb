{
  lock.acquireSharedLock();
  try {
    long size=0;
    for (    ODataLocal d : dataSegments)     size+=d.getFilledUpTo();
    for (    OCluster c : clusters)     if (c != null)     size+=c.getSize();
    return size;
  }
  finally {
    lock.releaseSharedLock();
  }
}
