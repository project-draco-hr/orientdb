{
  if (instance != null)   instance.sendShutdown();
  if (pluginClassLoader != null)   try {
    pluginClassLoader.close();
  }
 catch (  IOException e) {
    OLogManager.instance().error(this,"Error during shutdown of plugin '%s'",e,name);
  }
}
