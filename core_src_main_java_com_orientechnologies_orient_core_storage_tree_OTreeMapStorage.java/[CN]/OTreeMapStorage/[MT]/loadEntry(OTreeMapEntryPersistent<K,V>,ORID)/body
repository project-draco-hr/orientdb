{
  OTreeMapEntryStorage<K,V> entry=(OTreeMapEntryStorage<K,V>)cache.get(iRecordId);
  if (entry == null) {
    entry=new OTreeMapEntryStorage<K,V>(this,iParent,iRecordId);
    cache.put(iRecordId,entry);
  }
 else   entry.setParent(iParent);
  return entry;
}
