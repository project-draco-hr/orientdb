{
  if (debug)   OLogManager.instance().debug(this,"Reading string set. Reading string set items as int (4 bytes)...");
  int items=in.readInt();
  if (debug)   OLogManager.instance().debug(this,"Read string set items: " + items);
  if (items < 0)   return null;
  Set<String> result=new HashSet<String>();
  for (int i=0; i < items; ++i)   result.add(readString());
  if (debug)   OLogManager.instance().debug(this,"Read string set with %d items: ",items);
  return result;
}
