{
  if (_database != null && !_database.isValidationEnabled())   return;
  checkForLoading();
  checkForFields();
  if (_clazz != null)   for (  OProperty p : _clazz.properties()) {
    validateField(this,p);
  }
}
