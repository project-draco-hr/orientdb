{
  acquireWriteLock();
  try {
    if (force || iHow != filledUpTo) {
      filledUpTo=iHow;
      headerBuffer.putLong(FILLEDUPTO_OFFSET,filledUpTo);
      setHeaderDirty();
    }
  }
  finally {
    releaseWriteLock();
  }
}
