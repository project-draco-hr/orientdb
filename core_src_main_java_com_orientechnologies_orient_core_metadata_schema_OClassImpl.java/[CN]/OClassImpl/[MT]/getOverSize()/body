{
  acquireSchemaReadLock();
  try {
    if (overSize > 0)     return overSize;
    if (superClass != null)     return superClass.getOverSize();
    return 0;
  }
  finally {
    releaseSchemaReadLock();
  }
}
