{
  int entryPosition=OIntegerSerializer.INSTANCE.deserializeNative(dataBuffer,POSITIONS_ARRAY_OFFSET + index * OIntegerSerializer.INT_SIZE);
  return keySerializer.deserializeNative(dataBuffer,entryPosition);
}
