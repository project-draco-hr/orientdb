{
  try {
    while (running) {
      try {
        printStats(databaseUrl);
        Thread.sleep(delayReader);
      }
 catch (      Exception e) {
        running=false;
        break;
      }
    }
  }
  finally {
    printStats(databaseUrl);
  }
  return null;
}
