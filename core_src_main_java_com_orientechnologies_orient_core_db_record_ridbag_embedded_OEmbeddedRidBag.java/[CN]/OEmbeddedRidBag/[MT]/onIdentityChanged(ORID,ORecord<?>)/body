{
  if (!prevRid.isValid()) {
    final OModifiableInteger counter=newEntries.remove(record);
    if (counter != null)     entries.put(record,counter);
  }
 else {
    final OModifiableInteger counter=entries.remove(prevRid);
    if (counter != null)     entries.put(record,counter);
  }
}
