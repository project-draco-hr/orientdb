{
  iKey=delegate.getCollatingValue(iKey);
  if (removedKeys.contains(iKey))   return null;
  OIdentifiable updated=updatedKeys.get(iKey);
  if (updated != null)   return updated;
  return delegate.get(iKey);
}
