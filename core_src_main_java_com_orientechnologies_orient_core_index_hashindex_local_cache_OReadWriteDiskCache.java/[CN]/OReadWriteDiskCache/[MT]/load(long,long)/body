{
synchronized (syncObject) {
    OCacheEntry cacheEntry=writeCache.load(fileId,pageIndex);
    if (cacheEntry != null)     return cacheEntry.dataPointer;
    cacheEntry=readCache.load(fileId,pageIndex);
    return cacheEntry.dataPointer;
  }
}
