{
  if (pool != null && transactional != iTransactional)   throw new IllegalArgumentException("Cannot change transactional state after creating the pool");
  this.transactional=iTransactional;
  return this;
}
