{
  if (!isRecording())   return null;
  final Map<String,Object> result=new HashMap<String,Object>();
  for (  Map.Entry<String,OProfilerHookValue> v : hooks.entrySet())   result.put(v.getKey(),v.getValue().getValue());
  return result;
}
