{
  try {
    super.create();
    metadata.create();
    if (getStorage() instanceof OStorageLocal)     createRolesAndUsers();
    dictionary.create();
  }
 catch (  Exception e) {
    throw new ODatabaseException("Can't create database",e);
  }
  return (DB)this;
}
