{
  lock.writeLock().lock();
  try {
    for (    OMMapBufferEntry entry : new ArrayList<OMMapBufferEntry>(bufferPoolLRU))     removeEntry(entry);
    bufferPoolLRU.clear();
    bufferPoolPerFile.clear();
    totalMemory=0;
  }
  finally {
    lock.writeLock().unlock();
  }
}
