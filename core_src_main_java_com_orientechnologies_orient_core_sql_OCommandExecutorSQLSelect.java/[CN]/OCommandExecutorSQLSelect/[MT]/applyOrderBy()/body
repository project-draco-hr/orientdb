{
  if (orderedFields == null || tempResult == null)   return;
  ODocumentSorter.sort(tempResult,orderedFields);
  for (  ODocument doc : tempResult)   sendResultToListener(doc);
  orderedFields.clear();
  tempResult.clear();
}
