{
  ORawBuffer raw=((OTreeMapStorage<K,V>)tree).storage.readRecord(-1,record.getIdentity().getClusterId(),record.getIdentity().getClusterPosition());
  record.setVersion(raw.version);
  fromStream(raw.buffer);
  return this;
}
