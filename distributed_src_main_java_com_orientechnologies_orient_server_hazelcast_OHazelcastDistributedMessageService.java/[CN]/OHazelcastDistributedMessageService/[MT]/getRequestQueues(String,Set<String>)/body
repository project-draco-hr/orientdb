{
  final IQueue<ODistributedRequest>[] queues=new IQueue[nodes.size()];
  int i=0;
  for (  String node : nodes)   queues[i++]=getNodeQueue(getRequestQueueName(node,iDatabaseName));
  return queues;
}
