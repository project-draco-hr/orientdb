{
  acquireExclusiveLock();
  try {
    final Number removed=getDatabase().<OCommandRequest>command(new OSQLSynchQuery<ODocument>("delete from ORole where name = '" + iRoleName + "'")).execute();
    return removed != null && removed.intValue() > 0;
  }
  finally {
    releaseExclusiveLock();
  }
}
