{
  lastReset=new Date();
  if (counters != null) {
synchronized (counters) {
      counters.clear();
    }
  }
  if (chronos != null) {
synchronized (chronos) {
      chronos.clear();
    }
  }
  if (stats != null) {
synchronized (stats) {
      stats.clear();
    }
  }
}
