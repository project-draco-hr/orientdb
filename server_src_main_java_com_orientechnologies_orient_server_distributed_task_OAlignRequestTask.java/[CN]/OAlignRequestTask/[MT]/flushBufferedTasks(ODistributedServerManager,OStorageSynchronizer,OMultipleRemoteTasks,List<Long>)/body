{
  @SuppressWarnings("unused") final Object[] result=(Object[])dManager.sendOperation2Node(getNodeSource(),tasks);
  final int aligned=tasks.getTasks();
  tasks.clearTasks();
  positions.clear();
  return aligned;
}
