{
  try {
    if (!storage.deleteRecord(id,iRid,iVersion))     throw new ORecordNotFoundException("The record with id #" + iRid + " was not found");
  }
 catch (  Exception e) {
    OLogManager.instance().exception("Error on deleting record #" + iRid,e,ODatabaseException.class);
  }
}
