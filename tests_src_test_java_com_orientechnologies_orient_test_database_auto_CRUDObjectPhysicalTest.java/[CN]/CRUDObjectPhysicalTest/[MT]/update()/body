{
  database.open("admin","admin");
  int i=0;
  Animal a;
  for (  Object o : database.browseCluster("Animal")) {
    a=(Animal)o;
    if (i % 2 == 0)     a.setLocation("Spain");
    a.setPrice(i + 100);
    database.save(a);
    i++;
  }
  database.close();
}
