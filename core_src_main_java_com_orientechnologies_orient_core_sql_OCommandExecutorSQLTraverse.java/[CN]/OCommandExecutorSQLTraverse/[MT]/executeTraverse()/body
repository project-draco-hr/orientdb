{
  if (target == null)   throw new OCommandExecutionException("Traverse error: target not specified");
  context=new OTraverseContext();
  for (  OIdentifiable id : target) {
    traverse(id,compiledFilter.getRootCondition());
  }
}
