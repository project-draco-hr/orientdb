{
  boolean changed=false;
  final Iterator<OIdentifiable> it=rawIterator();
  while (it.hasNext()) {
    final OIdentifiable o=it.next();
    if (o instanceof ORecordElement)     if (((ORecordElement)o).setDatabase(iDatabase))     changed=true;
  }
  return changed;
}
