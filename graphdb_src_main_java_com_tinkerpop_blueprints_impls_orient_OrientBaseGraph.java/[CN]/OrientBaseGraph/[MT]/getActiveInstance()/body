{
  final OrientBaseGraph graph=currentGraph.get();
  if (graph != null) {
    final ODatabaseDocument tlDb=ODatabaseRecordThreadLocal.INSTANCE.getIfDefined();
    if (graph.database != null && tlDb != graph.database)     ODatabaseRecordThreadLocal.INSTANCE.set(graph.database);
  }
  return graph;
}
