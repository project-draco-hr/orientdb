{
synchronized (syncObject) {
    flushBuffer();
    am.clear();
    a1in.clear();
    a1out.clear();
    for (    Set<Long> fileEntries : filesPages.values())     fileEntries.clear();
    for (    SortedSet<Long> fileDirtyPages : dirtyPages.values())     fileDirtyPages.clear();
  }
}
