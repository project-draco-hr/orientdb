{
  if (changes != null) {
    changes.setByteValue(buffer,value,pageOffset + PAGE_PADDING);
  }
 else   buffer.put(pageOffset + PAGE_PADDING,value);
  cacheEntry.markDirty();
  return OByteSerializer.BYTE_SIZE;
}
