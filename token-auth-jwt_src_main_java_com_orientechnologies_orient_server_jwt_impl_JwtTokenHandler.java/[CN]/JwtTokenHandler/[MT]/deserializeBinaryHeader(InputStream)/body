{
  try {
    int size=bais.read();
    byte[] data=new byte[size];
    bais.read(data);
    return mapper.readValue(data,OrientJwtHeader.class);
  }
 catch (  Exception e) {
    throw new OException(e);
  }
}
