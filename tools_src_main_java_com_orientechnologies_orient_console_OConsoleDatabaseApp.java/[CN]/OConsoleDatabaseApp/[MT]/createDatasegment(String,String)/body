{
  checkCurrentDatabase();
  if (iPath != null)   out.println("Creating data-segment [" + iName + "] in database "+ currentDatabaseName+ " in path: "+ iPath+ "...");
 else   out.println("Creating data-segment [" + iName + "] in database directory...");
  currentDatabase.addDataSegment(iName,iPath);
  updateDatabaseInfo();
}
