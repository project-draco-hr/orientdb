{
  setDirty();
  if (orphanRemoval)   for (  OIdentifiable value : recordList)   ((OObjectProxyMethodHandler)sourceRecord.getHandler()).getOrphans().add(value.getIdentity());
  recordList.clear();
  super.clear();
}
