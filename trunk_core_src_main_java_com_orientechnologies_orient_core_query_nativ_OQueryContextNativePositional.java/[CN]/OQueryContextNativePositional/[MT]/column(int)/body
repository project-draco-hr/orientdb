{
  if (result != null && result.booleanValue())   return this;
  if (iIndex >= record.size())   throw new OQueryExecutionException("Column " + iIndex + " not exists. Columns range is: 0-"+ (record.size() - 1));
  currentValue=((ORecordPositional<?>)record).field(iIndex);
  return this;
}
